# Current DNS Zone — klphotography.ie

**Snapshot taken:** 2026-05-17T11:11:22Z
**Resolver used:** 1.1.1.1 (Cloudflare public)
**Command set:** `Resolve-DnsName klphotography.ie -Server 1.1.1.1 -Type {ANY|NS|A|MX|TXT|SOA} -DnsOnly` (PowerShell — `dig` unavailable on Windows host)

> Source of truth: whatever the live public DNS returned at the timestamp above.
> Do NOT edit this file once captured — it is a forensic record for Phase 6.

## NS (nameservers — these are what we will replace at the IEDR registrar in Phase 6)

| NameHost | TTL |
|----------|-----|
| ns10.wixdns.net | 86400 |
| ns11.wixdns.net | 86400 |

## SOA

| Field | Value |
|-------|-------|
| PrimaryServer | ns10.wixdns.net |
| NameAdministrator | support.wix.com |
| SerialNumber | 2022111223 |
| TimeToZoneRefresh | 10800 |
| TimeToZoneFailureRetry | 3600 |
| TimeToExpiration | 1209600 |
| DefaultTTL | 3600 |

> SOA admin contact = `support.wix.com` — the zone is currently authoritatively served by Wix.

## Apex (klphotography.ie)

| Type | Value | TTL |
|------|-------|-----|
| A | 185.230.63.171 | 3600 |
| A | 185.230.63.186 | 3600 |
| A | 185.230.63.107 | 3600 |
| AAAA | (none) | — |

> The three A records resolve to Wix's hosting load balancer pool (`185.230.63.0/24` is Wix-owned). These are Wix-specific and will be DROPPED in Phase 6 — Cloudflare Pages serves the apex via CF's own routing.

## www

| Type | Value | TTL |
|------|-------|-----|
| CNAME | cdn1.wixdns.net (→ td-ccm-neg-87-45.wixdns.net → 34.149.87.45) | 3600 |

> Wix CDN chain. Will be REPLACED in Phase 6 with a CNAME to Cloudflare Pages.

## MX (email routing)

| Pref | Host | TTL | Backing A record |
|------|------|-----|------------------|
| — | (none — no MX records exist for klphotography.ie) | — | — |

> **No MX records.** Public email for the domain does not exist. The owner's email
> address is `klphotography.ie@gmail.com` — that is a Gmail mailbox under the
> `gmail.com` domain, NOT a custom-domain mailbox at `klphotography.ie`. The mailbox
> name happens to spell the domain. The Phase 6 nameserver swap therefore CANNOT
> break inbound email for the owner — there is no inbound email at this domain to
> break.

## TXT / SPF / DKIM / DMARC

| Name | Value | TTL | Purpose |
|------|-------|-----|---------|
| @ | (no TXT records returned) | — | — |
| _dmarc | (none) | — | — |
| google._domainkey | (none) | — | — |
| selector1._domainkey | (none) | — | — |
| default._domainkey | (none) | — | — |

> No SPF, no DKIM, no DMARC currently published. Phase 5 (Resend domain
> verification) will introduce these from a clean slate — no merging required.

## DNSSEC

Not probed (Wix-managed zone, generally not DNSSEC-signed). To verify before Phase 6:
- `Resolve-DnsName klphotography.ie -Server 1.1.1.1 -Type DNSKEY` — empty result = unsigned

## Other records

None returned beyond NS/A/CNAME/SOA above. No SRV, no CAA.

## Inferred current setup

- **Hosting & DNS:** Wix (zone authoritative on `ns10.wixdns.net` / `ns11.wixdns.net`, apex A records on Wix IPs, www CNAME into Wix CDN)
- **Email:** None on this domain. Owner uses `klphotography.ie@gmail.com` (Gmail) for contact.
- **CDN:** Wix's CDN (`cdn1.wixdns.net`)
- **DNSSEC:** Almost certainly not signed (Wix DNS does not enable DNSSEC by default)

## Records that MUST be preserved verbatim in Phase 6

**None.** This is the safety-net section that the cutover plan reads — for this domain
it is empty. There is no MX, no SPF, no DKIM, no DMARC, no third-party verification TXT,
no SRV, no CAA. The Phase 6 cutover can DROP every existing record without breaking any
live service.

## Records to DROP in Phase 6

- All three apex `A` records pointing at Wix (`185.230.63.107`, `185.230.63.171`, `185.230.63.186`)
- The `www` CNAME at `cdn1.wixdns.net`
- The current NS delegation (`ns10.wixdns.net`, `ns11.wixdns.net`) — replaced at the
  IEDR registrar level, not inside the Wix zone

## Records to CREATE in Phase 6 (informational, not part of this snapshot)

- Apex routing (A or CNAME-flattened) at `klphotography.pages.dev`
- `www` CNAME at the apex (or at `klphotography.pages.dev`)
- Resend SPF + DKIM TXT records (added in Phase 5, listed here for completeness)

## Phase 6 risk re-assessment

PITFALLS.md P3 ("DNS cutover breaks email") is effectively MITIGATED for this domain
because there is no email at the domain to break. P3 is downgraded from CRITICAL to
LOW for klphotography.ie specifically.
