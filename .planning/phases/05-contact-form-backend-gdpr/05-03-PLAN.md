---
phase: 05-contact-form-backend-gdpr
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/pages/privacy.astro
  - src/layouts/BaseLayout.astro
  - public/robots.txt
autonomous: false
requirements:
  - GDPR-01
  - GDPR-02
  - GDPR-03
  - GDPR-04
  - GDPR-05
  - DESIGN-06
user_setup:
  - service: cloudflare-web-analytics
    why: "Need the public analytics token to inject the beacon script — token is account-specific and shown in CF dashboard"
    env_vars:
      - name: PUBLIC_CF_ANALYTICS_TOKEN
        source: "Cloudflare Dashboard -> Web Analytics -> klphotography site -> Manage site -> copy token from data-cf-beacon='{\"token\":\"...\"}' snippet"
    dashboard_config:
      - task: "Add Web Analytics site for klphotography.pages.dev (and later klphotography.ie in Phase 6)"
        location: "Cloudflare Dashboard -> Web Analytics -> Add a site"
      - task: "Set PUBLIC_CF_ANALYTICS_TOKEN as plain text env var (Preview + Production, build-time)"
        location: "Cloudflare Dashboard -> Workers & Pages -> klphotography -> Settings -> Variables and Secrets"

must_haves:
  truths:
    - "/privacy renders the real GDPR Article 13-compliant policy (no '[OWNER-REVIEW] Phase 3 placeholder' text remains)"
    - "/privacy NO LONGER carries the noindex meta tag (was Phase 3 stub guard; real content is publicly indexable per GDPR Art 12 transparency)"
    - "public/robots.txt no longer contains Disallow: /privacy (was Phase 3 guard)"
    - "Every page rendered from BaseLayout.astro includes the CF Web Analytics beacon script tag with the configured token"
    - "Built dist/ contains zero matches for re_*, RESEND_API_KEY string, TURNSTILE_SECRET_KEY string, or test secret 1x00000000000000 pattern (per D-15)"
    - "Built dist/index.html contains exactly two third-party script sources: Turnstile + CF Analytics beacon (no GA, no Hotjar, no FB pixel, etc.)"
    - "Lighthouse Accessibility score on / and on /privacy is >=95 (DESIGN-06)"
    - "DevTools Cookies panel shows zero cookies on / and /privacy after full page load including Turnstile + beacon (GDPR-02)"
  artifacts:
    - path: "src/pages/privacy.astro"
      provides: "Real GDPR-compliant privacy policy per RESEARCH section 10 (D-11)"
      contains: "Data Protection Commission"
      min_lines: 80
    - path: "src/layouts/BaseLayout.astro"
      provides: "CF Web Analytics beacon injected into every page"
      contains: "static.cloudflareinsights.com/beacon.min.js"
    - path: "public/robots.txt"
      provides: "Allows /privacy crawling; preserves /styleguide disallow + Sitemap line byte-identical"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "https://static.cloudflareinsights.com/beacon.min.js"
      via: "is:inline script tag with data-cf-beacon attribute and PUBLIC_CF_ANALYTICS_TOKEN substitution"
      pattern: "cloudflareinsights\\.com/beacon\\.min\\.js"
    - from: "src/pages/privacy.astro"
      to: "Data Protection Commission Ireland"
      via: "explicit contact block citing dataprotection.ie"
      pattern: "dataprotection\\.ie|DPC"
    - from: "public/robots.txt"
      to: "Sitemap"
      via: "preserve Phase 2 byte-identical Sitemap line for Phase 6 SEO"
      pattern: "Sitemap:.*sitemap-index.xml"
---

<objective>
Replace the Phase 3 privacy stub with the real GDPR Article 13-compliant policy, inject the cookieless Cloudflare Web Analytics beacon into BaseLayout, audit the built dist/ for secret leaks per D-15, and re-run Lighthouse accessibility on / and /privacy to confirm DESIGN-06 (>=95) holds after Phase 5 changes.

Purpose: Closes the GDPR compliance loop for Phase 5. Without this plan the form ships to a stub privacy policy (insufficient under GDPR Art. 13), no analytics visibility, and unverified secret hygiene. After this plan the site is launchable on the preview URL with full GDPR posture and zero cookies. Depends on 05-01 because the secret-leak negative gate verifies 05-01's env-var-only contract held end-to-end through the build.

Output: real privacy.astro content, BaseLayout.astro beacon injection, robots.txt unblocked for /privacy, secret-leak audit pass, Lighthouse a11y >=95 on / and /privacy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-contact-form-backend-gdpr/05-RESEARCH.md
@.planning/phases/05-contact-form-backend-gdpr/05-01-SUMMARY.md
@src/pages/privacy.astro
@src/layouts/BaseLayout.astro
@public/robots.txt

<interfaces>
<!-- The existing privacy.astro stub (Phase 3) — preserve route, BaseLayout wrapper, Nav, Footer, Section structure. -->
<!-- Replace ONLY the Section body content + remove the noindex meta. -->

From src/pages/privacy.astro (line 14):
- noindex meta is currently: <meta slot="head" name="robots" content="noindex, nofollow" />
- This MUST be REMOVED in this plan (real policy is publicly indexable per GDPR Art 12 transparency).

From src/layouts/BaseLayout.astro:
- Head slot ordering preserved: Font preloads, then <slot name="head">, then title/description
- Analytics beacon goes just before </body> per RESEARCH section 9 line 581
- Use is:inline per Pitfall 3 (Vite would otherwise try to bundle the CDN URL)

CF Web Analytics beacon contract (RESEARCH section 9):
- Script src: https://static.cloudflareinsights.com/beacon.min.js
- Attributes: defer, data-cf-beacon='{"token":"<token>"}'
- Token is PUBLIC (it is account-specific but not a secret per CF docs)
- Token via env var: PUBLIC_CF_ANALYTICS_TOKEN

From public/robots.txt current:
```
User-agent: *
Allow: /
Disallow: /styleguide
Disallow: /privacy

Sitemap: https://klphotography.ie/sitemap-index.xml
```
Keep User-agent, Allow, Disallow: /styleguide, Sitemap LINE BYTE-IDENTICAL (Phase 2 lesson).
Remove ONLY: Disallow: /privacy

Privacy policy content sections (from RESEARCH section 10 + D-11) — ALL mandatory:
1. Data Controller (owner name + address — owner-confirm placeholder)
2. Data Collected via Contact Form (per-field table)
3. Purpose + Legal Basis (Art 6(1)(b) pre-contractual per D-11)
4. Data Processors (Cloudflare, Resend, Google/Gmail — all USA via SCCs)
5. Retention (12 months or until booking confirmed/declined per D-11)
6. Data Subject Rights (Art 15-21, 77)
7. DPC Ireland contact (21 Fitzwilliam Square South, Dublin 2, D02 RD28)
8. No automated decision-making (Art 13(2)(f))
9. International transfers (SCCs)
10. Changes to this policy (last-updated date)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write real /privacy content + remove noindex + unblock robots.txt</name>
  <files>src/pages/privacy.astro, public/robots.txt</files>
  <action>
    Replace the entire body of src/pages/privacy.astro with the real GDPR Article 13-compliant policy from RESEARCH section 10 (D-11). PRESERVE everything outside the Section body:
    - Keep the BaseLayout wrapper, title, description, Nav, Section id="privacy" tone="cream", Footer (with all its slot content for brand/links/social — copy verbatim from current file lines 38-58)
    - REMOVE the line `<meta slot="head" name="robots" content="noindex, nofollow" />` (current line 14) — real policy is publicly indexable per GDPR Art 12 (transparency); per D-11 the stub guard is no longer needed
    - REPLACE the Section body (current lines 19-34) with the full policy content below

    Policy content — render as plain Astro markup using existing typography (h1, h2, p, ul, table). Sections in order:

    **# Privacy policy** (h1 — already present, keep)
    Add immediately under h1: `<p class="text-sm text-ink-soft">Last updated: 2026-05-18</p>` (use today's date as ISO format)

    **## Data controller**
    `KL Photography`
    `Operated by [OWNER-CONFIRM:full-legal-name]`
    `[OWNER-CONFIRM:business-address], Ireland`
    `Email: klphotography.ie@gmail.com`
    (Keep the [OWNER-CONFIRM:...] markers — owner fills in for launch. The address must be provided before Phase 6 cutover.)

    **## What data we collect**
    Render as a `<table>` with thead "Field / Personal data? / Required" and tbody rows for: Name (Yes / Required), Email address (Yes / Required), Wedding date (No / Optional), Venue (No / Optional), Message (Potentially / Required).
    Add explanatory paragraph below: "We do not collect anything else. The contact form does not write cookies and we use no third-party tracking scripts beyond Cloudflare Turnstile (spam protection) and Cloudflare Web Analytics (cookieless page performance metrics — see Cookies and analytics below)."

    **## Why we process this data (purpose and legal basis)**
    `Purpose: To respond to your enquiry about wedding photography services.`
    `Legal basis: Pre-contractual measures (GDPR Article 6(1)(b)) — your enquiry is a step toward a possible photography contract.`
    (Use Art 6(1)(b) per D-11. RESEARCH section 10 mentioned Art 6(1)(f) as alternative; D-11 locks 6(1)(b) — pre-contract enquiry.)

    **## Data processors**
    `<table>` with columns Processor / Role / Location / Privacy policy, rows:
    - Cloudflare, Inc. / Website hosting, form infrastructure, spam protection (Turnstile), cookieless analytics / USA (SCCs apply) / `<a href="https://www.cloudflare.com/privacypolicy/">cloudflare.com/privacypolicy</a>`
    - Resend, Inc. / Email delivery of your enquiry to the photographer / USA (SCCs apply) / `<a href="https://resend.com/legal/privacy-policy">resend.com/legal/privacy-policy</a>`
    - Google LLC (Gmail) / Storage of the received email in the photographer's mailbox / USA (SCCs apply) / `<a href="https://policies.google.com/privacy">policies.google.com/privacy</a>`

    **## How long we keep your data (retention)**
    `We retain your enquiry data in our email inbox for up to 12 months, or until a booking is confirmed or declined — whichever comes first. After that, we delete it from active correspondence. You may request earlier deletion at any time by emailing klphotography.ie@gmail.com.`

    **## Your rights under GDPR**
    `Under the General Data Protection Regulation you have the right to:`
    `<ul>` listing each as a separate `<li>`:
    - Access the personal data we hold about you (Article 15)
    - Correct any inaccurate data (Article 16)
    - Request deletion ("right to be forgotten", Article 17)
    - Restrict how we process your data (Article 18)
    - Object to our processing (Article 21)
    - Data portability (Article 20)
    - Lodge a complaint with the supervisory authority (Article 77)
    `To exercise any of these rights, email <a href="mailto:klphotography.ie@gmail.com">klphotography.ie@gmail.com</a>.`

    **## Cookies and analytics**
    `This website sets zero cookies and uses no local storage. We use Cloudflare Web Analytics for cookieless page performance metrics — this beacon does not set cookies, does not use localStorage, does not log your IP address, and does not track you across sites. For details see <a href="https://developers.cloudflare.com/web-analytics/">developers.cloudflare.com/web-analytics</a>.`

    **## International data transfers**
    `Our processors (Cloudflare, Resend, Google) are based in the United States. These transfers rely on Standard Contractual Clauses under GDPR Article 46. Each processor publishes its data transfer documentation at the links in the Processors section above.`

    **## Automated decision-making**
    `We do not use automated profiling or automated decision-making (GDPR Article 13(2)(f)).`

    **## Supervisory authority**
    `If you have concerns about how we handle your personal data, you may contact the Irish Data Protection Commission:`
    `<address class="not-italic">`
    `Data Protection Commission (DPC)`
    `21 Fitzwilliam Square South`
    `Dublin 2, D02 RD28`
    `Ireland`
    `Phone: +353 (0)761 104 800`
    `Web: <a href="https://www.dataprotection.ie">www.dataprotection.ie</a>`
    `Complaint form: <a href="https://www.forms.dataprotection.ie/raise-a-concern">forms.dataprotection.ie/raise-a-concern</a>`
    `</address>`
    (Mark this whole block with a comment `<!-- [OWNER-CONFIRM] Verify DPC contact details at dataprotection.ie before launch per RESEARCH A4 -->` — owner does a final check before Phase 6.)

    **## Changes to this policy**
    `We may update this policy from time to time. The "Last updated" date at the top of this page reflects the most recent revision.`

    Modify public/robots.txt:
    - Remove the line `Disallow: /privacy` (Phase 3 stub guard no longer needed — real policy is indexable)
    - PRESERVE EVERY OTHER LINE BYTE-IDENTICAL: keep User-agent: *, Allow: /, Disallow: /styleguide, the blank line, and the Sitemap: https://klphotography.ie/sitemap-index.xml line (Phase 2 lesson — sitemap line is contractual for Phase 6 SEO).
  </action>
  <verify>
    <automated>cd /c/projects/new-project &amp;&amp; ! grep -q "noindex" src/pages/privacy.astro &amp;&amp; ! grep -q "OWNER-REVIEW.*Phase 3 placeholder" src/pages/privacy.astro &amp;&amp; grep -q "Data Protection Commission" src/pages/privacy.astro &amp;&amp; grep -q "dataprotection.ie" src/pages/privacy.astro &amp;&amp; grep -q "Article 6(1)(b)" src/pages/privacy.astro &amp;&amp; grep -q "Standard Contractual Clauses" src/pages/privacy.astro &amp;&amp; grep -q "12 months" src/pages/privacy.astro &amp;&amp; grep -q "Resend" src/pages/privacy.astro &amp;&amp; grep -q "Cloudflare" src/pages/privacy.astro &amp;&amp; grep -q "Gmail\|Google" src/pages/privacy.astro &amp;&amp; ! grep -q "^Disallow: /privacy$" public/robots.txt &amp;&amp; grep -q "^Disallow: /styleguide$" public/robots.txt &amp;&amp; grep -q "^Sitemap: https://klphotography.ie/sitemap-index.xml$" public/robots.txt &amp;&amp; npx astro check</automated>
  </verify>
  <done>
    - src/pages/privacy.astro: noindex meta removed, [OWNER-REVIEW] stub text removed
    - Privacy page renders all 10 required sections per RESEARCH section 10
    - Cites Art 6(1)(b), 13, 15-21, 77, DPC Ireland contact
    - public/robots.txt: Disallow: /privacy line removed
    - public/robots.txt: all other lines byte-identical
    - astro check passes (no broken markup)
    - [OWNER-CONFIRM] markers remain for owner name + address + DPC verification
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Inject CF Web Analytics beacon into BaseLayout</name>
  <files>src/layouts/BaseLayout.astro</files>
  <action>
    Modify src/layouts/BaseLayout.astro to inject the Cloudflare Web Analytics beacon on every page that uses BaseLayout (which is every page on the site).

    Per RESEARCH section 9 + Pitfall 3, the beacon goes just before `</body>` and MUST use `is:inline` so Astro/Vite does not try to bundle the CDN URL as a local module.

    Place the script tag immediately before the closing `</body>` tag (current line 32 in BaseLayout.astro). Do NOT touch:
    - The Font preload tags (lines 20-21) — critical resource priority must be preserved
    - The head slot ordering comment + slot (lines 22-23)
    - The body data-nav-sentinel div (line 30) — Phase 3 IntersectionObserver target
    - The main <slot /> (line 31)

    Use the dynamic-token form per RESEARCH section 9 line 583:

    ```astro
    <!-- CF Web Analytics beacon — cookieless, no localStorage; satisfies GDPR-02, GDPR-03. -->
    <script
      defer
      src="https://static.cloudflareinsights.com/beacon.min.js"
      data-cf-beacon={`{"token": "${import.meta.env.PUBLIC_CF_ANALYTICS_TOKEN}"}`}
      is:inline
    ></script>
    ```

    Per D-10: token via env var PUBLIC_CF_ANALYTICS_TOKEN (Astro PUBLIC_* convention — exposed to browser at build time). The token is account-specific and surfaced as public per CF docs (no security risk).

    Behavioral contract for the token expression:
    - At build time, import.meta.env.PUBLIC_CF_ANALYTICS_TOKEN must resolve to a real string (set in CF Pages env Preview + Production)
    - If unset, the data-cf-beacon attribute will literally read `{"token": "undefined"}` — CF beacon silently ignores invalid tokens (no JS error, no visible breakage)
    - Local dev (npm run dev) without the env var set: token resolves to undefined string; this is acceptable for local dev — owner can optionally add PUBLIC_CF_ANALYTICS_TOKEN to a local .env file (gitignored via existing .env pattern)

    Do NOT hardcode the token in BaseLayout.astro — per D-10 keep it env-var-driven so the same source produces correct beacons across Preview + Production (different sites in CF Web Analytics dashboard get different tokens).

    Add an inline comment ABOVE the script tag explaining placement order:
    `<!-- Analytics beacon placed AFTER content (defer) so it never blocks rendering. data-nav-sentinel above and main <slot/> above remain in their Phase 3 positions. -->`
  </action>
  <verify>
    <automated>cd /c/projects/new-project &amp;&amp; grep -q "static.cloudflareinsights.com/beacon.min.js" src/layouts/BaseLayout.astro &amp;&amp; grep -q "data-cf-beacon" src/layouts/BaseLayout.astro &amp;&amp; grep -q "is:inline" src/layouts/BaseLayout.astro &amp;&amp; grep -q "PUBLIC_CF_ANALYTICS_TOKEN" src/layouts/BaseLayout.astro &amp;&amp; grep -q "data-nav-sentinel" src/layouts/BaseLayout.astro &amp;&amp; npx astro check</automated>
  </verify>
  <done>
    - BaseLayout.astro contains the beacon script tag with is:inline, defer, dynamic token from env
    - Beacon placed just before </body>
    - Font preloads, head slot, data-nav-sentinel, main <slot /> all unchanged
    - astro check passes
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Build + secret-leak audit (negative gate per D-15) + third-party script audit</name>
  <files></files>
  <action>
    This task is pure verification — no source files modified. Run from project root:

    1. Clean build:
       rm -rf dist/ .astro/
       npm run build
       # MUST exit 0

    2. Per D-15: secret-leak negative gate. Each command MUST return empty (zero matches). If any returns non-empty, fail this task and investigate before deploy. Use `grep -v '.map'` to exclude sourcemaps which legitimately reference original source paths.

       grep -rE "re_[A-Za-z0-9_]{8,}" dist/ | grep -v ".map"
       grep -rE "RESEND_API_KEY" dist/ | grep -v ".map"
       grep -rE "TURNSTILE_SECRET_KEY" dist/ | grep -v ".map"
       grep -rE "1x00000000000000" dist/ | grep -v ".map"

       Each of the four MUST produce zero lines of output. If output exists -> STOP, do not proceed, investigate which file/import path is leaking the value.

    3. Per RESEARCH section 13: third-party script positive + negative audit on built dist/index.html.

       Positive (must find both):
       grep -oE 'src="[^"]*"' dist/index.html | sort -u
       # Output MUST include:
       # - https://challenges.cloudflare.com/turnstile/v0/api.js (from 05-01)
       # - https://static.cloudflareinsights.com/beacon.min.js (from this plan Task 2)
       # All other src= URLs MUST be same-origin (start with /) or local font paths

       Negative (must all be empty):
       grep -iE "google-analytics|googletagmanager|ga\.js|gtag|facebook\.net|fbq|hotjar|intercom|hubspot|zendesk|livechat" dist/index.html
       grep -iE "google-analytics|googletagmanager|ga\.js|gtag|facebook\.net|fbq|hotjar|intercom|hubspot|zendesk|livechat" dist/privacy/index.html

    4. Positive expectations (must NOT be empty — these are CORRECT exposures):
       grep -E "PUBLIC_TURNSTILE_SITE_KEY|cf-turnstile" dist/index.html
       # SHOULD find: data-sitekey=... attribute on the Turnstile widget div (PUBLIC_TURNSTILE_SITE_KEY is exposed by design per Astro PUBLIC_* convention)

       grep -E "data-cf-beacon" dist/index.html
       # SHOULD find: the analytics beacon script tag

    5. Privacy page sanity:
       test -f dist/privacy/index.html
       ! grep -q "noindex" dist/privacy/index.html
       grep -q "Data Protection Commission" dist/privacy/index.html
       grep -q "Cloudflare" dist/privacy/index.html

    6. robots.txt sanity:
       test -f dist/robots.txt
       ! grep -q "^Disallow: /privacy$" dist/robots.txt
       grep -q "^Disallow: /styleguide$" dist/robots.txt
       grep -q "^Sitemap:" dist/robots.txt
  </action>
  <verify>
    <automated>cd /c/projects/new-project &amp;&amp; rm -rf dist/ .astro/ &amp;&amp; npm run build &amp;&amp; test -f dist/privacy/index.html &amp;&amp; test -z "$(grep -rE 're_[A-Za-z0-9_]{8,}' dist/ 2&gt;/dev/null | grep -v '.map')" &amp;&amp; test -z "$(grep -rE 'TURNSTILE_SECRET_KEY' dist/ 2&gt;/dev/null | grep -v '.map')" &amp;&amp; test -z "$(grep -rE '1x00000000000000' dist/ 2&gt;/dev/null | grep -v '.map')" &amp;&amp; grep -q "challenges.cloudflare.com/turnstile/v0/api.js" dist/index.html &amp;&amp; grep -q "static.cloudflareinsights.com/beacon.min.js" dist/index.html &amp;&amp; ! grep -iE "google-analytics|googletagmanager|gtag|facebook.net|fbq|hotjar|intercom|hubspot" dist/index.html &amp;&amp; ! grep -iE "google-analytics|googletagmanager|gtag|facebook.net|fbq|hotjar|intercom|hubspot" dist/privacy/index.html &amp;&amp; ! grep -q "noindex" dist/privacy/index.html &amp;&amp; ! grep -q "^Disallow: /privacy$" dist/robots.txt &amp;&amp; grep -q "^Sitemap:" dist/robots.txt</automated>
  </verify>
  <done>
    - npm run build exits 0
    - All four secret-leak greps return empty
    - dist/index.html has exactly Turnstile + CF Analytics script tags (no other third-party)
    - dist/privacy/index.html exists, has real policy content, no noindex
    - dist/robots.txt no longer blocks /privacy, still blocks /styleguide, still has Sitemap line
    - No GA / FB Pixel / Hotjar / Intercom / HubSpot / live-chat scripts anywhere
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Lighthouse a11y re-scan on / and /privacy + cookie inspection on preview URL</name>
  <what-built>Real /privacy policy live + CF Web Analytics beacon on every page + secret-leak audit passed + no third-party scripts beyond Turnstile/Analytics. Need to verify DESIGN-06 (a11y >=95) survived Phase 5 changes and GDPR-02 (zero cookies) on the live preview URL.</what-built>
  <how-to-verify>
    Prerequisite: 05-01 must be deployed to CF Pages preview (push to main or open a PR to trigger preview deploy). Note the preview URL (e.g. https://klphotography.pages.dev or a per-PR URL like https://abc123.klphotography.pages.dev).

    1. Lighthouse Accessibility on home page:
       npx lighthouse https://klphotography.pages.dev/ --only-categories=accessibility --output=json --quiet | jq '.categories.accessibility.score * 100'
       # MUST output a number >= 95
       # If under 95, re-run with --view to see specific issues (most likely candidates: Turnstile iframe label, beacon script async/defer attribute confusion)

    2. Lighthouse Accessibility on /privacy:
       npx lighthouse https://klphotography.pages.dev/privacy/ --only-categories=accessibility --output=json --quiet | jq '.categories.accessibility.score * 100'
       # MUST output a number >= 95
       # If under 95, most likely cause: heading hierarchy skip (h1 -> h3 instead of h1 -> h2). Fix in privacy.astro then redeploy.

    3. Zero-cookie verification (GDPR-02) — manual DevTools check:
       a. Open https://klphotography.pages.dev/ in Chrome (fresh incognito window — no extensions)
       b. Open DevTools (F12) -> Application tab -> Cookies -> klphotography.pages.dev
       c. Wait for full page load (Turnstile widget rendered + analytics beacon fired — check Network tab for beacon.min.js completion)
       d. Cookies for klphotography.pages.dev: must show ZERO entries
       e. Switch to https://klphotography.pages.dev/privacy/ — recheck cookies: still ZERO entries
       f. In DevTools Console run: `console.log('Cookies:', document.cookie)` — must print `Cookies: ` (empty string)

    4. Visual smoke of /privacy:
       a. Load https://klphotography.pages.dev/privacy/ in browser
       b. Page should render the full policy (not the Phase 3 placeholder)
       c. Footer Privacy link works (already wired in Phase 3 — verify)
       d. Contact form's privacy link (in Contact.astro adjacency paragraph) navigates to /privacy correctly

    5. Beacon firing check:
       a. DevTools Network tab on home page load
       b. Filter: cloudflareinsights
       c. Expected: one GET request to https://static.cloudflareinsights.com/beacon.min.js (200) and one or more POST requests to https://cloudflareinsights.com/cdn-cgi/rum (200 or 204)
       d. If beacon.min.js returns 200 but no POST to /cdn-cgi/rum follows -> PUBLIC_CF_ANALYTICS_TOKEN is wrong or unset in CF Pages env. Fix in dashboard + redeploy.

    Report all four scores + cookie check + beacon firing status.
  </how-to-verify>
  <resume-signal>Paste Lighthouse a11y scores for / and /privacy, cookie count (must be 0), and beacon firing status. Type "approved" if all pass. If any fail, describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> CF Web Analytics beacon | Beacon posts performance metrics to CF; token is public; no cookies set; no PII |
| repo -> dist/ build output | Secrets must not cross; PUBLIC_* env vars (PUBLIC_TURNSTILE_SITE_KEY, PUBLIC_CF_ANALYTICS_TOKEN) intentionally exposed |
| public/robots.txt -> search engines | /privacy now indexable; /styleguide still blocked |
| /privacy -> visitor | Document is GDPR Art 13 disclosure; legally significant — owner must review before launch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Information Disclosure | Resend API key leaks into dist/ via accidental import | mitigate | Task 3 negative gate `grep -rE "re_[A-Za-z0-9_]{8,}" dist/` MUST return empty per D-15 |
| T-05-03-02 | Information Disclosure | Turnstile secret key leaks into dist/ via accidental import | mitigate | Task 3 negative gate `grep -rE "TURNSTILE_SECRET_KEY" dist/` and `1x00000000000000` test pattern MUST return empty |
| T-05-03-03 | Information Disclosure | Test secret value committed to .dev.vars.example (the always-success key) gets baked into dist/ | accept | The test secret is published by Cloudflare for public use (RESEARCH section 7); inclusion in dist/ would be incorrect but not harmful — still excluded by Task 3 gate as defense-in-depth |
| T-05-03-04 | Information Disclosure | Third-party tracker script silently added (GA, FB pixel, etc.) | mitigate | Task 3 negative grep enumerates common trackers; CI gate fails build if any appear |
| T-05-03-05 | Spoofing | Privacy policy missing required GDPR Art 13 fields exposes owner to DPC complaint | mitigate | Task 1 enforces all 10 sections via grep gates (Data Protection Commission, Article 6(1)(b), 12 months, SCCs, etc.) |
| T-05-03-06 | Tampering | robots.txt drift breaks Phase 6 SEO (Sitemap line removed or modified) | mitigate | Task 1 byte-identical preservation requirement + explicit grep verification of Sitemap line + Disallow: /styleguide line |
| T-05-03-07 | Repudiation | Owner publishes policy without legal name/address (left as OWNER-CONFIRM) | accept | OWNER-CONFIRM markers force human review before Phase 6 cutover; markers grep-detectable in pre-launch checklist |
| T-05-03-08 | Information Disclosure | CF Web Analytics beacon discloses visitor URLs/referrers to Cloudflare | accept | Documented in /privacy policy Cookies & Analytics section; cookieless behavior verified in Task 4 checkpoint |
</threat_model>

<plan_level_verification>
End-of-plan verification combines automated and human checks:

1. Local build artifacts pass all gates from Task 3:
   cd /c/projects/new-project &amp;&amp; rm -rf dist/ .astro/ &amp;&amp; npm run build
   # All Task 3 grep gates return expected results

2. Privacy policy contains all 10 required sections (grep enumeration):
   for section in "Data controller" "What data we collect" "Why we process" "Data processors" "How long we keep" "Your rights under GDPR" "Cookies and analytics" "International data transfers" "Automated decision-making" "Supervisory authority"; do
     grep -q "$section" src/pages/privacy.astro || echo "MISSING: $section"
   done
   # MUST print nothing (all sections present)

3. robots.txt byte-stability check (Sitemap line MUST be identical to Phase 2):
   grep -q "^Sitemap: https://klphotography.ie/sitemap-index.xml$" public/robots.txt
   # MUST exit 0

4. astro check across all modified files:
   npx astro check
   # MUST exit 0

5. Task 4 human checkpoint passes:
   - Lighthouse a11y >= 95 on /
   - Lighthouse a11y >= 95 on /privacy
   - DevTools cookies count == 0 on both / and /privacy
   - Beacon network requests confirmed firing

6. Cross-plan negative gate (verifies 05-01's env-var-only contract):
   ! grep -rE "re_[A-Za-z0-9_]{8,}" dist/ | grep -v ".map"
   ! grep -rE "TURNSTILE_SECRET_KEY" dist/ | grep -v ".map"
   # Both MUST be empty — defines 05-01 + 05-03 success at the dist/ boundary
</plan_level_verification>

<risks>
- **R1: Lighthouse a11y drops below 95 due to Turnstile iframe** — Mitigation: Turnstile managed mode is WCAG compliant per RESEARCH section 15; if score drops, the issue is most likely the iframe's missing accessible name (Lighthouse aria-allowed-attr rule). Fix: add aria-label="Spam protection challenge" to the wrapper div around the Turnstile div in Contact.astro. Track in SUMMARY if needed.
- **R2: PUBLIC_CF_ANALYTICS_TOKEN unset at CF Pages build time** — beacon renders as `{"token":"undefined"}` which CF silently ignores. Symptom: no entries in CF Web Analytics dashboard. Fix: set env var in dashboard + redeploy. Task 4 checkpoint catches this via beacon firing check.
- **R3: Privacy policy DPC contact details outdated** — Mitigation: [OWNER-CONFIRM] marker on the supervisory authority block + RESEARCH A4 calls for verification at dataprotection.ie before launch. Pre-launch checklist (Phase 6) must check.
- **R4: Removing noindex from /privacy before real content is reviewed could expose [OWNER-CONFIRM] placeholders to search engines** — Mitigation: the [OWNER-CONFIRM] placeholders are for legal name + address + DPC details which, even if indexed, are not legally damaging (they are just incomplete). Owner reviews real policy before Phase 6 production deploy; preview URL is not indexed by default (CF Pages preview deployments have x-robots-tag: noindex by CF policy).
- **R5: Cookie banner debate — should we add one even though we set zero cookies?** — Resolved: per D-10 + RESEARCH section 9, CF Web Analytics is confirmed cookieless; per GDPR ePrivacy Directive, a cookie banner is only required when cookies are actually set. Zero cookies = no banner needed. Privacy policy still describes the analytics beacon for transparency.
</risks>

<success_criteria>
Maps to ROADMAP Phase 5 Success Criteria + REQUIREMENTS:

- SC-3 (privacy describes actual data flow + linked from footer + adjacent to submit): satisfied by Task 1 (real content) + existing Phase 3 footer link + existing Phase 3 contact form privacy adjacency
- SC-4 (CF Web Analytics fires + zero cookies + no third-party scripts other than Turnstile + analytics): satisfied by Task 2 (beacon) + Task 3 (third-party grep audit) + Task 4 (cookie inspection)
- SC-5 (no secrets in dist/): satisfied by Task 3 secret-leak negative gate per D-15
- GDPR-01: real /privacy policy per RESEARCH section 10 (Task 1)
- GDPR-02: zero cookies verified in Task 4 checkpoint
- GDPR-03: CF Web Analytics beacon installed (Task 2), no banner needed
- GDPR-04: no third-party scripts beyond Turnstile + Analytics (Task 3 negative grep)
- GDPR-05: privacy link adjacent to submit button — already satisfied by Phase 3 markup per RESEARCH section 14; this plan does not regress it
- DESIGN-06: Lighthouse a11y >=95 on / and /privacy verified in Task 4 checkpoint
</success_criteria>

<output>
Create `.planning/phases/05-contact-form-backend-gdpr/05-03-SUMMARY.md` per `@$HOME/.claude/get-shit-done/templates/summary.md`. Summary MUST include:

- **what-built:** real /privacy policy (all 10 GDPR Art 13 sections), CF Web Analytics beacon in BaseLayout, robots.txt unblocked for /privacy, secret-leak audit gate, Lighthouse a11y re-confirmed
- **how-verified:** astro check + npm run build clean + secret-leak negative gate (4 patterns return empty) + third-party script positive/negative grep (Turnstile + Analytics only) + Lighthouse a11y >=95 on / and /privacy + DevTools zero-cookie inspection on preview URL
- **deviations:** any privacy section content that differs from RESEARCH section 10 wording (note rationale); [OWNER-CONFIRM] markers that remain
- **owner-action-required:**
  1. Set PUBLIC_CF_ANALYTICS_TOKEN in CF Pages env (Preview + Production) per CF Web Analytics dashboard
  2. Replace [OWNER-CONFIRM:full-legal-name] in /privacy with the photographer's legal trading name
  3. Replace [OWNER-CONFIRM:business-address] in /privacy with the registered business address
  4. Verify DPC contact details at dataprotection.ie before Phase 6 production deploy (per RESEARCH A4)
- **next-phase-readiness:** Phase 5 complete; Phase 6 picks up DNS-03 (CF DNS zone creation) + nameserver swap + Resend domain verify (uses 05-02 SETUP-RESEND-DOMAIN.md runbook) + CONTACT_FROM_EMAIL production switch to enquiries@klphotography.ie + sitemap.xml + final SEO meta + JSON-LD + LCP/CLS/Performance audits + Wix archive + Wix cancel
- **carry-forward:** [OWNER-CONFIRM] markers in /privacy (4 items: legal name, business address, DPC contact verification, DPC complaint form URL) — must be resolved before Phase 6 production deploy
</output>

<done>
- [ ] src/pages/privacy.astro contains real GDPR Art 13 policy, all 10 sections
- [ ] noindex meta removed from /privacy
- [ ] [OWNER-REVIEW] Phase 3 placeholder text removed
- [ ] [OWNER-CONFIRM] markers remain for legal name + address + DPC verification
- [ ] public/robots.txt no longer blocks /privacy
- [ ] public/robots.txt Sitemap line + /styleguide block byte-identical
- [ ] BaseLayout.astro injects CF Analytics beacon (is:inline, defer, env-var token)
- [ ] Beacon placed just before </body>, after main slot
- [ ] npm run build exits 0
- [ ] All 4 secret-leak greps return empty on dist/
- [ ] Only Turnstile + CF Analytics third-party scripts in dist/index.html
- [ ] No GA / FB / Hotjar / Intercom / HubSpot anywhere
- [ ] dist/privacy/index.html contains real policy, no noindex
- [ ] Owner checkpoint: Lighthouse a11y >=95 on / and /privacy
- [ ] Owner checkpoint: zero cookies on / and /privacy in DevTools
- [ ] Owner checkpoint: beacon firing confirmed in Network tab
- [ ] SUMMARY.md created with all required sections
</done>
</content>
</invoke>