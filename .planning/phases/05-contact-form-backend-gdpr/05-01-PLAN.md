---
phase: 05-contact-form-backend-gdpr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .dev.vars.example
  - package.json
  - functions/api/contact.ts
  - functions/tsconfig.json
  - src/components/sections/Contact.astro
  - src/pages/index.astro
autonomous: false
requirements:
  - FORM-01
  - FORM-02
  - FORM-03
  - FORM-04
  - FORM-05
  - FORM-06
  - FORM-07
  - FORM-09
  - FORM-10
  - FORM-11
user_setup:
  - service: resend
    why: "Email delivery via direct fetch to Resend REST API"
    env_vars:
      - name: RESEND_API_KEY
        source: "resend.com -> API Keys -> Create API Key (full access; tag 'klphotography-prod' and 'klphotography-preview' separately for rotation)"
    dashboard_config:
      - task: "Sign up at resend.com (free tier: 3000/mo, 100/day)"
        location: "resend.com/signup"
      - task: "Verify ownership email (any inbox — domain verify deferred to Phase 6 per D-06)"
        location: "Resend dashboard"
  - service: cloudflare-turnstile
    why: "CAPTCHA/spam protection per FORM-03"
    env_vars:
      - name: TURNSTILE_SECRET_KEY
        source: "Cloudflare Dashboard -> Turnstile -> Add site -> klphotography.pages.dev -> copy Secret Key"
      - name: PUBLIC_TURNSTILE_SITE_KEY
        source: "Cloudflare Dashboard -> Turnstile -> same site -> copy Site Key (public, build-time)"
    dashboard_config:
      - task: "Add Turnstile site for klphotography.pages.dev (managed widget type per RESEARCH section 2 — cookieless)"
        location: "Cloudflare Dashboard -> Turnstile -> Add site"
  - service: cloudflare-pages-env-vars
    why: "Encrypted secrets + plain text contact addresses must live in CF Pages env not in repo (FORM-06)"
    env_vars:
      - name: RESEND_API_KEY
        source: "Set as ENCRYPTED secret in CF Pages -> Settings -> Variables and Secrets (Preview + Production)"
      - name: TURNSTILE_SECRET_KEY
        source: "Set as ENCRYPTED secret in CF Pages (Preview + Production)"
      - name: CONTACT_TO_EMAIL
        source: "Plain text in CF Pages env (Preview + Production): klphotography.ie@gmail.com"
      - name: CONTACT_FROM_EMAIL
        source: "Plain text in CF Pages env Preview: 'KL Photography <onboarding@resend.dev>' (per D-06; Production same until Phase 6 domain verify then switch to enquiries@klphotography.ie)"
      - name: DEV_SKIP_TURNSTILE
        source: "Plain text in CF Pages env Preview ONLY: 'true' (per D-09; production has NO such var so token check is enforced)"
      - name: PUBLIC_TURNSTILE_SITE_KEY
        source: "Plain text in CF Pages env Production + Preview (build-time per RESEARCH Pitfall 4)"

must_haves:
  truths:
    - "POST https://<preview>.pages.dev/api/contact with valid form fields + valid Turnstile token returns 200 JSON {ok:true} and the owner receives an email at CONTACT_TO_EMAIL within seconds"
    - "POST /api/contact with invalid or missing Turnstile token (production env: no DEV_SKIP_TURNSTILE) returns 403 JSON"
    - "POST /api/contact with the honeypot field contact_company filled returns 200 silently (no email sent)"
    - "POST /api/contact with missing required fields (name/email/message) returns 400 JSON"
    - "Browser form on / clears to a success state on 200 and shows error notice on non-200 (FORM-09)"
    - "Email received by CONTACT_TO_EMAIL has reply_to set to the couple's email (FORM-04)"
    - "Email subject is 'Wedding enquiry from {name}' or '... — {wedding_date}' when date provided (FORM-05 per D-13)"
    - "Form supports both JSON (JS path) and application/x-www-form-urlencoded (no-JS path) per D-09 + FORM-10"
    - "Local dev: wrangler pages dev dist/ + .dev.vars file exercises the full flow end-to-end with Turnstile test secret 1x0000000000000000000000000000000AA (per D-08)"
    - ".dev.vars is explicitly gitignored (per D-07) — committing it would leak real secrets"
  artifacts:
    - path: "functions/api/contact.ts"
      provides: "POST /api/contact handler — Turnstile verify + Resend send"
      exports:
        - "onRequestPost"
      contains: "challenges.cloudflare.com/turnstile/v0/siteverify"
    - path: "functions/tsconfig.json"
      provides: "Pages Functions TypeScript config per RESEARCH section 1"
    - path: ".dev.vars.example"
      provides: "Template for local secret file (real values in untracked .dev.vars)"
      contains: "TURNSTILE_SECRET_KEY=1x0000000000000000000000000000000AA"
    - path: ".gitignore"
      provides: "Explicit .dev.vars exclusion per D-07"
      contains: ".dev.vars"
    - path: "package.json"
      provides: "wrangler@^4.92.0 in devDependencies per D-03"
      contains: "wrangler"
    - path: "src/components/sections/Contact.astro"
      provides: "Real Turnstile widget + working submit handler + success/error UI"
      contains: "cf-turnstile"
    - path: "src/pages/index.astro"
      provides: "Turnstile JS CDN script in head slot per RESEARCH section 5"
      contains: "challenges.cloudflare.com/turnstile/v0/api.js"
  key_links:
    - from: "src/components/sections/Contact.astro"
      to: "/api/contact"
      via: "fetch POST in form submit handler"
      pattern: "fetch\\(['\"]/api/contact"
    - from: "functions/api/contact.ts"
      to: "https://challenges.cloudflare.com/turnstile/v0/siteverify"
      via: "POST fetch with secret + response + remoteip"
      pattern: "challenges\\.cloudflare\\.com/turnstile/v0/siteverify"
    - from: "functions/api/contact.ts"
      to: "https://api.resend.com/emails"
      via: "POST fetch with Authorization: Bearer + reply_to (snake_case per D-01)"
      pattern: "api\\.resend\\.com/emails"
    - from: "src/components/sections/Contact.astro"
      to: "PUBLIC_TURNSTILE_SITE_KEY"
      via: "import.meta.env build-time substitution on data-sitekey attribute"
      pattern: "PUBLIC_TURNSTILE_SITE_KEY|data-sitekey"
---

<objective>
Build the Cloudflare Pages Function at functions/api/contact.ts that receives the contact form POST, verifies the Turnstile token, and dispatches the enquiry to the owner's Gmail via the Resend REST API — with zero new runtime npm packages. Wire the existing Phase 3 form UI (real Turnstile widget, real fetch submit, real success/error states). Install wrangler as a devDependency only, for local Pages Function dev + TypeScript types. Add explicit .dev.vars gitignore per D-07.

Purpose: This plan turns the static form UI into a working enquiry funnel. Until this ships, the form is dead UI. After this ships, every required FORM-* requirement except FORM-08 (domain verify, Phase 6) is satisfied on the preview URL.

Output: working POST /api/contact on the CF Pages preview URL, .dev.vars-driven local dev via wrangler pages dev, and an end-to-end test enquiry delivered to klphotography.ie@gmail.com.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-contact-form-backend-gdpr/05-RESEARCH.md
@src/components/sections/Contact.astro
@src/layouts/BaseLayout.astro
@src/pages/index.astro
@astro.config.mjs
@package.json
@.gitignore

<interfaces>
<!-- Existing form markup contract — preserve all field names + ids + classes from Phase 3 Contact.astro -->

Existing form fields (from src/components/sections/Contact.astro lines 14-46):
- input name="contact_company" (honeypot, hidden, must remain hidden + tabindex=-1)
- input name="name" id="cf-name" type="text" required minlength=2 maxlength=100
- input name="email" id="cf-email" type="email" required
- input name="wedding_date" id="cf-date" type="date" (optional)
- input name="venue" id="cf-venue" type="text" maxlength=200 (optional)
- textarea name="message" id="cf-message" required minlength=20 maxlength=2000

Existing notice element:
- p id="cf-notice" role="status" aria-live="polite" — used by submit handler for success/error text

Existing privacy adjacency paragraph (lines 51):
- Already includes <a href="/privacy"> immediately above the submit button — satisfies GDPR-05 per RESEARCH section 14. DO NOT regress this paragraph (no rewording needed).

Existing submit button:
- <Button type="submit" variant="primary" size="lg" loading={false}>Send enquiry</Button>
- Button primitive supports loading state — wire it in the submit handler

Existing script (lines 58-72): the Phase 3 stub submit handler — REPLACE it entirely with real fetch logic per RESEARCH section 5.

PagesFunction handler shape (RESEARCH section 1):
```typescript
interface Env {
  RESEND_API_KEY: string;
  TURNSTILE_SECRET_KEY: string;
  CONTACT_TO_EMAIL: string;
  CONTACT_FROM_EMAIL: string;
  DEV_SKIP_TURNSTILE?: string;
}
export const onRequestPost: PagesFunction<Env> = async ({ request, env }) => { ... }
```

Full reference implementation provided in RESEARCH section "Architecture Patterns -> Contact Function -- full skeleton" (lines 1007-1158) — use it as the canonical template. Key contractual points repeated here:
- Honeypot check FIRST (silent 200 if contact_company filled)
- Required field check: name, email, message
- Length validation per MAX_FIELD_LENGTHS
- Email format sanity (includes @ + .)
- Turnstile verify (skip ONLY if env.DEV_SKIP_TURNSTILE === 'true')
- Resend send via direct fetch (no SDK per D-01) with reply_to (snake_case per D-01 + Pitfall 2)
- HTML-escape user input before email html body (RESEARCH section 4 security note)
- 502 on Resend non-2xx; 500 on uncaught
- Accept both multipart/form-data and application/x-www-form-urlencoded per D-09 (JS sends multipart via FormData; no-JS path sends url-encoded)
- For Accept: text/html (no-JS path), respond with minimal HTML success page; for Accept: application/json (JS path), respond with JSON
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Gitignore .dev.vars + install wrangler devDep + scaffold functions/ tsconfig + .dev.vars.example template</name>
  <files>.gitignore, package.json, functions/tsconfig.json, .dev.vars.example</files>
  <action>
    Foundation step — no Function code yet. Sets up the contract so Task 2 can write functions/api/contact.ts with TypeScript types and local dev secrets.

    1. Modify .gitignore: append a new section at the end. Add a blank line above and the comment + entry. The existing pattern `.env.*` does NOT cover `.dev.vars` (no dot before "dev"). Per D-07 add it explicitly.

       New lines to append:
       ```
       # Cloudflare Pages Functions — local dev secrets (NEVER commit)
       .dev.vars
       ```

    2. Install wrangler as devDependency per D-03:
       cd /c/projects/new-project &amp;&amp; npm install --save-dev wrangler@^4.92.0
       This is the ONLY package install in Phase 5. Verify postinstall script absent per RESEARCH Package Legitimacy Audit. After install, package.json devDependencies must include "wrangler": "^4.92.0".

    3. Add a convenience script to package.json scripts block per RESEARCH section 17:
       "dev:full": "npm run build &amp;&amp; wrangler pages dev dist/"
       (Use literal &amp;&amp; in the JSON value — npm runs through a shell.)

    4. Create functions/tsconfig.json per RESEARCH section 1 lines 81-92:
       ```json
       {
         "compilerOptions": {
           "target": "ESNext",
           "module": "ESNext",
           "lib": ["ESNext"],
           "types": ["./types.d.ts"]
         },
         "include": ["./**/*.ts"],
         "exclude": ["../node_modules"]
       }
       ```

    5. Generate functions/types.d.ts via wrangler (per RESEARCH section 1 line 76):
       cd /c/projects/new-project &amp;&amp; npx wrangler types --path='./functions/types.d.ts'
       This emits PagesFunction<Env> + related types. The generated file will be committed for IDE convenience (it is platform types only, not secrets).

    6. Create .dev.vars.example as a template (committed; safe — uses the public Turnstile test secret 1x0000000000000000000000000000000AA which Cloudflare publishes for public use per RESEARCH section 7). The real .dev.vars is gitignored per step 1.

       Content (no surrounding quotes per dotenv convention; wrangler accepts both, but plain values match RESEARCH section 17 line 902-906):
       ```
       # .dev.vars.example — copy to .dev.vars (gitignored) and fill in real values for local dev
       # Real RESEND_API_KEY: create at resend.com -> API Keys
       RESEND_API_KEY=re_REPLACE_ME_WITH_REAL_KEY

       # Turnstile test secret (public, always returns success — Cloudflare-published per RESEARCH section 7)
       TURNSTILE_SECRET_KEY=1x0000000000000000000000000000000AA

       # Where the enquiry email is delivered (local dev: use your own test inbox)
       CONTACT_TO_EMAIL=your-test-inbox@gmail.com

       # Sender — use Resend's onboarding domain until Phase 6 domain verify (per D-06)
       CONTACT_FROM_EMAIL=KL Photography <onboarding@resend.dev>

       # Skip Turnstile verification for local dev (NEVER set in production)
       DEV_SKIP_TURNSTILE=true

       # Cloudflare Web Analytics token (PUBLIC = build-time injected into BaseLayout beacon)
       # Leave BLANK locally — CF Pages env supplies in Preview/Production. Local builds will stamp "undefined" into the data-cf-beacon attribute (harmless, no beacon fires).
       PUBLIC_CF_ANALYTICS_TOKEN=
       ```

       NOTE on PUBLIC_* env vars: Astro reads `PUBLIC_*` vars from `.env` (or shell env) at build time, NOT from `.dev.vars` at runtime. `.dev.vars` is read by `wrangler pages dev` and exposed only to Pages Functions via `context.env` (RESEARCH section 17 + Pitfall 4 + Pitfall 8). So the `PUBLIC_CF_ANALYTICS_TOKEN` line above is a documentation reminder only — it has no effect at runtime via `.dev.vars`. If you want to test the analytics beacon locally with a real token, add `PUBLIC_CF_ANALYTICS_TOKEN=<token>` to a local `.env` file (already gitignored via the existing `.env.*` pattern in `.gitignore`).

    7. README-style note: do NOT edit the README.md in this task; Task 4 documentation checkpoint covers local dev instructions.

    Sanity check: confirm .dev.vars itself is NOT created (no real secrets in this repo). Only .dev.vars.example is committed.
  </action>
  <verify>
    <automated>cd /c/projects/new-project && grep -q "^\.dev\.vars$" .gitignore && grep -q '"wrangler"' package.json && grep -q '"dev:full"' package.json && test -f functions/tsconfig.json && test -f functions/types.d.ts && test -f .dev.vars.example && ! test -f .dev.vars && grep -q "1x0000000000000000000000000000000AA" .dev.vars.example && grep -q "onboarding@resend.dev" .dev.vars.example && grep -q "PagesFunction" functions/types.d.ts</automated>
  </verify>
  <done>
    - .gitignore contains the explicit `.dev.vars` line
    - wrangler@^4.92.0 in devDependencies
    - "dev:full" script in package.json
    - functions/tsconfig.json present
    - functions/types.d.ts generated (contains PagesFunction)
    - .dev.vars.example present, .dev.vars absent (still local)
    - No new runtime dependencies added (only devDep wrangler)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement functions/api/contact.ts (Turnstile verify + Resend send via direct fetch)</name>
  <files>functions/api/contact.ts</files>
  <behavior>
    Endpoint contract (canonical — execute-time tests assert each):
    - POST /api/contact with content-type multipart/form-data + all required fields + valid Turnstile token -> 200 JSON {ok:true} + Resend POST observed
    - POST /api/contact with content-type application/x-www-form-urlencoded + all required fields + valid token -> 200 JSON or 200 HTML based on Accept header (per D-09 + RESEARCH section 6)
    - POST /api/contact with honeypot field contact_company filled (any non-empty value) -> 200 silent (no Turnstile call, no Resend call)
    - POST /api/contact missing required field name -> 400 JSON {ok:false, error:"name is required"}
    - POST /api/contact with name length > 100 -> 400 JSON length error
    - POST /api/contact with email missing @ -> 400 JSON invalid email
    - POST /api/contact with no Turnstile token and DEV_SKIP_TURNSTILE unset -> 403 JSON {ok:false, error:"Turnstile token required"}
    - POST /api/contact with bad Turnstile token (siteverify returns success:false) -> 403 JSON
    - POST /api/contact when env.DEV_SKIP_TURNSTILE === 'true' -> skips siteverify entirely, proceeds to Resend
    - When Resend returns non-2xx -> 502 JSON {ok:false, error:"Failed to send email"} + console.error with status+body
    - When uncaught exception thrown -> 500 JSON {ok:false, error:"Internal server error"} + console.error
    - GET /api/contact (or any non-POST verb) -> 405 (Pages Functions auto-handle this because handler only exports onRequestPost per RESEARCH section 1)
    - Email body (HTML + text): contains escaped name + email + wedding_date (if provided) + venue (if provided) + message
    - Email subject: "Wedding enquiry from ${name}" or "Wedding enquiry from ${name} — ${wedding_date}" if wedding_date provided (per D-13)
    - Email reply_to (snake_case per D-01 + Pitfall 2): equals the form's email field value
    - Email from: env.CONTACT_FROM_EMAIL
    - Email to: [env.CONTACT_TO_EMAIL] (array, even for single recipient per Resend API)
    - HTML escape applied to all user-supplied strings before HTML insertion (defense against XSS in owner's Gmail viewer per RESEARCH section 4 security note)
  </behavior>
  <action>
    Create functions/api/contact.ts implementing the full handler from RESEARCH section "Architecture Patterns -> Contact Function -- full skeleton" (lines 1007-1158). Use that skeleton verbatim as the canonical implementation. Key adherence points:

    - PagesFunction<Env> type from generated functions/types.d.ts (Task 1)
    - Env interface includes: RESEND_API_KEY, TURNSTILE_SECRET_KEY, CONTACT_TO_EMAIL, CONTACT_FROM_EMAIL, DEV_SKIP_TURNSTILE?
    - REQUIRED_FIELDS = ['name', 'email', 'message'] (note: wedding_date and venue are optional per FORM-02)
    - MAX_FIELD_LENGTHS: name=100, email=254, venue=200, message=2000, wedding_date=10
    - Honeypot field: 'contact_company' (matches existing Phase 3 form input name on Contact.astro line 16) — if filled, return 200 silently without sending email or calling Turnstile (fool the bot)
    - Parse both multipart/form-data AND application/x-www-form-urlencoded into the same fields object (RESEARCH section 5 lines 401-413) — both use formData() per the Web API
    - Per D-09 + RESEARCH section 6: branch the success response on Accept header — JSON path returns JSON, no-JS path returns minimal HTML success page
    - Turnstile siteverify: POST https://challenges.cloudflare.com/turnstile/v0/siteverify with URLSearchParams { secret, response: token, remoteip } — remoteip from request.headers.get('CF-Connecting-IP') ?? ''
    - Skip Turnstile ONLY if env.DEV_SKIP_TURNSTILE === 'true' (string compare; the env var is plain text per RESEARCH section 7)
    - Resend send: POST https://api.resend.com/emails with Authorization: Bearer ${env.RESEND_API_KEY}; body uses snake_case `reply_to` (NOT replyTo per D-01 + Pitfall 2)
    - HTML body builder uses helper esc() that escapes &lt;, &gt;, &amp;, &quot;, &#x27; — see RESEARCH lines 1153-1157
    - Subject builder: const datePart = wedding_date ? ` — ${wedding_date}` : ''; const subject = `Wedding enquiry from ${name}${datePart}`; per D-13 + FORM-05 + RESEARCH section 4

    NO npm imports — pure Web API (fetch, URLSearchParams, FormData, Response, Request, JSON). RESEARCH Package Legitimacy Audit confirms zero new runtime packages for this Function.

    NEVER use import.meta.env inside this file per Pitfall 8 — Pages Functions run in the Workers runtime, not Node/Vite. All env vars come from the `env` parameter on the handler context.

    Console.error for diagnostic visibility:
    - Resend non-2xx: console.error('Resend error:', emailRes.status, errorBody)
    - Uncaught: console.error('Unexpected error:', err)
    These appear in `wrangler pages dev` terminal locally and in CF Pages dashboard Functions logs in production (visible per CF Dashboard -> Workers & Pages -> klphotography -> Functions tab).

    Do NOT add request logging that captures form field values — that would leak couples' personal data into CF Function logs (GDPR concern, see threat T-05-01-06 below).
  </action>
  <verify>
    <automated>cd /c/projects/new-project && test -f functions/api/contact.ts && grep -q "onRequestPost" functions/api/contact.ts && grep -q "challenges.cloudflare.com/turnstile/v0/siteverify" functions/api/contact.ts && grep -q "api.resend.com/emails" functions/api/contact.ts && grep -q "reply_to" functions/api/contact.ts && ! grep -q "replyTo" functions/api/contact.ts && grep -q "contact_company" functions/api/contact.ts && grep -q "DEV_SKIP_TURNSTILE" functions/api/contact.ts && grep -q "CF-Connecting-IP" functions/api/contact.ts && grep -q "PagesFunction" functions/api/contact.ts && ! grep -q "import.meta.env" functions/api/contact.ts && ! grep -q "from ['\"]resend['\"]" functions/api/contact.ts && npx tsc --noEmit --project functions/tsconfig.json</automated>
  </verify>
  <done>
    - functions/api/contact.ts exists with onRequestPost export
    - Uses direct fetch to Turnstile siteverify + Resend /emails endpoints
    - Uses reply_to (snake_case) — NEVER replyTo
    - Honeypot check on contact_company (matches Phase 3 form field name)
    - DEV_SKIP_TURNSTILE bypass works only when env var === 'true'
    - CF-Connecting-IP threaded through to remoteip
    - Zero npm imports (no `from 'resend'`, no `import.meta.env`)
    - functions/tsconfig.json tsc check passes
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire Contact.astro real Turnstile widget + real fetch submit + success/error UI states</name>
  <files>src/components/sections/Contact.astro, src/pages/index.astro</files>
  <behavior>
    - On page load, the Turnstile widget renders inside Contact.astro at the data-sitekey position (was {/* Phase 5: */} comment in Phase 3)
    - On form submit with JS enabled: e.preventDefault() runs, HTML5 validation gate runs (form.checkValidity()), then submit button shows loading state, then fetch POST to /api/contact with FormData body
    - On 200 response: form.reset(), success notice text "Thanks — I'll be in touch within two working days.", red error class removed
    - On non-2xx response or fetch throw: error notice text "Something went wrong. Please use phone, email, or WhatsApp below.", red error class added (text-red-600 per RESEARCH section 5)
    - In finally block: submit button loading state removed
    - With JS disabled (no-JS path per D-09 + FORM-10): the form posts natively because the <form> tag has method="POST" action="/api/contact" — browser submits application/x-www-form-urlencoded; Function detects no JSON Accept header and returns inline HTML success page (per Function logic in Task 2)
    - Honeypot field input name="contact_company" remains hidden + tabindex=-1 + autocomplete=off (unchanged from Phase 3)
    - Privacy adjacency paragraph above submit button remains unchanged (already satisfies GDPR-05 per RESEARCH section 14)
  </behavior>
  <action>
    Modify src/components/sections/Contact.astro:

    1. Add method + action to the <form> tag per D-09 + RESEARCH section 6 for no-JS graceful degradation:
       Current line 14: `<form class="space-y-6" id="contact-form" novalidate>`
       Becomes:        `<form class="space-y-6" id="contact-form" novalidate method="POST" action="/api/contact">`

    2. Replace the Phase 3 comment-stub for Turnstile (current line 49) with the real widget. The data-sitekey is bound to import.meta.env.PUBLIC_TURNSTILE_SITE_KEY (Astro build-time substitution; PUBLIC_* exposes to browser per Astro convention, safe for site key per FORM-07).

       Current line 49: `{/* Phase 5: <div class="cf-turnstile" data-sitekey="YOUR-PUBLIC-SITE-KEY"></div> */}`
       Becomes:
       ```astro
       <div class="cf-turnstile" data-sitekey={import.meta.env.PUBLIC_TURNSTILE_SITE_KEY} data-theme="light"></div>
       ```
       (Add data-theme="light" because the site uses a cream/light editorial palette per Phase 2 design tokens — keeps Turnstile widget visually coherent.)

       Wrap the widget div in a noscript fallback so the no-JS path users see a clear message that spam protection requires JS:
       ```astro
       <noscript>
         <p class="text-sm text-ink-soft">Spam protection requires JavaScript. If you have JS disabled, please use phone, email, or WhatsApp below.</p>
       </noscript>
       <div class="cf-turnstile" data-sitekey={import.meta.env.PUBLIC_TURNSTILE_SITE_KEY} data-theme="light"></div>
       ```

    3. Replace the Phase 3 stub script (current lines 58-72) with the real submit handler per RESEARCH section 5 lines 329-373. Implementation contract:
       - Use document.getElementById('contact-form') typed as HTMLFormElement | null
       - Use document.getElementById('cf-notice') typed as HTMLParagraphElement | null
       - Get submit button via form?.querySelector('button[type="submit"]')
       - On submit: preventDefault, HTML5 validity gate (form.checkValidity() / form.reportValidity()), set loading state (submitBtn?.setAttribute('disabled', '')), update notice
       - Send FormData: const res = await fetch('/api/contact', { method: 'POST', body: new FormData(form), headers: { 'Accept': 'application/json' } })
       - On res.ok: form.reset(), notice text success, remove 'text-red-600' class
       - On non-ok or throw: notice text error, add 'text-red-600' class
       - In finally: remove disabled attribute from submit button
       - All notice text values match FORM-09 contract:
         - Sending: "Sending…" (the ellipsis character … not three dots)
         - Success: "Thanks — I'll be in touch within two working days."
         - Error: "Something went wrong. Please use phone, email, or WhatsApp below."

       Note: the Button primitive's `loading` prop is set at compile time (loading={false}) and is not reactive — wiring it dynamically requires either re-rendering or DOM manipulation. Use the simpler approach of disabling the rendered button via setAttribute. The Button primitive's disabled styling will activate via CSS.

    4. Add the Turnstile CDN script to src/pages/index.astro head slot per RESEARCH section 5 line 390 (NOT BaseLayout — only the home page contains the contact form). Use is:inline per Pitfall 3 to prevent Vite from trying to bundle the CDN URL.

       Read src/pages/index.astro to find the BaseLayout invocation. Add inside the BaseLayout opening tag's head slot:
       ```astro
       <script
         slot="head"
         src="https://challenges.cloudflare.com/turnstile/v0/api.js"
         async
         defer
         is:inline
       ></script>
       ```

       If src/pages/index.astro already passes other slot="head" content, add this script as an additional fragment within the same head slot (Astro permits multiple slot="head" elements; they all concatenate into the BaseLayout <slot name="head" />).

    Do NOT modify:
    - The privacy adjacency paragraph (current line 51) — satisfies GDPR-05 per RESEARCH section 14
    - The honeypot block (lines 15-17)
    - The aside contact-info column (lines 75-92)
    - The Section wrapper, h2, intro paragraph
    - The Button primitive itself (only call it with same props — loading={false} stays, the runtime disabled attribute is added imperatively)
  </action>
  <verify>
    <automated>cd /c/projects/new-project && grep -q 'method="POST"' src/components/sections/Contact.astro && grep -q 'action="/api/contact"' src/components/sections/Contact.astro && grep -q "cf-turnstile" src/components/sections/Contact.astro && grep -q "PUBLIC_TURNSTILE_SITE_KEY" src/components/sections/Contact.astro && grep -q "fetch('/api/contact'" src/components/sections/Contact.astro && ! grep -q "Phase 5:" src/components/sections/Contact.astro && ! grep -q "Form not yet active" src/components/sections/Contact.astro && grep -q "challenges.cloudflare.com/turnstile/v0/api.js" src/pages/index.astro && grep -q "is:inline" src/pages/index.astro && grep -q "Thanks" src/components/sections/Contact.astro && grep -q 'href="/privacy"' src/components/sections/Contact.astro && npm run build</automated>
  </verify>
  <done>
    - Contact.astro form has method=POST + action=/api/contact for no-JS path
    - Turnstile widget renders with data-sitekey bound to PUBLIC_TURNSTILE_SITE_KEY
    - Phase 3 stub comment and stub script removed
    - Real fetch submit handler with loading + success + error states
    - Turnstile CDN script in src/pages/index.astro head slot, is:inline
    - Privacy adjacency paragraph preserved verbatim
    - Honeypot field preserved
    - npm run build passes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Local end-to-end smoke via wrangler pages dev + CF preview env setup + live preview enquiry round-trip (FORM-11)</name>
  <what-built>functions/api/contact.ts handler + real Turnstile widget + real fetch submit in Contact.astro + Turnstile CDN script in index.astro + wrangler devDep + .gitignore .dev.vars. Need to verify end-to-end flow locally + on CF Pages preview before marking the plan done.</what-built>
  <how-to-verify>
    PART A — LOCAL DEV SMOKE (uses .dev.vars + wrangler pages dev):

    1. Create your local .dev.vars file (NOT committed — .gitignore covers it). Copy from template:
       cp .dev.vars.example .dev.vars
       Edit .dev.vars and replace RESEND_API_KEY with your real Resend API key. Set CONTACT_TO_EMAIL to your own test inbox (NOT klphotography.ie@gmail.com for local — avoid noise in the owner's inbox).
       Keep TURNSTILE_SECRET_KEY=1x0000000000000000000000000000000AA (test secret, always succeeds per RESEARCH section 7).
       Keep DEV_SKIP_TURNSTILE=true if you want to skip Turnstile entirely; or remove the line and test with the test secret.

    2. Start the local Pages dev server:
       npm run dev:full
       (This runs `npm run build &amp;&amp; wrangler pages dev dist/`. Server starts on http://localhost:8788 per RESEARCH section 17.)

    3. Open http://localhost:8788 in browser. Verify:
       a. Contact section renders the Turnstile widget visibly (small checkbox or auto-resolving managed widget)
       b. Submit empty form -> browser HTML5 validation fires (red outlines on required fields, no network request)
       c. Fill all required fields (name, email, message); leave wedding_date + venue empty; submit. Watch:
          - Button shows disabled state during request
          - DevTools Network tab shows POST /api/contact -> 200 with JSON {ok:true}
          - Notice text under button reads "Thanks — I'll be in touch within two working days."
          - Form fields reset to empty
       d. Check your test inbox: email arrived with subject "Wedding enquiry from <name>", reply_to is the email you entered

    4. Test with wedding_date filled:
       Submit again with all fields including a wedding_date. Verify email subject reads "Wedding enquiry from <name> — <YYYY-MM-DD>" per D-13 + FORM-05.

    5. Test honeypot path (simulate bot via DevTools):
       Open DevTools Console on http://localhost:8788. Run:
       document.querySelector('input[name="contact_company"]').value = 'bot-filler';
       Then submit. Verify:
       - Network shows POST /api/contact -> 200
       - Notice shows success text (bot is fooled)
       - Your inbox: NO email arrives (silent honeypot reject)

    6. Test no-JS path (graceful degradation per D-09 + FORM-10):
       DevTools -> Settings -> Disable JavaScript. Reload http://localhost:8788. Verify:
       - Turnstile widget shows the noscript fallback message
       - Submit with required fields -> browser submits natively
       - Function returns HTML success page (you should see plain "Thank you" page or browser stays on /api/contact with HTML response)
       - Email still arrives in your inbox (with DEV_SKIP_TURNSTILE=true, no token required)
       Re-enable JS.

    7. Test missing-required-field rejection:
       Re-enable JS. In DevTools Console:
       fetch('/api/contact', { method: 'POST', body: new URLSearchParams({ email: 'test@example.com' }) }).then(r => r.json()).then(console.log);
       Verify: {ok:false, error:"name is required"}

    8. Test Turnstile rejection (only meaningful if DEV_SKIP_TURNSTILE removed and you use an invalid token):
       Edit .dev.vars: comment out DEV_SKIP_TURNSTILE=true (or set to "false"). Restart `npm run dev:full`.
       In DevTools Console:
       fetch('/api/contact', { method: 'POST', body: new URLSearchParams({ name: 'X', email: 'x@y.z', message: 'hi'.repeat(20), 'cf-turnstile-response': 'definitely-not-valid' }) }).then(r => r.json()).then(console.log);
       Verify: {ok:false, error:"Turnstile verification failed"} with 403 status.
       Then re-enable DEV_SKIP_TURNSTILE=true for the rest of the session.

    PART B — CF PAGES PREVIEW ENV SETUP (one-time, in CF dashboard):

    9. Go to Cloudflare Dashboard -> Workers &amp; Pages -> klphotography -> Settings -> Variables and Secrets.
       Add for both Preview AND Production environments (per RESEARCH section 7):
       - RESEND_API_KEY (Encrypted/Secret): your real Resend key
       - TURNSTILE_SECRET_KEY (Encrypted/Secret): from Turnstile dashboard (NOT the test value — real CF Turnstile site secret)
       - CONTACT_TO_EMAIL (Plain text): klphotography.ie@gmail.com
       - CONTACT_FROM_EMAIL (Plain text): KL Photography <onboarding@resend.dev>  (per D-06; Phase 6 changes to enquiries@klphotography.ie)
       - PUBLIC_TURNSTILE_SITE_KEY (Plain text, BUILD-TIME): from Turnstile dashboard (the public Site Key)

       Add for Preview ONLY:
       - DEV_SKIP_TURNSTILE (Plain text): true  (per D-08 + D-09 — preview keeps the bypass for owner manual testing; production has NO such var)

       DO NOT add DEV_SKIP_TURNSTILE to Production.

    10. Push commit to a PR branch (or to main if owner accepts direct push) to trigger CF Pages preview build. Wait for the preview URL.

    PART C — LIVE PREVIEW ROUND-TRIP (FORM-11 per ROADMAP SC-1):

    11. Open preview URL (e.g. https://klphotography.pages.dev or a per-PR URL).
        a. Verify Turnstile widget renders (visible checkbox / managed challenge)
        b. Fill out form with realistic test enquiry (name "Test Couple", real test email you control, wedding_date e.g. 2026-09-15, venue "Test Venue", message at least 20 chars)
        c. Submit. Confirm:
           - Success notice appears
           - Form resets
           - Email arrives at klphotography.ie@gmail.com within seconds (check Gmail inbox)
           - Email reply_to is the test email you entered (Gmail "Reply" button addresses the test inbox, not the from)
           - Email subject: "Wedding enquiry from Test Couple — 2026-09-15"
           - Email body shows all fields with HTML escaping intact

    12. Test invalid Turnstile rejection on preview (REQUIRES temporarily removing DEV_SKIP_TURNSTILE in CF Preview env, OR using a separate branch where the env var is absent):
        Open preview URL. Use DevTools Console:
        fetch('/api/contact', { method: 'POST', body: new URLSearchParams({ name: 'X', email: 'x@y.z', message: 'hi'.repeat(20) }) }).then(r => console.log(r.status));
        Verify: status 403 (Turnstile token required).

        After test: restore DEV_SKIP_TURNSTILE=true for Preview if you removed it (so manual ad-hoc testing remains friction-free).

    Report: each of steps 3-7 (local), 11 (preview round-trip), 12 (preview 403). Paste email subject lines + the value of reply_to from email headers (View Original in Gmail).
  </how-to-verify>
  <resume-signal>Paste:
1. Local smoke: pass/fail for steps 3-7 (success email subject, honeypot silent, no-JS works, 400/403 rejections)
2. CF env setup confirmed (Preview + Production all 5+1 vars set)
3. Preview round-trip: email arrived at klphotography.ie@gmail.com with correct subject + reply_to
4. Preview 403 on missing token (with DEV_SKIP_TURNSTILE temporarily off)
Type "approved" if all pass. Describe any failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> /api/contact | Untrusted couple input crosses into a Worker; Turnstile + honeypot + validation gate apply |
| /api/contact -> challenges.cloudflare.com/turnstile/v0/siteverify | Outbound CF call; token + secret + remoteip |
| /api/contact -> api.resend.com/emails | Outbound HTTPS with Bearer auth; carries couple's name/email/message in JSON body |
| repo -> .dev.vars (local only) | Real Resend key on developer machine; never committed (D-07 enforcement) |
| repo -> CF Pages env vars | Secrets stored encrypted at CF; never in repo (FORM-06) |
| dist/ build output | PUBLIC_* env vars intentionally exposed; secret env vars must NOT appear (verified by 05-03 negative gate) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Spoofing | Missing or wrong reply_to lets owner reply to KL Photography's own from-address instead of the couple | mitigate | Use reply_to (snake_case per D-01 + Pitfall 2); Task 4 verifies via "View Original" in Gmail |
| T-05-01-02 | Tampering | No Turnstile enforcement in production = open spam relay | mitigate | env.DEV_SKIP_TURNSTILE check; production CF Pages env has NO such var (D-09); Task 4 step 12 verifies preview rejects missing token when bypass off |
| T-05-01-03 | Tampering | Field injection via newlines in name -> Resend subject header injection | mitigate | Resend SDK / REST API constructs headers from JSON body fields (`subject`, `from`, etc.) — Resend escapes header chars internally per their API contract (RESEARCH section 4 security note). No raw header construction in our code. |
| T-05-01-04 | Tampering | XSS via message field rendered in owner's Gmail HTML viewer | mitigate | esc() helper escapes &lt;, &gt;, &amp;, &quot;, &#x27; on all user fields before HTML template insertion per RESEARCH section 4 |
| T-05-01-05 | Information Disclosure | RESEND_API_KEY leaks into dist/ via accidental Astro import | mitigate | Function accesses env via `context.env.RESEND_API_KEY`, never via import.meta.env (Pitfall 8). 05-03 Task 3 negative grep gate enforces. |
| T-05-01-06 | Information Disclosure | Couple's PII (name/email/message) logged to CF Functions log via console.error | accept | console.error only emits on Resend failure (Resend status + error body, not the form fields) and on uncaught exception (err object, not request). Couple PII never logged. Documented in code comment. |
| T-05-01-07 | Repudiation | No timestamp / source IP on email -> owner cannot debug spam reports | mitigate | Resend automatically adds Date + X-Resend-* headers; CF-Connecting-IP threaded to siteverify (visible in CF Turnstile dashboard). Sufficient for v1. |
| T-05-01-08 | Denial of Service | High-volume spam submissions burn Resend quota (3000/mo free tier) | mitigate | Turnstile managed widget rate-limits at the CF edge before the Function executes; Resend free tier is natural cap; if exhausted, Function returns 502 to additional senders |
| T-05-01-09 | Elevation of Privilege | Honeypot bypass: bot sniffs `contact_company` field hidden CSS and skips it | mitigate | Honeypot is a defense-in-depth layer on top of Turnstile, not the primary control. If a bot bypasses honeypot AND Turnstile, that bot is sufficiently advanced that we accept the residual risk for v1. |
| T-05-01-SC | Tampering | npm install pulls wrangler (only new dep) | mitigate | Approved per RESEARCH Package Legitimacy Audit — wrangler is official Cloudflare CLI, ~8 years old, github.com/cloudflare/workers-sdk; postinstall script absent (verified). No human checkpoint needed; package is [Approved] not [ASSUMED]. |
</threat_model>

<plan_level_verification>
End-of-plan verification:

1. Static checks pass:
   cd /c/projects/new-project &amp;&amp; npm run build &amp;&amp; npx astro check &amp;&amp; npx tsc --noEmit --project functions/tsconfig.json
   # All exit 0

2. Repo hygiene:
   ! test -f .dev.vars                        # never committed
   test -f .dev.vars.example                  # template committed
   grep -q "^\.dev\.vars$" .gitignore         # explicit per D-07
   grep -q '"wrangler"' package.json          # devDep present per D-03

3. Function shape:
   grep -q "onRequestPost" functions/api/contact.ts
   grep -q "reply_to" functions/api/contact.ts
   ! grep -q "replyTo" functions/api/contact.ts
   ! grep -qE "from ['\"]resend['\"]" functions/api/contact.ts   # no SDK per D-01
   ! grep -q "import.meta.env" functions/api/contact.ts          # Pitfall 8

4. Form wiring:
   grep -q "method=\"POST\"" src/components/sections/Contact.astro    # no-JS path per D-09
   grep -q "fetch('/api/contact'" src/components/sections/Contact.astro
   grep -q "cf-turnstile" src/components/sections/Contact.astro

5. CDN script:
   grep -q "challenges.cloudflare.com/turnstile/v0/api.js" src/pages/index.astro
   grep -q "is:inline" src/pages/index.astro

6. Task 4 human checkpoint passes:
   - Local end-to-end email round-trip works against test inbox
   - Honeypot silent reject confirmed
   - No-JS path works (HTML success response)
   - 400 on missing required field
   - 403 on missing/invalid Turnstile token (when bypass off)
   - CF Preview env vars set per RESEARCH section 7 table
   - Live preview round-trip delivers to klphotography.ie@gmail.com with correct subject + reply_to (FORM-11 satisfied)
</plan_level_verification>

<risks>
- **R1: User has not yet provisioned Resend + Turnstile keys when execution starts** — Mitigation: keys can be provisioned in 10 min per RESEARCH "Environment Availability" section; .dev.vars.example documents what is needed; Task 4 checkpoint enforces real keys before declaring success.
- **R2: Turnstile widget fails to render on local dev because wrangler pages dev does not inject PUBLIC_TURNSTILE_SITE_KEY at build time the same way CF Pages does** — Mitigation: PUBLIC_* vars are read by Astro at `astro build` time from process.env. Set PUBLIC_TURNSTILE_SITE_KEY in shell env before `npm run dev:full`, OR add it to a local `.env` file (gitignored via existing `.env.*` pattern). If missing, the widget renders with data-sitekey="undefined" which causes Turnstile to display an error message (not silent — debuggable).
- **R3: Resend "from" address rejected because onboarding@resend.dev not allowed for production sends** — Mitigation: Resend's onboarding@resend.dev works on free tier without domain verify per RESEARCH section 3; verified across multiple Resend docs. If Resend changes this, fallback is to defer 05-01 until Phase 6 domain verify completes — but documented behavior as of 2026-05-18 is that the test domain works.
- **R4: CF Pages auto-detects functions/ — but if folder is misplaced (inside src/ or dist/) functions silently don't deploy** — Mitigation: Task 1 + Task 2 place functions at project root per RESEARCH section 1 (and Q6) — explicit verification in Task 4 preview round-trip catches this (would return 404 on /api/contact instead of 200).
- **R5: HTML escaping helper esc() incomplete -> XSS in owner's Gmail viewer** — Mitigation: helper escapes the 5 standard HTML special chars per OWASP recommendation (RESEARCH lines 1153-1157). Gmail's HTML renderer additionally sandboxes content. Defense-in-depth is sufficient for v1.
- **R6: No rate limiting beyond Turnstile -> sustained attack drains Resend quota** — Accepted for v1 per T-05-01-08; Phase 6 launch checklist can revisit if real-world spam emerges. CF Pages Functions free tier 100k requests/day provides additional natural cap.
</risks>

<success_criteria>
Maps to ROADMAP Phase 5 Success Criteria + REQUIREMENTS:

- SC-1 (preview URL form delivers email to klphotography.ie@gmail.com within seconds with reply_to): satisfied by full plan, verified in Task 4 step 11
- SC-2 (submissions without valid Turnstile token rejected with 403): satisfied by Task 2 logic, verified in Task 4 step 8 (local) + step 12 (preview)
- FORM-01: functions/api/contact.ts handles POST per RESEARCH section 1 (Task 2)
- FORM-02: all six required fields parsed; required-vs-optional gating per Task 2 behavior
- FORM-03: Turnstile siteverify with 403 on failure (Task 2)
- FORM-04: Resend send with reply_to to couple's email (Task 2, verified Task 4 step 11)
- FORM-05: Subject includes name + optional wedding_date per D-13 (Task 2, verified Task 4 step 4)
- FORM-06: All secrets in CF Pages env vars, never in repo (.dev.vars gitignored per D-07; Task 1)
- FORM-07: PUBLIC_TURNSTILE_SITE_KEY surfaced via Astro PUBLIC_* convention (Task 3)
- FORM-09: Success + error UI states (Task 3, verified Task 4 step 3)
- FORM-10: No-JS graceful degradation via form method=POST action=/api/contact + Function dual content-type parsing (Task 2 + Task 3, verified Task 4 step 6)
- FORM-11: Real end-to-end enquiry round-trip verified in Task 4 step 11

FORM-08 (Resend domain verify via SPF/DKIM) is OUT OF SCOPE for this plan — owned by 05-02 (docs) and Phase 6 (DNS execution).
</success_criteria>

<output>
Create `.planning/phases/05-contact-form-backend-gdpr/05-01-SUMMARY.md` per `@$HOME/.claude/get-shit-done/templates/summary.md`. Summary MUST include:

- **what-built:** functions/api/contact.ts (Turnstile siteverify + Resend send via direct fetch — zero new runtime deps); wrangler@^4.92.0 devDep; functions/tsconfig.json + types.d.ts; .dev.vars.example template + explicit .gitignore entry; Contact.astro real Turnstile widget + fetch submit + success/error UI; index.astro Turnstile CDN script
- **how-verified:** astro check + tsc on functions/ + npm run build + local wrangler pages dev end-to-end round-trip with test inbox + CF Preview round-trip delivering to klphotography.ie@gmail.com
- **deviations:** any field-name changes (e.g. wedding_date vs weddingDate); any subject-line wording difference from D-13
- **owner-action-required:**
  1. Provision Resend API key + add to CF Pages env (Preview + Production) as encrypted secret
  2. Provision CF Turnstile site + add TURNSTILE_SECRET_KEY (encrypted, both envs) + PUBLIC_TURNSTILE_SITE_KEY (plain, both envs, build-time)
  3. Set CONTACT_TO_EMAIL=klphotography.ie@gmail.com (plain, both envs)
  4. Set CONTACT_FROM_EMAIL="KL Photography <onboarding@resend.dev>" (plain, both envs — Phase 6 swaps Production to enquiries@klphotography.ie)
  5. Set DEV_SKIP_TURNSTILE=true on Preview ONLY (NOT Production)
  6. Copy .dev.vars.example to local .dev.vars for local testing
- **next-phase-readiness:** Phase 5 wave 2 picks up 05-03 (privacy + analytics + secret-leak audit). Phase 6 picks up FORM-08 (Resend domain verify per 05-02 runbook) + CONTACT_FROM_EMAIL production cutover to enquiries@klphotography.ie.
- **carry-forward:** Real form-submission rate monitoring (CF Pages Functions dashboard logs) recommended post-launch in Phase 6 retrospective.
</output>

<done>
- [ ] .gitignore contains explicit `.dev.vars` line per D-07
- [ ] wrangler@^4.92.0 in devDependencies per D-03 (zero new runtime deps)
- [ ] "dev:full" convenience script in package.json
- [ ] functions/tsconfig.json + functions/types.d.ts present
- [ ] .dev.vars.example committed; .dev.vars NOT committed
- [ ] functions/api/contact.ts exports onRequestPost (PagesFunction<Env>)
- [ ] Uses direct fetch (no `from 'resend'` import, no `import.meta.env`)
- [ ] Uses reply_to (snake_case per D-01) NOT replyTo
- [ ] Honeypot field contact_company silent-rejects per D-14
- [ ] DEV_SKIP_TURNSTILE bypass works only when env var === 'true' per D-09
- [ ] CF-Connecting-IP threaded to siteverify
- [ ] Both multipart/form-data AND application/x-www-form-urlencoded parsed per D-09
- [ ] HTML escape on all user fields before email html body
- [ ] Contact.astro: real Turnstile widget bound to PUBLIC_TURNSTILE_SITE_KEY
- [ ] Contact.astro: form has method=POST action=/api/contact for no-JS per D-09
- [ ] Contact.astro: real fetch submit handler with loading/success/error per FORM-09
- [ ] Contact.astro: privacy adjacency paragraph + honeypot preserved verbatim
- [ ] index.astro: Turnstile CDN script in head slot with is:inline
- [ ] npm run build exits 0
- [ ] Owner checkpoint: local wrangler dev round-trip works (email arrives at test inbox)
- [ ] Owner checkpoint: CF Preview env vars set per RESEARCH section 7 table
- [ ] Owner checkpoint: preview round-trip delivers to klphotography.ie@gmail.com with correct subject + reply_to (FORM-11)
- [ ] Owner checkpoint: preview returns 403 on missing token (DEV_SKIP_TURNSTILE off)
- [ ] SUMMARY.md created
</done>
</content>
</invoke>