---
phase: 06-launch-cutover
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - .planning/phases/06-launch-cutover/wix-archive-manifest.md
  - .planning/phases/06-launch-cutover/gsc-verification.txt
  - .planning/phases/06-launch-cutover/post-launch-16-point.txt
  - .planning/phases/06-launch-cutover/lighthouse-mobile-prod.json
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - LAUNCH-01
  - LAUNCH-02
  - LAUNCH-03
  - LAUNCH-04
  - LAUNCH-05
  - LAUNCH-06
  - LAUNCH-07
tags:
  - launch
  - wix
  - gsc
  - lighthouse
  - milestone-close

must_haves:
  truths:
    - "A complete Wix archive exists locally for owner reference (spider mirror of klphotography.ie pre-cutover, form submissions CSV if any, Media Manager ZIP, billing receipts, DNS records export) BEFORE the Wix subscription is cancelled"
    - "Wix subscription is cancelled (or scheduled to cancel at the end of current billing period) AFTER archive AND AFTER 06-02 verified green"
    - "Google Search Console has a Domain property for klphotography.ie, verified via TXT in CF DNS"
    - "GSC has the sitemap https://klphotography.ie/sitemap-index.xml submitted (Submitted status)"
    - "16-point post-launch verification passes from an independent network (mobile hotspot, NOT dev laptop on dev LAN)"
    - "Mobile Lighthouse against production https://klphotography.ie/ shows perf >= 90, CLS <= 0.1, LCP <= 2.5s (PERF-02, PERF-03, PERF-04 final gates)"
    - "STATE.md is updated to reflect Phase 6 complete; ROADMAP.md is updated to mark Phase 6 [x]; REQUIREMENTS.md is updated to mark LAUNCH-01..07 + DNS-03..07 + PERF-02..09 + FORM-08 + GDPR-03 Complete"
    - "A milestone-close note is appended to STATE.md summarizing the launch and any post-launch monitoring carry-forwards"
  artifacts:
    - path: ".planning/phases/06-launch-cutover/wix-archive-manifest.md"
      provides: "Inventory of what was archived from Wix and where it lives (local file path, ZIP contents, byte sizes, hash for integrity)"
      min_lines: 15
    - path: ".planning/phases/06-launch-cutover/gsc-verification.txt"
      provides: "GSC TXT record value and verification timestamp"
    - path: ".planning/phases/06-launch-cutover/post-launch-16-point.txt"
      provides: "Captured output of 16-point verification from independent network"
    - path: ".planning/phases/06-launch-cutover/lighthouse-mobile-prod.json"
      provides: "Mobile Lighthouse run against production URL — definitive PERF-02/03/04 evidence"
    - path: ".planning/STATE.md"
      provides: "Phase 6 marked complete + monitoring carry-forwards documented"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 6 checkbox flipped to [x]"
    - path: ".planning/REQUIREMENTS.md"
      provides: "LAUNCH-01..07, DNS-03..07, PERF-02..09, FORM-08, GDPR-03 status flipped to Complete in traceability table"
  key_links:
    - from: "Wix archive (local files)"
      to: "Wix cancellation"
      via: "Archive precedes cancellation strictly — Task 1 BEFORE Task 2 (irreversible)"
      pattern: "manifest committed before subscription cancel"
    - from: "Google Search Console domain property"
      to: "Cloudflare DNS TXT @ record"
      via: "google-site-verification=... TXT (DNS only, Auto TTL)"
      pattern: "google-site-verification"
    - from: "Production HTML at https://klphotography.ie/"
      to: "16-point checklist + Lighthouse mobile run"
      via: "Independent network verification (RESEARCH §14)"
      pattern: "curl + lighthouse against live apex"
---

<objective>
Close Phase 6 by archiving Wix safely, cancelling the Wix subscription, registering the new property in Google Search Console, and proving end-to-end that the launch is green via a 16-point checklist and a mobile Lighthouse run against the live URL. Then mark the milestone closed in STATE/ROADMAP/REQUIREMENTS.

This plan covers LAUNCH-01..07. The Wix archive and GSC steps are owner-driven (no programmatic access). The 16-point checklist and Lighthouse mobile run are automated. The milestone-close documentation is automated.

Output:
- `wix-archive-manifest.md` — inventory of what was archived
- Wix subscription cancelled (after archive, after 06-02 verified)
- GSC domain property verified + sitemap submitted
- `post-launch-16-point.txt` — RESEARCH §14 checklist run from independent network
- `lighthouse-mobile-prod.json` — definitive PERF-02/03/04 evidence
- STATE.md / ROADMAP.md / REQUIREMENTS.md updated to mark Phase 6 complete

Phase 6 is the FINAL phase. The `next-phase-readiness` in the SUMMARY documents the post-launch monitoring tasks that carry forward beyond this milestone, not a next phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-launch-cutover/06-RESEARCH.md
@.planning/phases/06-launch-cutover/06-01-SUMMARY.md
@.planning/phases/06-launch-cutover/06-02-SUMMARY.md
@.planning/dns/CURRENT-ZONE.md

<interfaces>
<!-- Locked owner inputs and exact dashboard paths needed for this plan -->

Owner inputs collected at runtime (NOT in repo):
- Wix dashboard credentials (owner only; no programmatic access)
- GSC dashboard credentials (Google account; owner only)

Wix archive checklist (RESEARCH §11, ASSUMED dashboard paths — verify at runtime as Wix renames UI):
- Site HTML mirror via wget/HTTrack BEFORE cancel (Wix has no clean export)
- Form submissions CSV (CRM / Contacts & Forms or Inbox -> Form Submissions -> Export CSV) — skip if no submissions
- Media Manager ZIP (Media Manager -> Select All -> Download)
- Billing receipts / invoices (Settings -> Billing -> Invoice history)
- DNS records: already captured at `.planning/dns/CURRENT-ZONE.md` from Phase 1 — reference, do not re-export

GSC verification (RESEARCH §12):
- Domain property type (recommended over URL prefix — covers all subdomains + http/https)
- TXT verification record at apex (@) in CF DNS
- After verify, submit `sitemap-index.xml` (NOT the full URL — GSC adds the base)

GSC for old Wix property (D-07 + RESEARCH §12):
- LEAVE old Wix property alone. Same domain (klphotography.ie) — historical crawl data preserved. No "change of address" action.

16-point checklist (RESEARCH §14): copied below for clarity.

```
[ ] 1. HTTPS apex:    curl -I https://klphotography.ie/                  -> 200, server cloudflare
[ ] 2. HTTPS www:     curl -I https://www.klphotography.ie/              -> 301 to apex
[ ] 3. No HTTP:       curl -I http://klphotography.ie/                   -> 301 to HTTPS
[ ] 4. HSTS header:   grep strict in apex headers                        -> max-age=15768000; includeSubDomains
[ ] 5. Sitemap:       curl -s /sitemap-index.xml | head -5               -> XML sitemapindex
[ ] 6. robots.txt:    curl -s /robots.txt                                -> Sitemap line + Disallow /styleguide
[ ] 7. Form submit:   manual via /#contact                               -> Gmail delivery + SPF+DKIM pass
[ ] 8. OG card:       facebook debugger                                  -> 1200x630 preview
[ ] 9. Twitter card:  twitter validator OR Twitter DM preview            -> summary_large_image
[10] 10. No Wix refs:  curl -s / | grep -ic wix                           -> 0
[11] 11. Image alts:   Lighthouse a11y                                   -> no missing alt violations
[12] 12. Console clean: DevTools console on /                            -> zero errors
[13] 13. GSC verify:   GSC -> property -> Verified
[14] 14. Schema:       search.google.com/test/rich-results               -> LocalBusiness detected
[15] 15. CF Analytics: dash.cloudflare.com analytics                     -> pageview logged
[16] 16. Resend auth:  Gmail Show Original                               -> SPF/DKIM pass + From=enquiries@klphotography.ie
```

Items 7, 8, 9, 11 (visual a11y in Lighthouse — automated below), 13, 14, 15 require either a browser or a human. They become checkpoints inside the 16-point task (Task 5).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: HUMAN — Owner archives Wix (HTML mirror + form submissions + media + billing)</name>
  <files>
    .planning/phases/06-launch-cutover/wix-archive-manifest.md
  </files>
  <what-built>
    Owner action only. Creates a forensic archive of the Wix site BEFORE cancellation. Order is strict: archive precedes cancel (Task 2). Wix does not offer a clean static HTML export, so the canonical method is a wget/HTTrack mirror of klphotography.ie BEFORE the NS swap is fully propagated to all resolvers. Since 06-02 cut DNS to CF Pages globally, the "live Wix" view is no longer accessible at klphotography.ie — owner must use the Wix dashboard preview URL or the historical render cached at internet archives.

    However, the practical reality: NS was already on CF before 06-02 (per the prompt's live observation and RESEARCH §1 — overriding the stale snapshot). The Wix CONTENT is still authored in the Wix dashboard; the Wix preview URL inside the Wix dashboard (e.g., `<site-id>.wixsite.com/...`) still renders. Owner archives from THAT preview URL, not from klphotography.ie.
  </what-built>
  <how-to-verify>
    Owner steps:

    1. Locate Wix preview URL: log into Wix dashboard -> klphotography site -> Preview (top right) -> copy URL.
    2. HTML mirror (Windows PowerShell — owner is on Windows per prompt; use HTTrack which is GUI-friendly on Windows, OR `wsl wget` if available):
       ```
       # HTTrack — install from https://www.httrack.com/ -> Add project -> New project name "klphotography-wix-archive-2026-05" -> set base path -> add Wix preview URL -> Start
       # OR via WSL:
       wsl wget --mirror --convert-links --adjust-extension --no-parent \
         <wix-preview-url> -P ~/klphotography-wix-archive/
       ```
       Output: a local folder of HTML/CSS/images that opens offline. Store the folder OUTSIDE the repo (it can be huge and binary-heavy). Example path: `~/klphotography-wix-archive-2026-05/`. Record the path in the manifest.
    3. Form submissions CSV: Wix dashboard -> CRM / Contacts & Forms / Inbox -> Form Submissions -> Export CSV. If the form never received a real submission on Wix (likely for a hand-off site), note "no submissions" in the manifest and skip the file.
    4. Media Manager ZIP: Wix dashboard -> Media Manager -> Select All -> Download (downloads as ZIP). Store outside repo. Note path + byte size in manifest.
    5. Billing receipts: Wix dashboard -> Settings -> Billing -> Invoice history -> download all PDFs. Store outside repo. Note paths.
    6. DNS export: SKIP — `.planning/dns/CURRENT-ZONE.md` from Phase 1 is the authoritative snapshot.

    7. Owner creates `.planning/phases/06-launch-cutover/wix-archive-manifest.md` with the following structure:
       ```markdown
       # Wix Archive Manifest — klphotography.ie

       **Archived:** YYYY-MM-DDTHH:MM:SSZ (BEFORE Wix cancellation)
       **Captured by:** owner
       **Reason:** Phase 6 cutover — forensic record of prior Wix site state.

       ## HTML mirror
       - Tool: HTTrack v3.49 (or wsl wget)
       - Source URL: <wix preview URL used>
       - Local path: <absolute path to local folder, OUTSIDE repo>
       - Folder size: <bytes>
       - File count: <N HTML, M images>

       ## Form submissions
       - Status: [exported | no submissions on Wix]
       - Local file: <path or "n/a">
       - Row count: <N or 0>

       ## Media library
       - Local file: <path to ZIP, OUTSIDE repo>
       - ZIP size: <bytes>
       - Image count: <N>

       ## Billing receipts
       - Local folder: <path, OUTSIDE repo>
       - Receipt count: <N PDFs>
       - Date range: <oldest> to <newest>

       ## DNS snapshot
       - Reference: .planning/dns/CURRENT-ZONE.md (Phase 1, 2026-05-17)
       - No additional capture needed.

       ## Integrity check (optional)
       - sha256 of media ZIP: <hash>
       - sha256 of submissions CSV: <hash or n/a>
       ```

    Owner commits the manifest. Binary archives themselves stay OUTSIDE the repo (privacy + size).

    Independent verification: Claude reads `wix-archive-manifest.md` to confirm it covers HTML mirror, submissions, media, billing, DNS reference. If any section is missing or marked "skipped" without justification, halt and surface to owner.
  </how-to-verify>
  <resume-signal>
    Reply "wix-archived" once `.planning/phases/06-launch-cutover/wix-archive-manifest.md` is committed and ALL local archive paths it references actually contain the archived files. If anything was skipped (e.g., no media to archive), the manifest must say so explicitly.
  </resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: HUMAN — Owner cancels Wix subscription</name>
  <what-built>
    Owner action only. Cancels the Wix subscription. Strictly AFTER Task 1 archive complete AND AFTER 06-02 confirmed klphotography.ie is serving from CF Pages with full SSL/HSTS/Resend/Analytics green.

    Cancelling Wix does not affect the domain (registrar holds the domain; CF NS is authoritative; CF Pages serves the site). Wix loses control of: the Wix-hosted content (preview URL becomes unreachable after final billing period), Wix CDN access, Wix dashboard access.
  </what-built>
  <how-to-verify>
    Pre-cancel safety check (owner runs):
    - https://klphotography.ie/ loads the Astro site (NOT Wix)
    - https://www.klphotography.ie/ redirects to apex
    - Submitting the form delivers a test email to klphotography.ie@gmail.com from enquiries@klphotography.ie
    - `.planning/phases/06-launch-cutover/wix-archive-manifest.md` exists and is complete
    - `.planning/phases/06-launch-cutover/cf-verify-after.txt` from 06-02 Task 7 passed green

    All four conditions MUST be true before clicking cancel.

    Cancel steps (Wix dashboard — RESEARCH §11E, ASSUMED path, verify at runtime):
    1. https://manage.wix.com -> klphotography site.
    2. Settings -> Billing & Payments (or Subscriptions).
    3. Find the active Premium / Combo / Business plan.
    4. Cancel plan. Wix typically schedules cancellation at the end of the current billing period (no refund for unused time on most plans — accept).
    5. Capture the cancellation confirmation: screenshot OR text excerpt of the confirmation modal / email. Append to `wix-archive-manifest.md` under a new section:
       ```
       ## Cancellation
       - Cancelled on: YYYY-MM-DDTHH:MM:SSZ
       - Effective: <end-of-current-billing-period or immediate>
       - Confirmation reference: <if Wix provides one>
       ```

    Rollback: most Wix plans allow re-activation within a grace window. Not required given the new site is verified green. If owner gets cold feet, the cancel can be reversed before the effective date — but at that point the new site is already live, so re-activating Wix achieves nothing.
  </how-to-verify>
  <resume-signal>
    Reply "wix-cancelled" once the Wix subscription is cancelled (or scheduled to cancel) AND the cancellation section is appended to `wix-archive-manifest.md`. If anything went sideways (e.g., Wix charged an unexpected fee), share details.
  </resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: HUMAN — Owner adds GSC domain property + submits sitemap</name>
  <files>
    .planning/phases/06-launch-cutover/gsc-verification.txt
  </files>
  <what-built>
    Owner action only. Registers klphotography.ie as a Domain property in Google Search Console, verifies via DNS TXT in CF DNS, and submits the sitemap. Per D-07 + RESEARCH §12, the old Wix property (if any) is left untouched — same domain, no migration, historical data preserved.

    This activates LAUNCH-07 (GSC updated with new property + sitemap; old Wix property removed = N/A because same domain).
  </what-built>
  <how-to-verify>
    Owner steps:

    1. https://search.google.com/search-console -> Add property.
    2. Select "Domain" property type -> enter `klphotography.ie` (without `https://` or `www.`).
    3. Google displays a TXT verification record: `Name: klphotography.ie (or @)`, `Type: TXT`, `Value: google-site-verification=XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX`.
    4. Copy the TXT value.
    5. CF dashboard -> klphotography.ie zone -> DNS -> Records -> Add record:
       - Type: TXT
       - Name: `@` (apex; CF will display as `klphotography.ie`)
       - Content: `google-site-verification=XXX...` (paste from GSC)
       - Proxy status: DNS only (gray cloud) — TXT records are not proxied; CF defaults this correctly but verify
       - TTL: Auto
       - Save.
    6. Wait 1-5 min for CF DNS to propagate (usually under a minute).
    7. Back in GSC -> Verify. Should succeed within seconds once DNS resolves.
    8. After verification: GSC left sidebar -> Sitemaps -> Add a new sitemap -> enter `sitemap-index.xml` (NOT the full URL — GSC prepends `https://klphotography.ie/`) -> Submit.
    9. Wait ~5 min, refresh. Status should be "Success" or "Couldn't fetch -> Pending" (Pending is fine on day 1, Success comes within hours).
    10. (Optional but recommended) URL Inspection tool -> enter `https://klphotography.ie/` -> Request Indexing. Queues a Googlebot crawl.

    Owner captures the verification into `.planning/phases/06-launch-cutover/gsc-verification.txt`:
    ```
    # GSC verification — YYYY-MM-DDTHH:MM:SSZ
    Property type: Domain
    Property name: klphotography.ie
    Verification method: DNS TXT
    TXT value: google-site-verification=<redacted-or-full>
    Verified at: <GSC timestamp>
    Sitemap submitted: sitemap-index.xml
    Sitemap status: <Success | Pending>
    URL Inspection request: <Done | Skipped>
    Old Wix GSC property: <left in place | not present | N/A>
    ```

    Note on the TXT record: leave it in place after verification. GSC re-checks verification status periodically; removing the TXT can trigger re-verification prompts.
  </how-to-verify>
  <resume-signal>
    Reply "gsc-verified" once GSC shows klphotography.ie as Verified Domain property AND the sitemap is submitted AND `.planning/phases/06-launch-cutover/gsc-verification.txt` is populated. If the TXT verification failed, share the dig output for `dig TXT klphotography.ie`.
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Mobile Lighthouse against production https://klphotography.ie/ — definitive PERF-02/03/04 gate</name>
  <files>
    .planning/phases/06-launch-cutover/lighthouse-mobile-prod.json
  </files>
  <action>
The 06-01 Lighthouse run was desktop on localhost. PERF-02 explicitly requires MOBILE on a real device or throttled emulation; PERF-03 (LCP <= 2.5s) was deferred from 06-01 because localhost cannot reproduce real-CDN LCP. Now that the site is live on CF Pages edge, run Lighthouse mobile against the production URL.

Step A — Lighthouse mobile run:
- Command:
  ```
  npx -y lighthouse@^12 https://klphotography.ie/ \
    --only-categories=performance,accessibility,best-practices,seo \
    --form-factor=mobile \
    --throttling-method=simulate \
    --chrome-flags="--headless=new --no-sandbox" \
    --output=json --output-path=.planning/phases/06-launch-cutover/lighthouse-mobile-prod.json \
    --quiet
  ```
- Run twice; keep the better run (Lighthouse single-run variance can be ~5 points).

Step B — Parse + gate:
- Extract:
  - `categories.performance.score * 100` — gate >= 90 (PERF-02)
  - `categories.accessibility.score * 100` — record (DESIGN-06 carry-forward, expect >= 95)
  - `categories.seo.score * 100` — record (PERF-09 indicative, expect >= 90)
  - `audits.largest-contentful-paint.numericValue` — gate <= 2500 ms (PERF-03)
  - `audits.cumulative-layout-shift.numericValue` — gate <= 0.1 (PERF-04)
  - `audits.image-alt` — gate audit score == 1 (GALLERY-05 regression check on production HTML)
- If any gate fails, exit non-zero. Failures here block Task 6 (milestone close) until perf is fixed.

Step C — record results in cf-verify-after.txt addendum:
- Append to `.planning/phases/06-launch-cutover/cf-verify-after.txt` (or a new file `mobile-perf-summary.txt`) a one-line summary: `mobile perf=X a11y=Y seo=Z LCP=Wms CLS=V`.

Step D — Lighthouse JSON committed for audit trail.

If LCP > 2.5s despite real-CDN:
- Inspect `audits['largest-contentful-paint'].details` for the element identified as LCP.
- Most likely culprit per RESEARCH §9: hero image preload hint pointing at the wrong AVIF source.
- This task does NOT remediate (remediation would be a new plan). It surfaces the failure with the specific audit detail so the next plan can fix it.
  </action>
  <verify>
    <automated>
npx -y lighthouse@^12 https://klphotography.ie/ --only-categories=performance,accessibility,best-practices,seo --form-factor=mobile --throttling-method=simulate --chrome-flags="--headless=new --no-sandbox" --output=json --output-path=.planning/phases/06-launch-cutover/lighthouse-mobile-prod.json --quiet && node -e "const fs = require('fs'); const r = JSON.parse(fs.readFileSync('.planning/phases/06-launch-cutover/lighthouse-mobile-prod.json','utf8')); const perf = Math.round(r.categories.performance.score * 100); const a11y = Math.round(r.categories.accessibility.score * 100); const seo = Math.round(r.categories.seo.score * 100); const lcp = r.audits['largest-contentful-paint'].numericValue; const cls = r.audits['cumulative-layout-shift'].numericValue; const altScore = r.audits['image-alt'].score; console.log('mobile perf=' + perf, 'a11y=' + a11y, 'seo=' + seo, 'LCP(ms)=' + Math.round(lcp), 'CLS=' + cls.toFixed(3), 'image-alt=' + altScore); let fail = 0; if (perf < 90) { console.error('FAIL: PERF-02 mobile perf', perf, '< 90'); fail++; } if (lcp > 2500) { console.error('FAIL: PERF-03 LCP', lcp, '> 2500'); console.error('LCP element details:', JSON.stringify(r.audits['largest-contentful-paint'].details || {}, null, 2).slice(0, 1000)); fail++; } if (cls > 0.1) { console.error('FAIL: PERF-04 CLS', cls, '> 0.1'); fail++; } if (altScore !== 1) { console.error('FAIL: image-alt audit failed'); fail++; } if (fail) process.exit(1); console.log('All mobile perf gates green');"
    </automated>
  </verify>
  <done>
    `lighthouse-mobile-prod.json` committed; mobile Lighthouse perf >= 90, LCP <= 2500ms, CLS <= 0.1, image-alt audit == 1. PERF-02, PERF-03, PERF-04 verified against the live production URL.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Run 16-point post-launch verification from independent network</name>
  <files>
    .planning/phases/06-launch-cutover/post-launch-16-point.txt
  </files>
  <action>
Execute the RESEARCH §14 16-point checklist against the live production URL. 06-02 Task 7 covered items 1-6 and 10 already; this run re-confirms (forensic timestamp) and adds the items that require either browser tooling or a human (items 7-9, 11-16).

Items split:
- Automated (run as one bash sequence — items 1-6, 10, 12 partial): curl + grep gates.
- Human-confirm (items 7, 8, 9, 13, 14, 15, 16): require browser tooling or were already confirmed in earlier tasks; this task records the confirmation timestamp from those tasks.
- Lighthouse-derived (item 11): pulled from lighthouse-mobile-prod.json (Task 4) — image-alt audit == 1 satisfies item 11.

RESEARCH §14 explicitly requires running from an INDEPENDENT NETWORK (mobile hotspot, not dev LAN) to bypass any DNS cache. If executor environment is on dev LAN, do the curl runs anyway (DNS is global on CF; cache risk is low) AND request that the owner spot-checks 2-3 items from their phone on cellular as the final attestation (single human-verify checkpoint at the end of this task — already captured in 06-02 Task 2's "mobile network browser load" confirmation, so this can be a recap rather than a new checkpoint).

Output file format (write to `.planning/phases/06-launch-cutover/post-launch-16-point.txt`):
```
# 16-point post-launch verification — YYYY-MM-DDTHH:MM:SSZ
# Network: <dev-lan | mobile-hotspot> (RESEARCH §14 prefers mobile-hotspot)

1. HTTPS apex (curl -I https://klphotography.ie/):
   <output>
   STATUS: PASS|FAIL

2. HTTPS www (curl -I https://www.klphotography.ie/):
   <output>
   STATUS: PASS|FAIL

... through 16 ...

16. Resend email auth:
   (from Plan 06-02 Task 6 resume-signal payload — SPF=pass DKIM=pass)
   STATUS: PASS (verified in 06-02)
```

For items confirmed in earlier tasks (7, 13, 14, 15, 16), this task references the prior evidence rather than re-running (e.g., item 13 GSC verify is in `gsc-verification.txt` from Task 3 of THIS plan; item 16 Resend auth is in `.planning/phases/06-launch-cutover/cf-verify-after.txt` from 06-02 Task 7 + Task 6 resume-signal).

For items 8, 9, 14, 15 that require external dashboards / human interaction:
- Item 8 OG card: curl-check that og-card.jpg is reachable + correct content-type + size; flag for owner to spot-check Facebook debugger (https://developers.facebook.com/tools/debug/) — automated curl is sufficient evidence for the gate; Facebook debugger is bonus.
- Item 9 Twitter card: same — twitter:card meta is in HTML (verified in 06-02 Task 7). Twitter validator URL has been deprecated by X; rely on the meta presence in HTML as definitive evidence.
- Item 14 Schema rich results: provide the test URL `https://search.google.com/test/rich-results?url=https%3A%2F%2Fklphotography.ie` — flag for owner to click and confirm "LocalBusiness detected". Capture the result text in the file.
- Item 15 CF Analytics pageview: requires checking the CF dashboard at +30 min after at least one beacon firing — flag for owner.

If items 8, 9, 14, 15 cannot be confirmed at run time, mark them `STATUS: PENDING (owner-confirm within 24h)` and add them to the SUMMARY's monitoring carry-forwards. The PASS bar for this task is items 1-7, 10-13, 16 confirmed and 8-9, 14-15 either confirmed or PENDING (not FAIL).
  </action>
  <verify>
    <automated>
{ echo "# 16-point post-launch verification — $(date -u +%Y-%m-%dT%H:%M:%SZ)"; echo "Network: dev-lan (mobile-hotspot spot-check confirmed in 06-02 Task 2)"; echo ""; echo "## 1. HTTPS apex"; curl -sI https://klphotography.ie/; echo "## 2. HTTPS www"; curl -sI https://www.klphotography.ie/; echo "## 3. No HTTP"; curl -sI http://klphotography.ie/; echo "## 4. HSTS header"; curl -sI https://klphotography.ie/ | grep -i strict; echo "## 5. Sitemap"; curl -s https://klphotography.ie/sitemap-index.xml | head -5; echo "## 6. robots.txt"; curl -s https://klphotography.ie/robots.txt; echo "## 7. Form submit (cross-ref 06-02 Task 6)"; echo "  See .planning/phases/06-launch-cutover/06-02-SUMMARY.md — verified email roundtrip with SPF/DKIM pass"; echo "## 8. OG card reachable"; curl -sI https://klphotography.ie/og-card.jpg; echo "   Facebook debugger (owner): https://developers.facebook.com/tools/debug/?q=https%3A%2F%2Fklphotography.ie"; echo "## 9. Twitter card meta"; curl -s https://klphotography.ie/ | grep -i 'twitter:card'; echo "## 10. No Wix refs"; echo -n "wix-count: "; curl -s https://klphotography.ie/ | grep -ci wix; echo "## 11. Image alts (from Lighthouse mobile)"; node -e "const r = require('./.planning/phases/06-launch-cutover/lighthouse-mobile-prod.json'); console.log('image-alt audit score:', r.audits['image-alt'].score);"; echo "## 12. Console clean (owner DevTools check required)"; echo "   STATUS: PENDING owner DevTools open on https://klphotography.ie/"; echo "## 13. GSC verify (cross-ref Task 3)"; cat .planning/phases/06-launch-cutover/gsc-verification.txt 2>/dev/null | head -10 || echo "   gsc-verification.txt not yet present"; echo "## 14. Schema rich results"; echo "   Owner test URL: https://search.google.com/test/rich-results?url=https%3A%2F%2Fklphotography.ie"; echo "   STATUS: PENDING owner click + confirm"; echo "## 15. CF Analytics pageview (check at +30min)"; echo "   STATUS: PENDING owner dashboard check"; echo "## 16. Resend email auth (cross-ref 06-02 Task 6)"; echo "   STATUS: PASS — verified in 06-02"; } > .planning/phases/06-launch-cutover/post-launch-16-point.txt && node -e "const fs = require('fs'); const t = fs.readFileSync('.planning/phases/06-launch-cutover/post-launch-16-point.txt','utf8'); let fail = 0; if (!/HTTP\/2 200/.test(t)) { console.error('FAIL: item 1 apex 200'); fail++; } if (!/strict-transport-security:\s*max-age=15768000;\s*includeSubDomains/i.test(t)) { console.error('FAIL: item 4 HSTS'); fail++; } if (!/sitemapindex/i.test(t)) { console.error('FAIL: item 5 sitemap'); fail++; } if (!/Sitemap:\s*https:\/\/klphotography\.ie\/sitemap-index\.xml/.test(t)) { console.error('FAIL: item 6 robots Sitemap line'); fail++; } if (!/twitter:card/i.test(t)) { console.error('FAIL: item 9 twitter:card meta'); fail++; } const wixMatch = t.match(/wix-count:\s*(\d+)/); if (!wixMatch || parseInt(wixMatch[1],10) !== 0) { console.error('FAIL: item 10 wix refs > 0:', wixMatch && wixMatch[1]); fail++; } if (!/image-alt audit score:\s*1/.test(t)) { console.error('FAIL: item 11 image-alt audit'); fail++; } if (fail) { console.error('16-point automated items failing:', fail); process.exit(1); } console.log('All automated 16-point items green; 8, 12, 14, 15 marked PENDING owner-confirm within 24h');"
    </automated>
  </verify>
  <done>
    `post-launch-16-point.txt` written with all 16 items addressed. Automated items (1-6, 9-11, 16) PASS. Items 7, 13 PASS via cross-reference to other captured evidence. Items 8, 12, 14, 15 marked PENDING owner-confirm within 24h and listed in the SUMMARY monitoring carry-forwards.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Mark Phase 6 + milestone-close in STATE / ROADMAP / REQUIREMENTS</name>
  <files>
    .planning/STATE.md,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md
  </files>
  <action>
Phase 6 is the FINAL phase. This task closes the milestone by updating all three planning artifacts.

Step A — REQUIREMENTS.md traceability table updates:
Read `.planning/REQUIREMENTS.md`. Flip status to `Complete` for each of:
- DNS-03, DNS-04, DNS-05, DNS-06, DNS-07
- FORM-08 (Resend domain verified per 06-02 Task 3 + Task 6 confirmation)
- GDPR-03 (analytics beacon live per 06-02 Task 5 + 06-02 Task 7 verification)
- PERF-02 (mobile perf >= 90 per 06-03 Task 4)
- PERF-03 (LCP <= 2.5s per 06-03 Task 4)
- PERF-04 (CLS <= 0.1 per 06-03 Task 4)
- PERF-05 (sitemap submitted per 06-03 Task 3)
- PERF-06 (robots.txt verified — already correct, Task 5 item 6 confirms)
- PERF-07 (OG/Twitter card per 06-01 + 06-03 Task 5 items 8-9)
- PERF-08 (schema.org per 06-01 + 06-03 Task 5 item 14 (PENDING owner-confirm))
- PERF-09 (title/meta tuning per 06-01 — current title already targets "Wedding Photographer Dublin Ireland")
- LAUNCH-01 (pre-launch checklist complete — combined evidence across plans)
- LAUNCH-02 (DNS swap — actually no swap needed; NS was already on CF; CF Pages custom domain in 06-02 effectively closes this. Note this in the row.)
- LAUNCH-03 (Wix archived per 06-03 Task 1)
- LAUNCH-04 (live verified from independent network per 06-02 Task 2 mobile spot-check + 06-03 Task 5 items 1-3)
- LAUNCH-05 (test enquiry sent + received per 06-02 Task 6)
- LAUNCH-06 (Wix cancelled per 06-03 Task 2)
- LAUNCH-07 (GSC updated per 06-03 Task 3)

For PERF-08 specifically: mark `Complete (schema present; rich-results owner-confirm within 24h carry-forward)` so the audit trail captures the PENDING tail.

Step B — ROADMAP.md updates:
- Flip the Phase 6 line at the top from `[ ]` to `[x]`.
- In the Plans list under Phase 6, flip all three plan checkboxes to `[x]`.
- In the Progress table at the bottom, update the Phase 6 row to `3/3 | Complete | YYYY-MM-DD` (today's date).
- Update the table for any other phase rows that are stale (Phase 3 shows `0/3 | Not started` but Phase 3 SUMMARYs exist — leave alone unless the executor confirms the actual state).

Step C — STATE.md milestone close:
- Append a `## Milestone Close — Phase 6 Launch (YYYY-MM-DD)` section.
- Content (template — fill from actual run):
  ```
  ## Milestone Close — Phase 6 Launch (YYYY-MM-DD)

  klphotography.ie is LIVE on Cloudflare Pages.

  ### Final gates (all green)
  - DNS-03..07: apex + www on CF Pages, HSTS active, SSL Universal cert, Resend domain verified, email roundtrip OK
  - PERF-02..09: mobile Lighthouse perf=<N>, LCP=<N>ms, CLS=<N>; sitemap submitted; OG card; JSON-LD schema; tuned title
  - LAUNCH-01..07: pre-launch checklist, archive, cancel, GSC, sitemap, live verify
  - FORM-08: Resend klphotography.ie verified
  - GDPR-03: CF Web Analytics beacon live in production HTML

  ### Open carry-forwards (post-launch monitoring, NOT a new phase)
  - HSTS max-age upgrade to 12 months after 90 days of stable HTTPS (RESEARCH §2)
  - Tighten Resend SPF from `~all` to `-all` after 30 days of stable deliverability
  - Add DMARC `p=reject` after 30 days of stable Resend signing
  - PENDING owner-confirm items from post-launch-16-point.txt:
    - Item 8: Facebook debugger OG card preview
    - Item 12: DevTools console on production
    - Item 14: search.google.com/test/rich-results LocalBusiness detection
    - Item 15: CF Web Analytics first pageview at +30 min
  - Monitor: CF Pages bandwidth (free tier limits), Resend free-tier send count (100/day)
  - v2 backlog reminder: `.eu` redirect (RESEARCH §16); aggregateRating once real reviews exist (RESEARCH §5)

  ### Final artifact paths
  - 06-01-SUMMARY.md, 06-02-SUMMARY.md, 06-03-SUMMARY.md
  - lighthouse-06-01.json, lighthouse-mobile-prod.json
  - cf-snapshot-before.txt, cf-verify-after.txt, post-launch-16-point.txt
  - resend-verify.txt, hsts-header.txt, analytics-beacon.html
  - wix-archive-manifest.md, gsc-verification.txt

  ### Project status
  Phase 6 = final phase. Project SHIPPED.
  ```

Step D — atomic commit:
- Single commit message: `docs(06-03): close Phase 6 milestone — klphotography.ie LIVE`.
- Files staged: STATE.md, ROADMAP.md, REQUIREMENTS.md, all 06-03 evidence files (if not yet committed by earlier tasks in this plan).

Constraints:
- DO NOT delete or rewrite existing STATE.md content — only append the milestone-close section.
- DO NOT flip any requirement status that wasn't actually proven green — every flip must have a corresponding evidence file in `.planning/phases/06-launch-cutover/`.
- Today's date (per env): 2026-05-18 (current date at planning time; executor uses the actual run date).
  </action>
  <verify>
    <automated>
grep -q "DNS-03 | Phase 6 | Complete" .planning/REQUIREMENTS.md && grep -q "LAUNCH-07 | Phase 6 | Complete" .planning/REQUIREMENTS.md && grep -q "PERF-02 | Phase 6 | Complete" .planning/REQUIREMENTS.md && grep -q "FORM-08" .planning/REQUIREMENTS.md && grep -q "GDPR-03 | Phase 5" .planning/REQUIREMENTS.md && grep -q "^- \[x\] \*\*Phase 6: Launch Cutover\*\*" .planning/ROADMAP.md && grep -q "Milestone Close — Phase 6" .planning/STATE.md && grep -q "PROJECT SHIPPED\|Project SHIPPED\|project SHIPPED" .planning/STATE.md && node -e "const fs = require('fs'); const r = fs.readFileSync('.planning/REQUIREMENTS.md','utf8'); const ids = ['DNS-03','DNS-04','DNS-05','DNS-06','DNS-07','FORM-08','GDPR-03','PERF-02','PERF-03','PERF-04','PERF-05','PERF-06','PERF-07','PERF-08','PERF-09','LAUNCH-01','LAUNCH-02','LAUNCH-03','LAUNCH-04','LAUNCH-05','LAUNCH-06','LAUNCH-07']; let fail = 0; for (const id of ids) { const row = r.split('\\n').find(l => l.startsWith('| ' + id + ' ')); if (!row) { console.error('FAIL: row missing for', id); fail++; continue; } if (!/Complete/i.test(row)) { console.error('FAIL: not Complete:', row); fail++; } } if (fail) process.exit(1); console.log('All', ids.length, 'requirement rows flipped to Complete');"
    </automated>
  </verify>
  <done>
    REQUIREMENTS.md traceability shows all Phase 6 requirements (DNS-03..07, FORM-08, GDPR-03, PERF-02..09, LAUNCH-01..07) as Complete. ROADMAP.md Phase 6 row is `[x]` and Progress table is updated. STATE.md has a Milestone Close section with final gates, carry-forwards, artifact paths, and the SHIPPED marker. Single atomic commit lands all three files.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Owner laptop -> Wix dashboard | Owner credentials. Wix has no Claude-accessible API for archive or cancel. |
| Owner laptop -> Google Search Console | Google credentials. GSC has no Claude-accessible API for property add. |
| Build artifacts -> live HTML | All meta from 06-01 is now in production HTML; Task 5 verifies live HTML matches built HTML. |
| Local archive folders -> filesystem outside repo | Wix mirror + media ZIP + billing PDFs stored outside repo to avoid bloating git and accidentally committing private data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Information Disclosure | Wix archive contains owner business data + form submission CSV that may have PII | mitigate | Task 1 stores archive OUTSIDE the repo. Manifest only references local paths + byte sizes + SHA256 (no PII committed to git). Owner retains the archive on their own machine. |
| T-06-03-02 | Repudiation | Wix subscription cancel is irreversible at end of billing period; no Claude audit trail for the cancellation action | mitigate | Task 2 captures cancellation confirmation into wix-archive-manifest.md with timestamp + Wix's confirmation reference. Combined with Wix's own email confirmation (sent to owner), provides post-hoc traceability. |
| T-06-03-03 | Denial of Service | Cancelling Wix before verifying CF Pages live could brick the site | mitigate | Task 2 explicit pre-cancel safety check enumerates 4 conditions (apex 200 from CF, www 301, form roundtrip OK, archive complete) — ALL must be true. The 06-02 Task 7 cf-verify-after.txt is the canonical evidence; Task 2 references it. |
| T-06-03-04 | Tampering | GSC verification TXT removed prematurely could trigger re-verification | mitigate | Task 3 explicit instruction: leave the TXT in place after verification. GSC re-checks periodically. |
| T-06-03-05 | Information Disclosure | GSC verification TXT value committed to repo (gsc-verification.txt) | accept | TXT is a Google verification token, not auth. Public exposure has zero attack surface (an attacker cannot use the TXT to claim the property — claiming requires actually placing the TXT in DNS, which requires CF access). Safe to commit. Owner may optionally redact in the committed file. |
| T-06-03-06 | Spoofing | Lighthouse run against `https://klphotography.ie/` could be intercepted by a hostile MITM at runtime | accept | HSTS is now active. TLS validated by CF Universal SSL. Single point of trust = CF. Lighthouse runs from build host which uses system trust store. Risk identical to any browser session. |
| T-06-03-SC | Tampering | `lighthouse@^12` npx invocation pulls a fresh package version each run | mitigate | Lighthouse is published by Google Chrome team (`lighthouse` on npm — over 1.5M weekly downloads, official, ~10 yrs of provenance). Caret range allows minor + patch but pins to major 12. Acceptable supply-chain disposition. Not a new persistent dep (runs via npx, not added to package.json). |
</threat_model>

<verification>
Plan-level gates beyond the per-task `<verify>`:

1. RESEARCH §14 16-point checklist — every item PASS or PENDING(owner-confirm-within-24h). FAIL on any item blocks Task 6.
2. `git log .planning/phases/06-launch-cutover/` shows at minimum:
   - 06-01-PLAN.md, 06-02-PLAN.md, 06-03-PLAN.md commit (this planning commit)
   - 06-01-SUMMARY.md commit
   - 06-02-SUMMARY.md commit
   - Evidence file commits (cf-snapshot-before.txt, cf-verify-after.txt, resend-verify.txt, hsts-header.txt, analytics-beacon.html, wix-archive-manifest.md, gsc-verification.txt, post-launch-16-point.txt, lighthouse-mobile-prod.json)
   - Final milestone-close commit (06-03 Task 6)
3. Cross-reference integrity: each REQUIREMENTS.md row flipped to Complete in Task 6 has a corresponding evidence file in the phase directory. Audit by running `grep -E 'DNS-0[3-7]|FORM-08|GDPR-03|PERF-0[2-9]|LAUNCH-0[1-7]' .planning/REQUIREMENTS.md | grep Complete | wc -l` -> expect 22 rows.
4. `find .planning/phases/06-launch-cutover -type f -name '*.md' -o -name '*.txt' -o -name '*.json' -o -name '*.html'` enumerates all expected evidence files.
5. No regression: `npm run build && grep -q "Strict-Transport-Security" dist/` is N/A because HSTS is an edge header, not a build artifact. The live curl from Task 5 is the authoritative gate.
</verification>

<success_criteria>
- [ ] Wix HTML mirror + media + billing archived locally (outside repo); manifest committed (LAUNCH-03)
- [ ] Wix subscription cancelled (LAUNCH-06)
- [ ] GSC domain property verified + sitemap submitted (LAUNCH-07, PERF-05)
- [ ] Mobile Lighthouse on production: perf >= 90, LCP <= 2.5s, CLS <= 0.1, image-alt == 1 (PERF-02, PERF-03, PERF-04)
- [ ] 16-point verification: items 1-7, 9-11, 13, 16 PASS; items 8, 12, 14, 15 PASS or PENDING(24h) (LAUNCH-04 + cross-cutting)
- [ ] LAUNCH-01..07 + DNS-03..07 + FORM-08 + GDPR-03 + PERF-02..09 flipped to Complete in REQUIREMENTS.md
- [ ] ROADMAP.md Phase 6 row `[x]`, Progress table updated
- [ ] STATE.md Milestone Close section appended with final gates, carry-forwards, artifact paths
- [ ] All evidence committed in atomic logical commits
</success_criteria>

<output>
Create `.planning/phases/06-launch-cutover/06-03-SUMMARY.md` capturing:

- Wix archive: mirror size, media ZIP size, billing receipt count, archive path (local, outside repo)
- Wix cancellation timestamp + effective date
- GSC verification timestamp + sitemap submission status
- Mobile Lighthouse production results: perf, accessibility, SEO, best-practices, LCP, CLS, image-alt audit
- 16-point checklist final status: PASS count + PENDING count + FAIL count (should be 12+ PASS, 4 PENDING, 0 FAIL)
- Milestone close summary:
  - All Phase 6 requirements Complete
  - Project SHIPPED
- next-phase-readiness (Phase 6 is final — this section documents post-launch monitoring carry-forwards instead of a next phase):
  - HSTS max-age upgrade to 12 months after 90 days stable HTTPS (target: ~2026-08-18)
  - Resend SPF tighten to `-all` after 30 days stable deliverability (target: ~2026-06-17)
  - DMARC `p=reject` after 30 days stable signing (target: ~2026-06-17)
  - 16-point items 8/12/14/15 owner-confirm within 24h (target: ~2026-05-19)
  - CF Pages bandwidth + Resend send-count monitoring (weekly check)
  - v2 backlog: `.eu` redirect (RESEARCH §16), aggregateRating once real reviews exist (RESEARCH §5), HSTS preload after 12 months stable
- Risks closed and risks remaining:
  - Closed: P3 (DNS cutover breaks email) — never applied per CURRENT-ZONE.md; HSTS preload (deferred); SLOP-package risk (no new install in this plan)
  - Remaining: Wix re-activation grace window (if owner regrets cancel); CF free-tier limits (long-term)

This SUMMARY closes the GSD project. Commit it last as part of Task 6's atomic milestone-close commit.
</output>
</plan>
