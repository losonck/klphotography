---
phase: 06-launch-cutover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - astro.config.mjs
  - src/layouts/BaseLayout.astro
  - src/components/seo/LocalBusinessSchema.astro
  - src/components/seo/SeoHead.astro
  - src/pages/index.astro
  - src/pages/privacy.astro
  - public/og-card.jpg
  - public/apple-touch-icon.png
  - scripts/build-og-card.mjs
  - scripts/build-apple-touch-icon.mjs
autonomous: true
requirements:
  - PERF-02
  - PERF-03
  - PERF-04
  - PERF-05
  - PERF-06
  - PERF-07
  - PERF-08
  - PERF-09
tags:
  - astro
  - seo
  - sitemap
  - schema.org
  - opengraph
  - lighthouse

must_haves:
  truths:
    - "GET /sitemap-index.xml on a production build returns valid sitemap XML referencing /sitemap-0.xml"
    - "/sitemap-0.xml lists / and /privacy and does NOT list /styleguide"
    - "Built HTML for / and /privacy contains <link rel=\"canonical\"> with the absolute https://klphotography.ie URL for that page"
    - "Built HTML for / and /privacy contains og:title, og:description, og:image (https://klphotography.ie/og-card.jpg), og:url, og:type, og:image:width=1200, og:image:height=630"
    - "Built HTML for / and /privacy contains twitter:card=summary_large_image, twitter:title, twitter:description, twitter:image"
    - "Built HTML for / contains exactly one <script type=\"application/ld+json\"> with @type containing LocalBusiness and ProfessionalService"
    - "public/og-card.jpg exists at 1200x630 dimensions, JPEG, file size under 300 KB"
    - "public/apple-touch-icon.png exists at 180x180 dimensions, PNG"
    - "Built HTML for / and /privacy contains <link rel=\"apple-touch-icon\" sizes=\"180x180\" href=\"/apple-touch-icon.png\">"
    - "Lighthouse desktop performance on localhost preview of / is >= 90"
    - "Lighthouse desktop CLS on localhost preview of / is <= 0.1"
    - "Built HTML contains no Wix references (grep -i wix returns nothing in dist/)"
  artifacts:
    - path: "package.json"
      provides: "@astrojs/sitemap@^3.7.2 dependency"
      contains: "@astrojs/sitemap"
    - path: "astro.config.mjs"
      provides: "sitemap integration registered with filter excluding /styleguide"
      contains: "sitemap("
    - path: "src/components/seo/LocalBusinessSchema.astro"
      provides: "Inline JSON-LD LocalBusiness + ProfessionalService"
      min_lines: 20
    - path: "src/components/seo/SeoHead.astro"
      provides: "Per-page OG/Twitter/canonical meta component"
      min_lines: 20
    - path: "src/layouts/BaseLayout.astro"
      provides: "Wired SeoHead + LocalBusinessSchema + apple-touch-icon link"
      contains: "LocalBusinessSchema"
    - path: "public/og-card.jpg"
      provides: "1200x630 social share image"
    - path: "public/apple-touch-icon.png"
      provides: "180x180 iOS home screen icon"
    - path: "scripts/build-og-card.mjs"
      provides: "Reproducible OG card generation from hero source"
    - path: "scripts/build-apple-touch-icon.mjs"
      provides: "Reproducible apple-touch-icon generation from logo"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/seo/LocalBusinessSchema.astro"
      via: "default import + element in <head>"
      pattern: "import LocalBusinessSchema"
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/seo/SeoHead.astro"
      via: "default import + element in <head> receiving ogImage/canonicalUrl/description props"
      pattern: "import SeoHead"
    - from: "src/pages/index.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "passes title + description to BaseLayout; BaseLayout derives canonical from Astro.url.href + ogImage default"
      pattern: "<BaseLayout"
    - from: "astro.config.mjs"
      to: "@astrojs/sitemap"
      via: "integrations array with filter callback"
      pattern: "filter:"
    - from: "public/robots.txt"
      to: "https://klphotography.ie/sitemap-index.xml"
      via: "Sitemap directive (already present, must remain valid)"
      pattern: "Sitemap:"
---

<objective>
Ship every SEO and performance artifact required for launch BEFORE the DNS cutover. This plan is autonomous and can land on the existing klphotography.pages.dev preview without affecting the live Wix site.

Purpose: PERF-02..09 must all be true on the production build before Phase 06-02 swaps custom domain. Social previews, sitemap, structured data, favicons, and Lighthouse targets are observable on the preview URL — making 06-02 a pure DNS / dashboard operation with zero code surface.

Output:
- `@astrojs/sitemap@^3.7.2` wired into `astro.config.mjs` with `/styleguide` filter
- `src/components/seo/LocalBusinessSchema.astro` (JSON-LD per D-10)
- `src/components/seo/SeoHead.astro` (canonical + OG + Twitter card meta, per D-11)
- `src/layouts/BaseLayout.astro` wiring for both, plus apple-touch-icon link
- `public/og-card.jpg` (1200x630, hero crop, per D-02)
- `public/apple-touch-icon.png` (180x180, from logo, per D-03)
- Reproducible `scripts/build-og-card.mjs` and `scripts/build-apple-touch-icon.mjs` (sharp; already installed)
- Lighthouse run on localhost preview proving PERF-02 >= 90, CLS <= 0.1
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-launch-cutover/06-RESEARCH.md

# Source files
@src/layouts/BaseLayout.astro
@src/pages/index.astro
@src/pages/privacy.astro
@src/components/sections/Hero.astro
@astro.config.mjs
@package.json
@public/robots.txt

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. Use these directly — no extra exploration required. -->

From src/layouts/BaseLayout.astro (current Props):
```typescript
interface Props {
  title: string;
  description?: string;
}
```
The slot `head` is present and is consumed by index.astro for HeroPreload + Turnstile. Preserve slot ordering: `<Font />` preloads must appear BEFORE `<slot name="head" />` and BEFORE `<title>`. The CF analytics beacon at end-of-body is Plan 05-03 and MUST stay intact (conditional on PUBLIC_CF_ANALYTICS_TOKEN).

From astro.config.mjs (current shape):
```javascript
import { defineConfig, fontProviders } from 'astro/config';
export default defineConfig({
  site: 'https://klphotography.ie',
  output: 'static',
  image: { service: { entrypoint: 'astro/assets/services/sharp' } },
  fonts: [ /* EB Garamond + Inter — keep intact */ ],
});
```

From src/components/sections/Hero.astro:
```typescript
// Hero source: import hero from '@/assets/placeholder/hero.jpg';
// Hero is 2048x1365 JPEG (per RESEARCH §9). 1200x630 crop (fit: cover) fits cleanly.
```

From package.json:
```json
"dependencies": {
  "astro": "^6.3.3",
  "sharp": "^0.34.0",         // already installed — use for OG/apple-icon generation
  // add: "@astrojs/sitemap": "^3.7.2"
}
```

From public/robots.txt (already correct, must NOT change):
```
User-agent: *
Allow: /
Disallow: /styleguide

Sitemap: https://klphotography.ie/sitemap-index.xml
```

Sitemap integration output filenames (per @astrojs/sitemap docs):
- dist/sitemap-index.xml  (the index)
- dist/sitemap-0.xml      (the URL list)
The existing robots.txt Sitemap line matches dist/sitemap-index.xml exactly. Do not touch robots.txt.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install sitemap integration + build OG card + apple-touch-icon assets</name>
  <files>
    package.json,
    package-lock.json,
    astro.config.mjs,
    scripts/build-og-card.mjs,
    scripts/build-apple-touch-icon.mjs,
    public/og-card.jpg,
    public/apple-touch-icon.png
  </files>
  <action>
Install `@astrojs/sitemap@^3.7.2` and wire it into astro.config.mjs. Generate the two static image assets via reproducible sharp scripts so future regenerations are deterministic.

Step A — install package (per D-09; D-09 limits new npm dependencies to this one package):
- Run `npm install --save @astrojs/sitemap@^3.7.2` (lockfile changes are expected).
- Confirm `package.json` lists `"@astrojs/sitemap": "^3.7.2"` under `dependencies`.

Step B — astro.config.mjs:
- Import `sitemap from '@astrojs/sitemap'`.
- Add to `integrations: [...]` an `sitemap({ filter: (page) => !page.includes('/styleguide') })` call.
- Do NOT change `site`, `output`, `image`, or `fonts`. The `site: 'https://klphotography.ie'` value is required for sitemap URL generation and is already present.
- Type check via `npx astro check` after the edit.

Step C — `scripts/build-og-card.mjs`:
- Node ESM script using `sharp` (already a dep, no install).
- Read `src/assets/placeholder/hero.jpg`.
- Resize with `fit: 'cover'` to exactly 1200x630.
- Encode as JPEG quality 82 (target <300 KB per D-02 + RESEARCH §6 budget).
- Write `public/og-card.jpg`.
- Log final file size in bytes.
- Add an `npm run` script entry `"build:og-card": "node scripts/build-og-card.mjs"` to package.json so regeneration is reproducible.

Step D — `scripts/build-apple-touch-icon.mjs`:
- Node ESM script using `sharp`.
- Read `src/assets/brand/logo.jpg` (verified to exist; 692x693 per RESEARCH §7).
- Resize with `fit: 'cover'` to 180x180.
- Encode as PNG (Apple iOS does not accept JPEG for apple-touch-icon reliably).
- Write `public/apple-touch-icon.png`.
- Add npm script `"build:apple-touch-icon": "node scripts/build-apple-touch-icon.mjs"` to package.json.

Step E — generate the assets:
- Run `node scripts/build-og-card.mjs`.
- Run `node scripts/build-apple-touch-icon.mjs`.
- Both files MUST appear in `public/` and be committed (per D-03 — "pre-generate + commit" is the chosen path).

Notes per locked decisions:
- D-02: Static `public/og-card.jpg` — do NOT use Astro `getImage()`. Stable URL required for social cache.
- D-03: Keep existing favicon.svg; ADD apple-touch-icon.png. ICO not required (modern browsers accept SVG; RESEARCH §7 confirms minimum-viable set).
- D-09: `@astrojs/sitemap@^3.7.2` is the ONLY new npm dep this phase.

No XML entity escapes anywhere — write `&&` not `&amp;&amp;`, `<` not `&lt;`.
  </action>
  <verify>
    <automated>
node -e "const pkg = require('./package.json'); if (!pkg.dependencies['@astrojs/sitemap']) { console.error('FAIL: @astrojs/sitemap not in dependencies'); process.exit(1); } console.log('OK:', pkg.dependencies['@astrojs/sitemap']);" && node -e "const fs = require('fs'); const s = fs.statSync('public/og-card.jpg'); console.log('og-card.jpg bytes:', s.size); if (s.size > 307200) { console.error('FAIL: og-card.jpg over 300 KB'); process.exit(1); }" && node -e "const sharp = require('sharp'); sharp('public/og-card.jpg').metadata().then(m => { if (m.width !== 1200 || m.height !== 630) { console.error('FAIL: og-card dims', m.width, m.height); process.exit(1); } console.log('OK og-card', m.width + 'x' + m.height); });" && node -e "const sharp = require('sharp'); sharp('public/apple-touch-icon.png').metadata().then(m => { if (m.width !== 180 || m.height !== 180) { console.error('FAIL: apple-touch dims', m.width, m.height); process.exit(1); } console.log('OK apple-touch', m.width + 'x' + m.height, m.format); });" && node -e "const c = require('fs').readFileSync('astro.config.mjs','utf8'); if (!/from\s+'@astrojs\/sitemap'/.test(c)) { console.error('FAIL: sitemap import missing'); process.exit(1); } if (!/sitemap\(\{[\s\S]*filter[\s\S]*styleguide/.test(c)) { console.error('FAIL: filter excluding /styleguide missing'); process.exit(1); } console.log('OK: sitemap integration wired with filter');"
    </automated>
  </verify>
  <done>
    `npm install` succeeded; `@astrojs/sitemap@^3.7.2` in package.json dependencies; `astro.config.mjs` imports and uses sitemap with a `filter` that excludes `/styleguide`; `public/og-card.jpg` is 1200x630 JPEG under 300 KB; `public/apple-touch-icon.png` is 180x180 PNG; both reproducible scripts live under `scripts/` with corresponding npm run entries.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire SEO head + JSON-LD schema into BaseLayout (per-page meta + sitewide schema)</name>
  <files>
    src/components/seo/LocalBusinessSchema.astro,
    src/components/seo/SeoHead.astro,
    src/layouts/BaseLayout.astro,
    src/pages/index.astro,
    src/pages/privacy.astro
  </files>
  <behavior>
    - Test 1: Built `dist/index.html` contains exactly one `<script type="application/ld+json">` with `"@type":["LocalBusiness","ProfessionalService"]`.
    - Test 2: Built `dist/index.html` contains `<link rel="canonical" href="https://klphotography.ie/">`.
    - Test 3: Built `dist/index.html` contains `og:image` with content `https://klphotography.ie/og-card.jpg`, plus `og:image:width=1200`, `og:image:height=630`.
    - Test 4: Built `dist/index.html` contains `twitter:card` with content `summary_large_image`.
    - Test 5: Built `dist/privacy/index.html` contains `<link rel="canonical" href="https://klphotography.ie/privacy/">` (or `/privacy` — match Astro's output, but it MUST be absolute and HTTPS and apex).
    - Test 6: Built `dist/index.html` contains `<link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">`.
    - Test 7: CF analytics beacon block at end-of-body is preserved unchanged (still conditional on `cfAnalyticsToken`).
    - Test 8: Existing `<Font />` preload elements still appear in `<head>` BEFORE the new SeoHead component (font preload critical-path order preserved per existing BaseLayout comment).
  </behavior>
  <action>
Create two new Astro components and wire them into BaseLayout. Pass the per-page OG props from page files.

Step A — `src/components/seo/LocalBusinessSchema.astro` (per D-10):
- No props. All values are constants for this site.
- Single `<script type="application/ld+json" is:inline>` block.
- Content: JSON object with `@context: "https://schema.org"`, `@type: ["LocalBusiness", "ProfessionalService"]`, plus `name`, `description`, `url`, `logo` (https://klphotography.ie/og-card.jpg), `image` (same), `telephone: "+353851665472"`, `priceRange: "€€€"`, `address` (PostalAddress, Dublin, D13, IE), `areaServed` (Country, Ireland), `sameAs` (Instagram + Facebook URLs from D-10), `knowsAbout: "Wedding Photography"`.
- DO NOT include `aggregateRating` (RESEARCH §5 — no fake review data; risk of manual action from Google).
- `is:inline` is required so Vite leaves JSON content alone (it would otherwise treat it as JS).

Step B — `src/components/seo/SeoHead.astro` (per D-11):
- Props:
  - `title: string` (used for `og:title` + `twitter:title`)
  - `description: string` (used for both og:description and twitter:description; D-11 says OG description = current meta description)
  - `ogImage?: string` (defaults to `https://klphotography.ie/og-card.jpg`)
  - `canonicalUrl?: string` (defaults to `Astro.url.href`)
- Output in this exact order (all literal `<` and `>`, no HTML entity escapes in source):
  - `<link rel="canonical" href={canonicalUrl}>`
  - Open Graph: `og:type=website`, `og:site_name="KL Photography"`, `og:title`, `og:description`, `og:image`, `og:image:width=1200`, `og:image:height=630`, `og:image:alt="KL Photography — Wedding Photographer, Dublin, Ireland"`, `og:url=canonicalUrl`
  - Twitter: `twitter:card=summary_large_image`, `twitter:title`, `twitter:description`, `twitter:image`, `twitter:image:alt` (same alt)
  - Favicon set: keep existing `favicon.svg` link in BaseLayout; ADD here `<link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">`
- All meta values are interpolated via Astro expressions, not concatenated strings.

Step C — `src/layouts/BaseLayout.astro` edits (DO NOT regress the conditional CF analytics beacon from Plan 05-03):
- Extend `Props` interface to accept optional `ogImage?: string` and `canonicalUrl?: string` (description already exists).
- Import `SeoHead` and `LocalBusinessSchema`.
- In `<head>`, AFTER the `<Font />` preloads and BEFORE the existing `<slot name="head" />` (so per-page additions can override or augment if ever needed):
  - Render `<SeoHead title={title} description={description} ogImage={ogImage} canonicalUrl={canonicalUrl} />`
  - Render `<LocalBusinessSchema />`
- The existing favicon.svg link, viewport, generator, font preloads, and slot stay intact.
- The trailing `<title>` and `<meta name="description">` stay intact (browsers tolerate duplicates from og:title — but title MUST remain for `<title>` element).
- End-of-body CF analytics beacon block stays unchanged (verify by diff that the conditional block is byte-identical to current).

Step D — `src/pages/index.astro` (D-11 says title and OG description already match):
- Keep existing title and description props.
- Add NO extra OG props — BaseLayout default for ogImage and canonical handle this page correctly.

Step E — `src/pages/privacy.astro`:
- Keep existing title and description props.
- Per D-12 privacy IS in the sitemap (already handled by sitemap filter — only `/styleguide` excluded).
- No additional change needed; SeoHead default canonical (`Astro.url.href`) will produce `https://klphotography.ie/privacy/` (or `/privacy` — both acceptable per Astro `trailingSlash` behavior).

Constraints:
- `<script type="application/ld+json">` content must parse as valid JSON. Use single-line or pretty-printed JSON but no JS comments inside the script.
- No XML entity leaks inside `<verify><automated>` blocks below — literal `&&`, `<`, `>`, `"`.
  </behavior>
  <verify>
    <automated>
npm run build && node -e "const fs = require('fs'); const html = fs.readFileSync('dist/index.html','utf8'); const checks = [['canonical','<link rel=\"canonical\" href=\"https://klphotography.ie/\"'],['ogtype','property=\"og:type\" content=\"website\"'],['ogimage','property=\"og:image\" content=\"https://klphotography.ie/og-card.jpg\"'],['ogw','property=\"og:image:width\" content=\"1200\"'],['ogh','property=\"og:image:height\" content=\"630\"'],['twcard','name=\"twitter:card\" content=\"summary_large_image\"'],['apple','rel=\"apple-touch-icon\" sizes=\"180x180\" href=\"/apple-touch-icon.png\"'],['jsonld','application/ld+json'],['locbiz','LocalBusiness'],['profsvc','ProfessionalService']]; let fail = 0; for (const [k,v] of checks) { if (!html.includes(v)) { console.error('FAIL', k, '— missing:', v); fail++; } else { console.log('OK', k); } } if (fail) process.exit(1);" && node -e "const fs = require('fs'); const html = fs.readFileSync('dist/privacy/index.html','utf8'); if (!/<link rel=\"canonical\" href=\"https:\/\/klphotography\.ie\/privacy\/?\"/.test(html)) { console.error('FAIL: privacy canonical'); process.exit(1); } if (!html.includes('property=\"og:image\" content=\"https://klphotography.ie/og-card.jpg\"')) { console.error('FAIL: privacy og:image inherits default'); process.exit(1); } console.log('OK privacy canonical + og:image inherited');" && node -e "const fs = require('fs'); const html = fs.readFileSync('dist/index.html','utf8'); const m = html.match(/<script type=\"application\/ld\+json\"[^>]*>([\s\S]*?)<\/script>/); if (!m) { console.error('FAIL: no JSON-LD script'); process.exit(1); } try { const j = JSON.parse(m[1]); const types = Array.isArray(j['@type']) ? j['@type'] : [j['@type']]; if (!types.includes('LocalBusiness') || !types.includes('ProfessionalService')) { console.error('FAIL: missing @type entries'); process.exit(1); } console.log('OK JSON-LD parses; types=', types.join(',')); } catch(e) { console.error('FAIL: JSON-LD invalid', e.message); process.exit(1); }"
    </automated>
  </verify>
  <done>
    Two new SEO components exist; BaseLayout wires both without regressing existing structure; built `dist/index.html` and `dist/privacy/index.html` carry canonical, OG (with stable og-card.jpg URL), Twitter card, and apple-touch-icon link; built `dist/index.html` contains a single valid JSON-LD block with `LocalBusiness` + `ProfessionalService` types and the constants from D-10; CF analytics beacon block in BaseLayout unchanged byte-for-byte.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify sitemap output + run Lighthouse + grep for Wix refs</name>
  <files>
    dist/sitemap-index.xml,
    dist/sitemap-0.xml
  </files>
  <action>
This task does NOT modify source code. It runs the production build (already produced by Task 2's `npm run build`, but re-run idempotently here for cleanliness) and validates the four outstanding must-haves: sitemap output, no Wix refs, Lighthouse performance, Lighthouse CLS.

Step A — build:
- `npm run build` produces `dist/` (re-running is idempotent).

Step B — sitemap content check:
- `dist/sitemap-index.xml` MUST exist and reference `/sitemap-0.xml`.
- `dist/sitemap-0.xml` MUST contain `<loc>https://klphotography.ie/</loc>` and `<loc>https://klphotography.ie/privacy/</loc>` (or `/privacy` without trailing slash — match Astro output).
- `dist/sitemap-0.xml` MUST NOT contain `styleguide`.

Step C — no Wix refs:
- Grep the entire `dist/` tree for case-insensitive `wix`. Expect zero matches in HTML/CSS/JS.
- Asset binaries (PNG/JPG/AVIF/WEBP) are excluded from the grep by extension filter.

Step D — Lighthouse desktop on localhost preview:
- Start `npm run preview` in background (Astro preview serves `dist/`).
- Wait for the preview URL (default `http://localhost:4321`) to respond.
- Run: `npx -y lighthouse@^12 http://localhost:4321/ --only-categories=performance,accessibility,seo --form-factor=desktop --chrome-flags="--headless=new --no-sandbox" --output=json --output-path=.planning/phases/06-launch-cutover/lighthouse-06-01.json --quiet`
- Parse the JSON output, extract:
  - `categories.performance.score * 100` — MUST be >= 90 (PERF-02)
  - `audits.cumulative-layout-shift.numericValue` — MUST be <= 0.1 (PERF-04)
  - `audits.largest-contentful-paint.numericValue` — record (PERF-03 target <= 2500ms; may pass only on real CDN — record value, flag if > 2500 but do not fail localhost run since RESEARCH §9 notes localhost LCP ~4.1s pre-CDN)
- If perf < 90 OR CLS > 0.1, exit non-zero and surface the failing audit names.

Note on PERF-03 (LCP <= 2.5s): per RESEARCH §9, real-CDN LCP is expected to pass once on CF Pages edge — localhost cannot reproduce that. This task records the localhost LCP and notes it; the definitive PERF-03 verification runs in Plan 06-03 post-cutover from a live network.

Step E — kill preview:
- Terminate the backgrounded `astro preview` process so this task does not leak a server.

Notes:
- DO NOT run `npm install` again in this task — Task 1 already did that.
- DO NOT modify source — only verify produced artifacts.
- Lighthouse JSON output is committed to `.planning/phases/06-launch-cutover/lighthouse-06-01.json` as evidence for the SUMMARY.
  </action>
  <verify>
    <automated>
test -f dist/sitemap-index.xml && test -f dist/sitemap-0.xml && node -e "const fs = require('fs'); const idx = fs.readFileSync('dist/sitemap-index.xml','utf8'); if (!idx.includes('sitemap-0.xml')) { console.error('FAIL: sitemap-index does not reference sitemap-0.xml'); process.exit(1); } const list = fs.readFileSync('dist/sitemap-0.xml','utf8'); if (!list.includes('https://klphotography.ie/')) { console.error('FAIL: sitemap-0 missing root URL'); process.exit(1); } if (!/https:\/\/klphotography\.ie\/privacy/.test(list)) { console.error('FAIL: sitemap-0 missing /privacy'); process.exit(1); } if (/styleguide/i.test(list)) { console.error('FAIL: sitemap-0 contains styleguide'); process.exit(1); } console.log('OK sitemap content');" && node -e "const cp = require('child_process'); const out = cp.execSync('grep -ril wix dist --include=*.html --include=*.css --include=*.js --include=*.xml --include=*.txt || true').toString().trim(); if (out) { console.error('FAIL: Wix references in dist:'); console.error(out); process.exit(1); } console.log('OK no Wix refs in dist');" && node -e "const fs = require('fs'); const r = JSON.parse(fs.readFileSync('.planning/phases/06-launch-cutover/lighthouse-06-01.json','utf8')); const perf = Math.round(r.categories.performance.score * 100); const cls = r.audits['cumulative-layout-shift'].numericValue; const lcp = r.audits['largest-contentful-paint'].numericValue; console.log('Lighthouse perf:', perf, 'CLS:', cls, 'LCP(ms):', lcp); let fail = 0; if (perf < 90) { console.error('FAIL: perf', perf, '< 90'); fail = 1; } if (cls > 0.1) { console.error('FAIL: CLS', cls, '> 0.1'); fail = 1; } if (lcp > 2500) { console.warn('NOTE: localhost LCP', lcp, 'ms > 2500 — expected on localhost; real-CDN re-verified in 06-03'); } if (fail) process.exit(1);"
    </automated>
  </verify>
  <done>
    `dist/sitemap-index.xml` and `dist/sitemap-0.xml` exist with correct contents (root + /privacy, no /styleguide); zero `wix` references in `dist/` HTML/CSS/JS/XML/TXT; Lighthouse desktop performance >= 90; CLS <= 0.1; LCP recorded (real-CDN verification deferred to 06-03 per RESEARCH §9); Lighthouse JSON report committed under `.planning/phases/06-launch-cutover/`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser <- CDN (static HTML) | Built HTML reaches social crawlers (Facebook, Twitter, Google) and end-user browsers. JSON-LD content is parsed by Google Rich Results. |
| build host <- npm | `@astrojs/sitemap@^3.7.2` install introduces supply-chain surface. |
| build host <- image source files | hero.jpg and logo.jpg are local repo files. No external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | npm dependency `@astrojs/sitemap` | mitigate | First-party Astro monorepo package (RESEARCH §Package Legitimacy Audit: VERIFIED official Astro publisher, 500K+ weekly downloads, ~4 yr age). Pinned via `^3.7.2`. lockfile committed. |
| T-06-01-02 | Information Disclosure | JSON-LD body contains telephone, email-equivalent (handled), address (Dublin D13 — neighborhood level only, no street) | accept | All values are intentionally public per CONTENT-05 (phone, email, social) and D-10 (LocalBusiness address). Photographer's business contact is the same data already in `Contact` section + `/privacy` page. No incremental disclosure. |
| T-06-01-03 | Tampering | JSON-LD script injection vector | mitigate | `is:inline` prevents Vite processing. Content is hand-written JSON constants (no user input, no template injection). Single static string. |
| T-06-01-04 | Spoofing | OG image URL stability (D-02 rationale) | mitigate | Static `public/og-card.jpg` at stable URL — social crawlers cache the URL. `getImage()` (hash-suffixed) explicitly rejected per D-02 to prevent cache breakage on rebuild. |
| T-06-01-SC | Tampering | npm install of @astrojs/sitemap | mitigate | Package legitimacy verified in RESEARCH §Package Legitimacy Audit (VERIFIED, first-party). No blocking-human checkpoint required since legitimacy is provable from withastro/astro monorepo membership. |
</threat_model>

<verification>
Plan-level gates (in addition to per-task `<verify>`):

1. `npx astro check` — TypeScript types pass on the modified Astro project.
2. `npm run build` — clean production build, no warnings escalated to errors.
3. Manual visual smoke (NOT a checkpoint — quick local browser load of `npm run preview` to confirm hero renders, contact section visible, no console errors). This is not required by acceptance, but executor should do it.
4. `git diff public/robots.txt` returns empty — robots.txt unchanged (it was already correct per RESEARCH §4).
5. `git diff` on Plan 05-03 CF analytics beacon block (`{cfAnalyticsToken && ...}` in BaseLayout.astro) shows zero changes — beacon must remain conditional and intact for 06-02 to wire the token.
</verification>

<success_criteria>
- [ ] `@astrojs/sitemap@^3.7.2` installed and wired with `/styleguide` filter (PERF-05, PERF-06)
- [ ] `public/og-card.jpg` 1200x630 JPEG < 300 KB at stable URL (PERF-07)
- [ ] `public/apple-touch-icon.png` 180x180 PNG (D-03)
- [ ] `LocalBusinessSchema.astro` emits valid JSON-LD per D-10 (PERF-08)
- [ ] `SeoHead.astro` emits canonical + OG + Twitter card meta per D-11 (PERF-07, PERF-09)
- [ ] `BaseLayout.astro` wires both components, preserves Font preload order, preserves Plan 05-03 CF analytics beacon (GDPR-03 carry-over untouched)
- [ ] Built `dist/index.html` and `dist/privacy/index.html` carry all expected meta (validated by Task 2 grep gate)
- [ ] `dist/sitemap-index.xml` + `dist/sitemap-0.xml` contain `/` and `/privacy` but not `/styleguide` (PERF-05)
- [ ] Lighthouse desktop perf >= 90 and CLS <= 0.1 on `npm run preview` (PERF-02, PERF-04)
- [ ] Zero `wix` references in built `dist/` HTML/CSS/JS/XML/TXT
- [ ] All changes committed in atomic logical commits
</success_criteria>

<output>
Create `.planning/phases/06-launch-cutover/06-01-SUMMARY.md` capturing:

- Artifacts created (component file paths, public asset paths, scripts)
- `@astrojs/sitemap@^3.7.2` install confirmation + lockfile hash delta if relevant
- og-card.jpg final byte size and dimensions
- apple-touch-icon.png dimensions
- Lighthouse desktop run results (perf, accessibility, SEO, CLS, LCP — note any localhost-only LCP caveat per RESEARCH §9)
- Sitemap URL count
- next-phase-readiness:
  - "06-02 owner-driven dashboard work unblocked. All code-side launch readiness is shipped. CF Pages preview at klphotography.pages.dev now carries the production-grade HTML that will appear at klphotography.ie once the custom domain is added in 06-02."
  - Carry-forward observations for 06-02: confirm beacon activates after PUBLIC_CF_ANALYTICS_TOKEN is set; confirm canonical resolves to apex after DNS swap; confirm sitemap-index.xml accessible at the real domain.
- Carry-forward observations for 06-03: localhost LCP value (post-CDN re-verification expected to be lower per RESEARCH §9); Lighthouse mobile run deferred to live production URL.
</output>
</plan>
