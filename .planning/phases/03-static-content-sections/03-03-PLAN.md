---
phase: 03-static-content-sections
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - src/components/ui/Section.astro
  - src/components/ui/Nav.astro
  - src/layouts/BaseLayout.astro
  - src/styles/global.css
autonomous: true
requirements: [CONTENT-06]
tags: [astro, astro-6, tailwind-v4, sticky-nav, intersection-observer, hamburger, mobile-menu, smooth-scroll, scroll-margin-top, accessibility, prefers-reduced-motion]

must_haves:
  truths:
    - "User opens `/` in `npm run preview`, scrolls past the Hero, and the Nav stays pinned to the viewport top (CSS `position: sticky top-0 z-50`) without any layout shift (Nav has fixed intrinsic height; no JS-driven insertion)"
    - "Before any scroll, Nav background renders as `bg-cream/40 backdrop-blur-sm` (subtle frost — per RESEARCH §6 contrast safeguard); after the page scrolls past the 1px sentinel, Nav background flips to `bg-cream/90 backdrop-blur-sm` with a bottom border (`border-rule`) via the `data-scrolled=\"true\"` attribute switch (per D-11)"
    - "Clicking any in-page anchor link (Nav link or Hero CTA `Enquire` → `#contact`) smoothly scrolls to the target section AND lands the section's heading clear of the sticky nav — i.e., `scroll-margin-top` baseline of `scroll-mt-20 lg:scroll-mt-24` (5rem mobile / 6rem desktop, matching Nav intrinsic height) is applied via `Section.astro` (per D-08)"
    - "Smooth-scroll behaviour is purely CSS (`html { scroll-behavior: smooth }`) — already shipped by Phase 2 — and is automatically disabled for `prefers-reduced-motion: reduce` users (also from Phase 2 global.css); this plan does NOT add JS-based scrollIntoView"
    - "On a viewport ≤640px (i.e., below the `sm:` breakpoint), the desktop nav `<ul>` is hidden and a `<button aria-expanded aria-controls aria-label>` hamburger toggle appears; tapping it toggles `aria-expanded` between `\"false\"` and `\"true\"` and reveals/hides a mobile menu `<ul>` (per D-07; per RESEARCH §5 NOT `<details>/<summary>` — documented a11y regression with VoiceOver)"
    - "Hamburger menu closes on: (a) clicking any link inside it, (b) pressing Escape (focus returns to the toggle button per WCAG 2.1.2), (c) clicking outside the menu, (d) viewport resize crossing the 640px threshold (so user rotating phone or resizing browser doesn't leave an orphan menu)"
    - "Hamburger toggle is ≥44×44px (`h-11 w-11`) per WCAG 2.5.5 target-size; visible focus ring matches the Phase 2 Button bronze focus-visible pattern"
    - "The IntersectionObserver target is a 1px-tall absolutely-positioned `<div data-nav-sentinel>` placed as the FIRST child of `<body>` inside `BaseLayout.astro` — does not push layout, contributes zero CLS"
    - "Mobile menu link tap targets are ≥44px tall (`block px-6 py-4 text-base` → ~48px) per WCAG 2.5.5"
    - "`npm run check` exits 0; `npm run build` exits 0; the IntersectionObserver `<script>` is bundled into `dist/_astro/*.js` (NOT inlined in HTML) and a `<script type=\"module\" src=\"/_astro/...\">` tag appears in `dist/index.html`"
  artifacts:
    - path: "src/components/ui/Section.astro"
      provides: "Section primitive WITH `scroll-mt-20 lg:scroll-mt-24` baseline so anchor jumps land clear of sticky nav (per D-08)"
      contains: "scroll-mt-20"
    - path: "src/components/ui/Nav.astro"
      provides: "Sticky nav with IntersectionObserver-driven `data-scrolled` attribute swap AND mobile hamburger (`<button aria-expanded>` + 25-line vanilla JS with Escape / click-outside / resize handling); REPLACES Phase 2 static Nav in place per D-11 + D-07"
      contains: "position: sticky"
    - path: "src/layouts/BaseLayout.astro"
      provides: "First-child `<div data-nav-sentinel>` inside `<body>` — IntersectionObserver target for the sticky-nav scrolled-state flip"
      contains: "data-nav-sentinel"
    - path: "src/styles/global.css"
      provides: "(No new rules — Phase 2 already ships `html { scroll-behavior: smooth }` + the reduced-motion override; this plan VERIFIES that hand-off and adds NO new CSS unless a defect surfaces)"
      contains: "scroll-behavior: smooth"
  key_links:
    - from: "src/components/ui/Nav.astro"
      to: "[data-nav-sentinel] in BaseLayout"
      via: "IntersectionObserver.observe(sentinel)"
      pattern: "data-nav-sentinel"
    - from: "src/components/ui/Section.astro"
      to: "scroll target landing offset"
      via: "scroll-mt-20 lg:scroll-mt-24 Tailwind utilities"
      pattern: "scroll-mt-"
    - from: "src/components/ui/Nav.astro"
      to: "hamburger toggle ↔ mobile menu"
      via: "aria-expanded + aria-controls + JS state sync"
      pattern: "aria-expanded"
---

<objective>
Add the **single interactive layer** that completes Phase 3: make the Nav sticky with a scroll-aware background, add the mobile hamburger menu with full a11y handling (Escape, click-outside, resize, focus return), and add the universal `scroll-margin-top` to `Section.astro` so smooth-scroll anchor jumps don't tuck section headings behind the sticky nav.

Purpose: deliver CONTENT-06 (sticky nav + smooth-scroll anchors + mobile hamburger). All three behaviours are interaction layers on top of the static section assembly that 03-01 + 03-02 already shipped — this plan touches `Nav.astro`, `Section.astro`, and `BaseLayout.astro` in place plus optionally verifies (no edit needed) the smooth-scroll CSS in `global.css`.

Output: extended Nav.astro with `position: sticky` + IntersectionObserver-driven `data-scrolled` swap + `<button aria-expanded>` hamburger + 25-line vanilla JS bundle; Section.astro with `scroll-mt-20 lg:scroll-mt-24` baseline; BaseLayout.astro with a 1px nav sentinel as first body child; global.css verified unchanged (or one-line adjustment if defect surfaces during verification).

This is the final Phase 3 plan. After this, every CONTENT-01..07 requirement ships in built HTML + interactive behaviours pass the manual mobile-device smoke test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-static-content-sections/03-RESEARCH.md
@.planning/phases/02-design-system/02-02-SUMMARY.md
@src/components/ui/Nav.astro
@src/components/ui/Section.astro
@src/components/ui/Button.astro
@src/components/ui/Footer.astro
@src/layouts/BaseLayout.astro
@src/styles/global.css
@src/pages/index.astro
@src/pages/styleguide.astro
@src/pages/privacy.astro

<interfaces>
<!-- Current Nav.astro shape (from Phase 2 — what THIS plan extends in place): -->

```typescript
// src/components/ui/Nav.astro — Phase 2 static
interface Props extends HTMLAttributes<'nav'> {
  brand?: string;  // default: 'KL Photography'
}
// Renders: <nav class="bg-cream border-b border-rule" aria-label="Primary">
//            <div class="mx-auto max-w-6xl px-6 sm:px-8 py-4 flex items-center justify-between">
//              <a href="/" class="font-serif text-xl text-ink">{brand}</a>
//              <ul class="hidden sm:flex gap-8">
//                {links.map((l) => <li><a href={l.href} class="...hover:text-bronze">{l.label}</a></li>)}
//              </ul>
//            </div>
//          </nav>
// const links = [
//   { href: '#about', label: 'About' },
//   { href: '#pricing', label: 'Pricing' },
//   { href: '#testimonials', label: 'Testimonials' },
//   { href: '#contact', label: 'Contact' },
// ];
```

<!-- Current Section.astro shape (Phase 2 — touched by this plan to add scroll-mt-*): -->

```typescript
interface Props extends HTMLAttributes<'section'> {
  id: string;                          // REQUIRED
  tone?: 'cream' | 'cream-deep';       // default: 'cream'
}
// Current render:
// <section id={id} class:list={[toneClass, 'py-16 sm:py-24 lg:py-32', className]} {...rest}>
//   <div class="mx-auto max-w-6xl px-6 sm:px-8"><slot /></div>
// </section>
// This plan adds 'scroll-mt-20 lg:scroll-mt-24' to the class:list (after py-* tokens, before className).
```

<!-- Current BaseLayout.astro (Phase 2 — this plan inserts <div data-nav-sentinel> as first body child): -->

```html
<body class="bg-cream text-ink font-sans antialiased">
  <slot />
</body>
<!-- becomes -->
<body class="bg-cream text-ink font-sans antialiased">
  <!-- IntersectionObserver target for sticky-nav scrolled-state (per Plan 03-03 + RESEARCH §6). 1px tall, absolutely positioned, contributes zero layout. -->
  <div data-nav-sentinel aria-hidden="true" style="position: absolute; top: 0; height: 1px; width: 100%; pointer-events: none;"></div>
  <slot />
</body>
```

<!-- Smooth-scroll CSS (already shipped in src/styles/global.css by Phase 2): -->

```css
/* Reduced-motion blanket rule (DESIGN-05, RESEARCH §7) */
html { scroll-behavior: smooth; }
@media (prefers-reduced-motion: reduce) {
  html { scroll-behavior: auto; }
  *, ::before, ::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
}
```

<!-- This plan ADDS NOTHING new to global.css unless verification finds a defect.
     The CSS smooth-scroll behaviour + reduced-motion override are already in place. -->

Path alias: `@/*` → `src/*`.
```
</interfaces>

<nav_template>
<!-- Full extended Nav.astro markup — executor uses this as the literal template per RESEARCH §5 + §6.
     Aspects to preserve verbatim: aria-* attributes, data-scrolled initial value, hamburger SVG paths,
     mobile menu structure, IntersectionObserver sentinel selector. -->

Frontmatter (unchanged Props + links arrays):

```
import type { HTMLAttributes } from 'astro/types';

interface Props extends HTMLAttributes<'nav'> {
  brand?: string;
}

const { brand = 'KL Photography', class: className, ...rest } = Astro.props;

// Per D-03: NO #portfolio link in Phase 3. Five links: Home (#hero) / About / Pricing / Testimonials / Contact.
// (Phase 4 plan will insert #portfolio when the real gallery ships.)
const links = [
  { href: '#hero', label: 'Home' },
  { href: '#about', label: 'About' },
  { href: '#pricing', label: 'Pricing' },
  { href: '#testimonials', label: 'Testimonials' },
  { href: '#contact', label: 'Contact' },
];
```

Render (replace the Phase 2 `<nav>` block in full):

```
<nav
  aria-label="Primary"
  data-scrolled="false"
  class:list={[
    'sticky top-0 z-50 w-full',
    'motion-safe:transition-colors',
    /* Initial state — subtle frost per RESEARCH §6 (D-11 + Pitfall 7): avoid pure transparent over arbitrary hero photo. */
    'bg-cream/40 backdrop-blur-sm border-b border-transparent',
    /* Scrolled state — flipped by IntersectionObserver via data-scrolled="true". */
    'data-[scrolled=true]:bg-cream/90 data-[scrolled=true]:border-rule',
    className,
  ]}
  {...rest}
>
  <div class="mx-auto max-w-6xl px-6 sm:px-8 py-4 flex items-center justify-between">
    <a href="/" class="font-serif text-xl text-ink">{brand}</a>

    {/* Desktop nav — visible sm: and up */}
    <ul class="hidden sm:flex gap-8">
      {links.map((l) => (
        <li>
          <a
            href={l.href}
            class="font-sans text-sm text-ink motion-safe:transition-colors hover:text-bronze focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-bronze"
          >{l.label}</a>
        </li>
      ))}
    </ul>

    {/* Mobile hamburger toggle — visible below sm: only */}
    <button
      id="nav-toggle"
      type="button"
      class="sm:hidden inline-flex items-center justify-center h-11 w-11 -mr-2 text-ink motion-safe:transition-colors hover:text-bronze focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-bronze"
      aria-expanded="false"
      aria-controls="nav-mobile-menu"
      aria-label="Toggle menu"
    >
      <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
        <path d="M3 6h18" />
        <path d="M3 12h18" />
        <path d="M3 18h18" />
      </svg>
    </button>
  </div>

  {/* Mobile menu — hidden until aria-expanded="true"; appears below nav bar without animation. */}
  <ul
    id="nav-mobile-menu"
    class="sm:hidden hidden flex-col gap-0 border-t border-rule bg-cream"
  >
    {links.map((l) => (
      <li>
        <a
          href={l.href}
          class="block px-6 py-4 text-base text-ink border-b border-rule motion-safe:transition-colors hover:text-bronze hover:bg-cream-deep focus-visible:outline-2 focus-visible:outline-offset-[-2px] focus-visible:outline-bronze"
        >{l.label}</a>
      </li>
    ))}
  </ul>
</nav>

<script>
  // 03-03 sticky-nav background flip (IntersectionObserver) + mobile hamburger toggle
  // (RESEARCH §5 + §6 — combined into one script block; Vite bundles into dist/_astro/*.js).

  // ── Sticky nav scrolled-state ────────────────────────────────────────────────
  const nav = document.querySelector('nav[aria-label="Primary"]');
  const sentinel = document.querySelector('[data-nav-sentinel]');
  if (nav && sentinel && 'IntersectionObserver' in window) {
    const obs = new IntersectionObserver(
      ([entry]) => {
        nav.setAttribute('data-scrolled', entry.isIntersecting ? 'false' : 'true');
      },
      { threshold: 0 }
    );
    obs.observe(sentinel);
  }

  // ── Mobile hamburger toggle ──────────────────────────────────────────────────
  const toggle = document.getElementById('nav-toggle');
  const menu = document.getElementById('nav-mobile-menu');
  if (toggle && menu) {
    const close = () => {
      toggle.setAttribute('aria-expanded', 'false');
      menu.classList.add('hidden');
    };
    const open = () => {
      toggle.setAttribute('aria-expanded', 'true');
      menu.classList.remove('hidden');
    };
    toggle.addEventListener('click', () => {
      toggle.getAttribute('aria-expanded') === 'true' ? close() : open();
    });
    menu.querySelectorAll('a').forEach((a) => a.addEventListener('click', close));
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape' && toggle.getAttribute('aria-expanded') === 'true') {
        close();
        toggle.focus();
      }
    });
    document.addEventListener('click', (e) => {
      if (toggle.getAttribute('aria-expanded') !== 'true') return;
      const t = e.target as Node;
      if (!menu.contains(t) && !toggle.contains(t)) close();
    });
    window.addEventListener('resize', () => {
      if (window.innerWidth >= 640 && toggle.getAttribute('aria-expanded') === 'true') close();
    });
  }
</script>
```

NOTE the `<script>` is Astro's component-scoped pattern. Vite hoists + bundles + emits the module tag automatically.
</nav_template>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add scroll-margin-top to Section + nav sentinel to BaseLayout (Wave 0 baseline)</name>
  <files>src/components/ui/Section.astro, src/layouts/BaseLayout.astro</files>
  <action>
**`src/components/ui/Section.astro`** — modify the `class:list` to add `scroll-mt-20 lg:scroll-mt-24` (5rem mobile / 6rem desktop) per D-08 + RESEARCH §4.

Current (Phase 2):
```
<section id={id} class:list={[toneClass, 'py-16 sm:py-24 lg:py-32', className]} {...rest}>
```

Change to:
```
<section
  id={id}
  class:list={[
    toneClass,
    'py-16 sm:py-24 lg:py-32',
    /* scroll-margin-top baseline so anchor jumps land clear of sticky Nav
       (Nav intrinsic height ≈ 64px mobile / 80px desktop; scroll-mt-20=5rem=80px, lg:scroll-mt-24=6rem=96px). Per D-08 + RESEARCH §4. */
    'scroll-mt-20 lg:scroll-mt-24',
    className,
  ]}
  {...rest}
>
```

Place the inline comment exactly where shown — it documents the magic numbers for future Nav-height adjustments.

This single edit propagates to every section consumed by 03-01 + 03-02 (Hero, PortfolioStub, About, Pricing, Testimonials, Contact) because they all wrap with `<Section id="...">`. Verification proves the class lands in built CSS / built HTML.

**`src/layouts/BaseLayout.astro`** — insert the 1px nav sentinel as the FIRST child of `<body>`, BEFORE `<slot />`, per RESEARCH §6.

Current body block:
```
<body class="bg-cream text-ink font-sans antialiased">
  <slot />
</body>
```

Change to:
```
<body class="bg-cream text-ink font-sans antialiased">
  {/* IntersectionObserver target for sticky-nav scrolled-state (Plan 03-03 + RESEARCH §6).
     1px tall, absolutely positioned, contributes zero layout / zero CLS. */}
  <div data-nav-sentinel aria-hidden="true" style="position: absolute; top: 0; height: 1px; width: 100%; pointer-events: none;"></div>
  <slot />
</body>
```

The sentinel MUST sit OUTSIDE any `<main>` or `<nav>` wrapper — it watches the top of the document. Astro will render the `style` attribute inline (acceptable for a 4-property declaration; not worth a CSS class for a one-off).

**Do NOT modify `src/styles/global.css`** unless `npm run build` reveals a regression. The smooth-scroll CSS + reduced-motion override are already shipped by Phase 2 (verified via `grep -q 'scroll-behavior: smooth' src/styles/global.css` from the existing file content). If verification surfaces an unexpected hand-off issue, document it under Deviations and patch in Task 3 with a minimal addition.

Run `npm run check` after editing. Run `npm run build` to confirm Tailwind v4 compiles `scroll-mt-20` and `lg:scroll-mt-24` utilities into `dist/_astro/*.css` (these are derived from `--spacing: 0.25rem` × 20 = 5rem, × 24 = 6rem; v4 generates them on-demand).
  </action>
  <verify>
    <automated>cd /c/projects/new-project && grep -q "scroll-mt-20" src/components/ui/Section.astro && grep -q "lg:scroll-mt-24" src/components/ui/Section.astro && grep -q "data-nav-sentinel" src/layouts/BaseLayout.astro && grep -q "scroll-behavior: smooth" src/styles/global.css && npm run check && npm run build && grep -q "data-nav-sentinel" dist/index.html && grep -q "data-nav-sentinel" dist/privacy/index.html && grep -rqE "scroll-margin-top: ?(5rem|80px)" dist/_astro/ && grep -rqE "(@media ?\(min-width: ?(64rem|1024px)\)).*scroll-margin-top: ?(6rem|96px)" dist/_astro/ 2>/dev/null || grep -rqE "scroll-margin-top: ?(6rem|96px)" dist/_astro/</automated>
  </verify>
  <done>Section.astro contains `scroll-mt-20` + `lg:scroll-mt-24`; BaseLayout.astro contains `data-nav-sentinel`; built HTML for both `/` and `/privacy` carries the sentinel `<div>`; compiled CSS in `dist/_astro/` contains the `scroll-margin-top: 5rem` rule (and the `lg:` 6rem rule, either as a separate selector under the lg media query or as a 6rem value); `npm run check` + `npm run build` exit 0; global.css still contains the Phase 2 `scroll-behavior: smooth` line (unmodified).</done>
</task>

<task type="auto">
  <name>Task 2: Replace Nav.astro with sticky + scrolled-state + hamburger version</name>
  <files>src/components/ui/Nav.astro</files>
  <action>
**Replace `src/components/ui/Nav.astro` in full** with the extended version per the `<nav_template>` block in context. This is an in-place replacement of the Phase 2 static Nav.

Critical details to preserve from the template:

1. **Links array** — exactly five entries in order `Home (#hero) / About / Pricing / Testimonials / Contact`. NO `#portfolio` entry per D-03 (Phase 4 inserts it when the real gallery ships). The Phase 2 Nav had only 4 links (no `#hero`); this plan adds `#hero` so the mobile-menu's first item gives users a "back to top" target.

2. **`position: sticky` + `top-0` + `z-50`** on the outer `<nav>` via the literal Tailwind utility chain — Tailwind v4 compiles `sticky` to `position: sticky` and `top-0` to `top: 0` (verify these emit in built CSS).

3. **`data-scrolled="false"` initial attribute** on the `<nav>` element so the IntersectionObserver flip has a starting state. The `data-[scrolled=true]:bg-cream/90 data-[scrolled=true]:border-rule` utilities are statically detectable string literals — Tailwind v4 scanner emits them without a safelist comment (per Phase 2 SUMMARY note about static literals).

4. **Initial state `bg-cream/40 backdrop-blur-sm border-b border-transparent`** — NOT fully transparent (per RESEARCH §6 + Pitfall 7 + D-11). This guarantees contrast over any hero photo composition.

5. **Hamburger button** is `<button type="button" id="nav-toggle">` with `h-11 w-11` (44×44 px = WCAG 2.5.5 minimum), `aria-expanded="false"`, `aria-controls="nav-mobile-menu"`, `aria-label="Toggle menu"`, the focus-visible bronze ring matching Phase 2 Button pattern, and an inline 3-line SVG (M3 6h18 / M3 12h18 / M3 18h18). The SVG `<path>` elements get NO classes — the CSS swap to an X icon is intentionally OMITTED (the lines stay as lines whether open or closed; the visual cue is the menu appearing below, not the icon morphing). RESEARCH §5 documents this — "no janky height-auto transition trick".

6. **Mobile menu `<ul id="nav-mobile-menu">`** starts with classes `sm:hidden hidden flex-col gap-0 border-t border-rule bg-cream`. The two `hidden` instances: `sm:hidden` (always hidden on `sm:` and up — desktop has its own `<ul>`); plain `hidden` (initial closed state — JS toggles by removing/adding this class). Critical: those two `hidden` utilities co-exist; `sm:hidden` keeps the menu hidden on desktop regardless of toggle state.

7. **Mobile link tap targets** — `block px-6 py-4 text-base` → 48px tall (16px body text + 16+16 vertical padding) — WCAG 2.5.5 compliant.

8. **`<script>` block** at the end of the Astro component (after the `</nav>`). The script is module-scoped per Astro convention; Vite hoists and bundles it into `dist/_astro/*.js`. The script combines BOTH the IntersectionObserver flip AND the hamburger logic into one place — per RESEARCH §"Alternatives Considered" ("Inline `<script>` per component" preferred over separate `src/scripts/nav.ts` — Phase 3 has only one component needing JS, so a separate file is over-organization).

9. **Hamburger script handlers** — exactly the five behaviours from RESEARCH §5:
   - `click` on toggle → toggle aria-expanded + remove/add hidden class on menu
   - `click` on any menu link → close (so anchor-jump doesn't leave menu open mid-scroll)
   - `keydown` Escape (when menu open) → close + return focus to toggle (WCAG 2.1.2)
   - `click` outside both toggle AND menu → close
   - `resize` crossing window.innerWidth ≥ 640 with menu open → close (so phone rotation / browser resize doesn't orphan menu)

   The `e.target as Node` TypeScript cast in the click-outside handler MUST stay — Astro's `<script>` blocks are processed by Vite with TypeScript, and `EventTarget` is not a `Node` in the type system.

10. **IntersectionObserver feature-check** — `'IntersectionObserver' in window` guard; if missing (no realistic case in 2026), Nav silently stays in `data-scrolled="false"` (initial frost). Graceful degradation.

11. **`motion-safe:transition-colors`** on the `<nav>` — the background swap is the ONLY animation in this plan; under `prefers-reduced-motion: reduce` (DESIGN-05) it becomes an instant state change, no easing. Phase 2's blanket reduced-motion CSS rule is the backstop.

12. **focus-visible outline-bronze** on every interactive element (desktop links, mobile links, hamburger toggle) — matches Phase 2 Button convention; keyboard users get the bronze ring everywhere.

After writing, run `npm run check` (TypeScript happy with the `e.target as Node` cast + the `Astro.props` destructuring), then `npm run build`. Inspect `dist/_astro/*.js` to confirm the bundled script contains both `IntersectionObserver` and `nav-toggle` references.

**Negative invariant:** the file MUST NOT contain `<details>`, `<summary>`, or any `<input type="checkbox">` (per Pitfall 3 + Pitfall 4 — both are explicitly rejected a11y patterns).
  </action>
  <verify>
    <automated>cd /c/projects/new-project && grep -q "sticky top-0 z-50" src/components/ui/Nav.astro && grep -q "data-scrolled=\"false\"" src/components/ui/Nav.astro && grep -q "data-\[scrolled=true\]:bg-cream/90" src/components/ui/Nav.astro && grep -q "bg-cream/40 backdrop-blur-sm" src/components/ui/Nav.astro && grep -q "aria-label=\"Primary\"" src/components/ui/Nav.astro && grep -q "aria-label=\"Toggle menu\"" src/components/ui/Nav.astro && grep -q "aria-expanded=\"false\"" src/components/ui/Nav.astro && grep -q "aria-controls=\"nav-mobile-menu\"" src/components/ui/Nav.astro && grep -q "id=\"nav-toggle\"" src/components/ui/Nav.astro && grep -q "id=\"nav-mobile-menu\"" src/components/ui/Nav.astro && grep -q "IntersectionObserver" src/components/ui/Nav.astro && grep -q "data-nav-sentinel" src/components/ui/Nav.astro && grep -q "addEventListener('keydown'" src/components/ui/Nav.astro && grep -q "Escape" src/components/ui/Nav.astro && grep -q "addEventListener('resize'" src/components/ui/Nav.astro && grep -q "href: '#hero'" src/components/ui/Nav.astro && ! grep -qE "<details|<summary|type=\"checkbox\"" src/components/ui/Nav.astro && ! grep -q "href: '#portfolio'" src/components/ui/Nav.astro && npm run check && npm run build</automated>
  </verify>
  <done>Nav.astro contains `sticky top-0 z-50` + `data-scrolled="false"` initial + `data-[scrolled=true]:bg-cream/90` + initial `bg-cream/40 backdrop-blur-sm` + all hamburger aria-* attributes + Escape handler + resize handler + IntersectionObserver + reference to `data-nav-sentinel` + the new `#hero` link; contains NO `<details>`/`<summary>`/`<input type="checkbox">`; contains NO `#portfolio` link (per D-03); `npm run check` + `npm run build` both exit 0.</done>
</task>

<task type="auto">
  <name>Task 3: Plan-level build verification + bundle inspection + functional smoke</name>
  <files>(no source edits — verification only; if Section.astro / Nav.astro / BaseLayout.astro / global.css need a tiny correction surfaced by this task's checks, edit minimally and document under Deviations)</files>
  <action>
This task runs the full plan-level verification suite — every check from `<verification>` below — and inspects the production bundle for the expected interactive layer.

**Step 1 — Clean build:**
```
cd /c/projects/new-project
npm run check
npm run build
```
Both must exit 0.

**Step 2 — Verify the JS bundle:**

Astro/Vite bundles the inline `<script>` from Nav.astro into a hashed `dist/_astro/*.js` file. Inspect it:
```
ls dist/_astro/*.js
grep -l "nav-toggle" dist/_astro/*.js
grep -l "IntersectionObserver" dist/_astro/*.js
```
At least one `.js` file should match both greps (same file or two files — Vite may or may not split). Per RESEARCH §5 bundle-cost estimate: ~25-30 lines of JS minify to ~600-800 bytes; the bundled `.js` file size should be well under 5 KB total.

Confirm `dist/index.html` references the bundled script via a `<script type="module" src="/_astro/...js">` tag near the closing `</body>` (Astro's default emit location).

**Step 3 — Verify the sticky/scrolled-state CSS:**

```
grep -rqE "\.sticky\{position: ?sticky" dist/_astro/*.css || grep -rqE "position: ?sticky" dist/_astro/*.css
grep -rqE "data-scrolled" dist/_astro/*.css
grep -rqE "(backdrop-filter|--tw-backdrop-blur)" dist/_astro/*.css
```
All three should match. The `data-[scrolled=true]:` variants compile to attribute selectors like `[data-scrolled="true"]\:bg-cream\/90` — escaped colons per the Phase 2 Deviation lesson; use whitespace-tolerant / escape-tolerant regex if a strict match fails.

**Step 4 — Verify scroll-margin-top compiled for every section:**

```
grep -rqE "scroll-margin-top: ?5rem" dist/_astro/*.css
grep -rqE "scroll-margin-top: ?6rem" dist/_astro/*.css  # under the lg: media query
```
Both should match. If the `lg:` variant grep fails because of minification (e.g., the rule is nested inside `@media(min-width:64rem){...}` without a space), use a whitespace-tolerant regex per Phase 2 lesson.

**Step 5 — Functional sentinel + script wiring smoke:**

Boot preview, curl `/` and grep for the literal sentinel `<div>` + the bundled `<script type="module">` tag:
```
npm run preview &
PREVIEW_PID=$!
sleep 3
curl -s http://localhost:4321/ | grep -q 'data-nav-sentinel'
curl -s http://localhost:4321/ | grep -qE '<script type="module" src="/_astro/'
curl -s http://localhost:4321/privacy/ | grep -q 'data-nav-sentinel'
kill $PREVIEW_PID
```
All three must succeed (both `/` and `/privacy` get the sentinel because both consume BaseLayout).

**Step 6 — OPTIONAL Lighthouse mobile a11y on `/` (matches Phase 2's 100/100 bar):**

If `npx --yes lighthouse@latest` runs in this environment (Phase 2 had a transient SSL retry — re-run on failure):
```
npm run preview &
PREVIEW_PID=$!
sleep 3
npx --yes lighthouse@latest http://localhost:4321/ \
  --form-factor=mobile \
  --throttling-method=provided \
  --only-categories=accessibility,performance \
  --output=json \
  --output-path=/tmp/lh-03-03.json \
  --quiet \
  --chrome-flags="--headless --no-sandbox"
kill $PREVIEW_PID

node -e "const r=require('/tmp/lh-03-03.json'); console.log('a11y:',r.categories.accessibility.score*100,'perf:',r.categories.performance.score*100)"
```
Targets (per RESEARCH §12 prediction): Accessibility 100, Performance ≥90.

If Lighthouse is unavailable, skip — this is a Phase 6 gate, not a Phase 3 blocker. Document skip under Deviations.

**Step 7 — Manual checklist (for the SUMMARY — these CANNOT be automated; mark as `human_checkpoint_pending` per Phase 2 SUMMARY pattern):**
- Open `npm run preview` URL in a real browser
- Resize to 320px width — hamburger appears, desktop nav hidden
- Tap hamburger — menu opens, aria-expanded flips to "true"
- Tap a menu link (e.g., About) — menu closes, page smooth-scrolls to #about, About heading sits clear of sticky nav
- Tap hamburger again, press Escape — menu closes, focus returns to hamburger button
- Tap hamburger, click outside (on body content) — menu closes
- Tap hamburger, rotate to landscape past 640px — menu auto-closes
- Resize to 1024px — desktop nav reappears, hamburger disappears
- Scroll past hero — Nav background visibly flips from subtle frost to cream-90% with bottom border
- DevTools → Rendering → prefers-reduced-motion: reduce → reload → click About link: scroll is INSTANT (not smooth); reduced-motion override from Phase 2 still active

Record these as `human_checkpoint_pending` items in the SUMMARY frontmatter for the orchestrator to ask the user to verify.

**No source edits in this task unless verification finds a regression.** If a regression is found (e.g., scroll-mt-24 doesn't compile because Tailwind's spacing scale stops at 24 — it doesn't, this is a hypothetical; OR data-[scrolled=true]: variant doesn't emit), patch the minimum required file and document under Deviations per Phase 2 convention.
  </action>
  <verify>
    <automated>cd /c/projects/new-project && npm run check && npm run build && ls dist/_astro/*.js && (grep -lq "nav-toggle" dist/_astro/*.js || grep -lq "IntersectionObserver" dist/_astro/*.js) && grep -qE '<script[^>]*type="module"[^>]*src="/_astro/' dist/index.html && grep -q "data-nav-sentinel" dist/index.html && grep -q "data-nav-sentinel" dist/privacy/index.html && grep -rqE "position: ?sticky" dist/_astro/ && grep -rqE "scroll-margin-top: ?5rem" dist/_astro/ && grep -rq "data-scrolled" dist/_astro/ && grep -q "id=\"nav-toggle\"" dist/index.html && grep -q "aria-expanded=\"false\"" dist/index.html && grep -q "id=\"nav-mobile-menu\"" dist/index.html</automated>
  </verify>
  <done>Production build successful; bundled JS contains hamburger + IntersectionObserver references; `<script type="module">` tag present in built HTML; sticky CSS + data-scrolled selectors + scroll-margin-top utilities all emitted to `dist/_astro/`; sentinel `<div>` present on both `/` and `/privacy`; hamburger toggle + mobile menu IDs + aria-expanded initial state all present in `dist/index.html`. Manual checklist enumerated for human checkpoint per Phase 2 pattern.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser DOM ↔ vanilla JS handlers | All script logic runs in the user's browser against well-defined DOM ids (`nav-toggle`, `nav-mobile-menu`, `[data-nav-sentinel]`). No data ingress; no fetch; no eval; no innerHTML mutation. |
| Vite bundle → CF Pages CDN | The hoisted `<script>` becomes one `dist/_astro/*.js` file served with 1y cache. No third-party JS, no analytics beacon (Phase 5 ships Cloudflare Web Analytics). |
| User keyboard input → focus management | Escape handler returns focus to toggle (WCAG 2.1.2) — no focus trap. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Tampering | `<details>/<summary>` accidentally adopted as hamburger | mitigate | Task 2 verify gate `! grep -qE "<details|<summary|type=\"checkbox\"" src/components/ui/Nav.astro` fails the build if any of the three rejected patterns appear. RESEARCH §5 + Pitfall 3/4 document the reasons (VoiceOver gaps, no Escape, no focus management). |
| T-03-13 | Denial of service (perf) | `window.scroll` listener flips Nav background every frame | mitigate | RESEARCH §6 + Pitfall 6: implementation uses IntersectionObserver (callback-once-per-state-change, off-main-thread) instead of `scroll` listener. Task 3 verify confirms `IntersectionObserver` literal in bundled JS. |
| T-03-14 | Information disclosure | Nav background fully transparent over arbitrary hero photo causes contrast failure | mitigate | Initial state `bg-cream/40 backdrop-blur-sm border-b border-transparent` (NOT fully transparent — per RESEARCH §6 + Pitfall 7 + D-11). Worst-case ≥5:1 contrast on Ink (#1A1A1A) text over any composition. |
| T-03-15 | Repudiation (a11y) | Hamburger menu open with no way to close via keyboard | mitigate | Five close paths: link click / Escape (+ focus return) / outside click / resize crossing 640px / second toggle click. RESEARCH §5 a11y checklist enumerates all five. |
| T-03-16 | Tampering (forward-looking) | Phase 4 inserts `#portfolio` nav link but forgets to update the `links` array AND the mobile menu | mitigate (documented) | The `links` array in Nav.astro is the single source of truth — both desktop `<ul>` and mobile `<ul>` map over the same array. Phase 4 plan must edit ONE line. SUMMARY next-phase-readiness enumerates this. |
| T-03-17 | Performance regression (LCP) | New `<script>` blocks hero render | mitigate | Astro defaults `<script>` to `type="module"` which is implicitly deferred — does NOT block parsing or LCP. Verify by inspecting `dist/index.html` for the literal `type="module"`. |
| T-03-SC | Tampering (supply chain) | npm installs | n/a | Zero new packages. Inline vanilla JS only; no Alpine/Stimulus/petite-vue (rejected per RESEARCH §"Alternatives Considered"). |
</threat_model>

<verification>
Plan-level checks (run after all 3 tasks complete):

```bash
cd /c/projects/new-project

# Build cleanly
npm run check
npm run build

# CONTENT-06 part 1 — sticky nav with scrolled-state attribute swap
grep -q 'sticky top-0 z-50' src/components/ui/Nav.astro
grep -q 'data-scrolled="false"' src/components/ui/Nav.astro
grep -q 'data-\[scrolled=true\]:bg-cream/90' src/components/ui/Nav.astro
grep -q 'bg-cream/40 backdrop-blur-sm' src/components/ui/Nav.astro
grep -q 'IntersectionObserver' src/components/ui/Nav.astro
grep -q 'data-nav-sentinel' src/components/ui/Nav.astro
grep -q 'data-nav-sentinel' src/layouts/BaseLayout.astro

# CONTENT-06 part 2 — mobile hamburger
grep -q 'id="nav-toggle"' src/components/ui/Nav.astro
grep -q 'id="nav-mobile-menu"' src/components/ui/Nav.astro
grep -q 'aria-expanded="false"' src/components/ui/Nav.astro
grep -q 'aria-controls="nav-mobile-menu"' src/components/ui/Nav.astro
grep -q 'aria-label="Toggle menu"' src/components/ui/Nav.astro
grep -q "addEventListener('keydown'" src/components/ui/Nav.astro
grep -q 'Escape' src/components/ui/Nav.astro
grep -q "addEventListener('resize'" src/components/ui/Nav.astro
grep -q 'h-11 w-11' src/components/ui/Nav.astro  # WCAG 2.5.5 target size

# CONTENT-06 part 3 — smooth scroll + offset
grep -q 'scroll-mt-20' src/components/ui/Section.astro
grep -q 'lg:scroll-mt-24' src/components/ui/Section.astro
grep -q 'scroll-behavior: smooth' src/styles/global.css  # Phase 2 already shipped this

# Negative invariants — rejected patterns must NOT appear
! grep -qE '<details|<summary|type="checkbox"' src/components/ui/Nav.astro
! grep -q "href: '#portfolio'" src/components/ui/Nav.astro  # D-03

# Built HTML evidence
grep -q 'data-nav-sentinel' dist/index.html
grep -q 'data-nav-sentinel' dist/privacy/index.html
grep -q 'id="nav-toggle"' dist/index.html
grep -q 'id="nav-mobile-menu"' dist/index.html
grep -qE '<script[^>]*type="module"[^>]*src="/_astro/' dist/index.html

# Built CSS evidence — sticky + scrolled-state + scroll-margin-top all emitted
grep -rqE 'position: ?sticky' dist/_astro/
grep -rq 'data-scrolled' dist/_astro/
grep -rqE 'scroll-margin-top: ?5rem' dist/_astro/
grep -rqE 'scroll-margin-top: ?6rem' dist/_astro/

# Built JS evidence — bundled vanilla JS contains both behaviours
ls dist/_astro/*.js
(grep -lq 'nav-toggle' dist/_astro/*.js && grep -lq 'IntersectionObserver' dist/_astro/*.js) || echo "WARN: bundled JS may have been split — verify manually"

# Preview boots + routes resolve
npm run preview &
PREVIEW_PID=$!
sleep 3
curl -sf http://localhost:4321/ -o /dev/null
curl -sf http://localhost:4321/privacy/ -o /dev/null
curl -sf http://localhost:4321/styleguide -o /dev/null  # Phase 2 styleguide still works
kill $PREVIEW_PID

# OPTIONAL — Lighthouse mobile (Phase 6 gate; Phase 3 aspirationally targets 100/100 a11y like Phase 2)
# npx --yes lighthouse@latest http://localhost:4321/ --form-factor=mobile --only-categories=accessibility,performance --output=json --output-path=/tmp/lh-03-03.json --quiet
# node -e "const r=require('/tmp/lh-03-03.json'); if (r.categories.accessibility.score<0.95) process.exit(1); if (r.categories.performance.score<0.90) console.warn('perf below 90 — likely placeholder hero weight; revisit in Phase 4')"
```
</verification>

<success_criteria>
- CONTENT-06 satisfied in three layers:
  1. **Sticky behaviour** — `<nav>` carries `position: sticky top: 0 z-index: 50` via Tailwind utilities; stays pinned through every section.
  2. **Scrolled-state background swap** — IntersectionObserver watching the 1px `[data-nav-sentinel]` flips `data-scrolled` between `"false"` (subtle frost `bg-cream/40 backdrop-blur-sm`) and `"true"` (more opaque `bg-cream/90` + visible `border-rule` bottom).
  3. **Smooth-scroll anchor offset** — `scroll-mt-20 lg:scroll-mt-24` on every `<Section>` via Section.astro baseline edit ensures clicking any in-page anchor lands the section heading clear of the Nav (CSS-only; Phase 2's `html { scroll-behavior: smooth }` + reduced-motion override already in place).
  4. **Mobile hamburger** — `<button aria-expanded>` (NOT `<details>`/`<summary>`/checkbox-trick per RESEARCH §5 + Pitfalls 3 + 4); five close paths (link click / Escape with focus return / outside click / resize / second tap); `h-11 w-11` WCAG-compliant tap target; bronze focus-visible ring.
- All five in-page anchor IDs (`#hero`, `#about`, `#pricing`, `#testimonials`, `#contact`) resolve and land cleanly under the sticky Nav. `#portfolio` from 03-01's PortfolioStub also exists in HTML (no Nav link to it per D-03, but the hash resolves).
- Bundled JS contains both `IntersectionObserver` and `nav-toggle` references; `<script type="module">` tag present in `dist/index.html`; bundle size well under 5 KB.
- `npm run check` + `npm run build` exit 0; preview serves `/`, `/privacy`, and `/styleguide` with 200; sentinel `<div>` appears on every page that uses BaseLayout.
- No new npm packages; no `<details>`/`<summary>`/checkbox-trick anywhere; no `#portfolio` link in Nav per D-03.
- `prefers-reduced-motion: reduce` continues to disable smooth scroll (Phase 2 CSS unchanged); the Nav background transition `motion-safe:transition-colors` also becomes instant under reduced-motion via the same Phase 2 blanket override.
- Phase 3 closes — all CONTENT-01..07 requirements observable in built HTML + interactive behaviours pass the manual mobile-device smoke test (human checkpoint enumerated for the orchestrator).
</success_criteria>

<output>
Create `.planning/phases/03-static-content-sections/03-03-SUMMARY.md` documenting:

- **Performance:** duration, tasks count (3), files modified (Section.astro, Nav.astro, BaseLayout.astro; optionally global.css if a regression was patched), npm packages added (0).
- **Accomplishments:** sticky nav with IntersectionObserver scrolled-state flip + mobile hamburger with full a11y handling (5 close paths) + universal scroll-margin-top baseline + 1px sentinel inside BaseLayout. Quote the actual bundled JS file size (bytes) measured from `dist/_astro/*.js`.
- **Task Commits:** one row per task with subject + hash + files.
- **Files Modified:** explicit list — Section.astro (1-line class-list edit), Nav.astro (full replacement), BaseLayout.astro (1-line sentinel insertion), global.css (unmodified — verify line).
- **Decisions Made:** any in-script TypeScript type-cast adjustments forced by Astro 6 + Vite TS strictness (e.g., `e.target as Node`); whether IntersectionObserver feature-detect actually triggered the silent-fallback path in any tested browser (modern browsers all support it — likely no).
- **Deviations from Plan:** per Phase 2 convention — categorise Rule 1/2/3. Likely candidates: (a) `data-[scrolled=true]:` variant emits with backslash-escaped colon in CSS (same root cause as Phase 2 Deviation 1) — verify gate should use whitespace + escape-tolerant regex; (b) the bundled `<script>` might land in either a Nav-specific `.js` file or the page bundle — Vite makes this choice, both are acceptable.
- **Verify Gates:** per-task + plan-level table mirroring Phase 2 SUMMARY format.
- **Threat Surface Scan:** confirm T-03-12..17 mitigations enumerated; confirm bundle size; confirm no rejected hamburger patterns.
- **Known Stubs:** None. This plan closes Phase 3.
- **User Setup Required:** None.
- **Human Checkpoint Items Still Pending:** The 10-item manual checklist from Task 3 Step 7 (hamburger toggle / Escape / click-outside / resize / scroll-state / reduced-motion smoke). These cannot be headless-automated; orchestrator should pause for the user to walk through them in a real browser before declaring Phase 3 complete.
- **Next Phase Readiness:**
  - **PHASE 3 CLOSED:** Every CONTENT-01..07 requirement has shipping evidence in built HTML / built CSS / built JS. The home page reads as a complete editorial site (modulo the visible `[OWNER-REVIEW]` / `[OWNER-CONFIRM:*]` tags that explicitly invite owner walkthrough).
  - **READY FOR PHASE 4 (Portfolio Gallery & Image Pipeline):**
    - Phase 4 must DELETE `src/assets/placeholder/` (5 images + LICENSES.md) and REPLACE `src/components/sections/PortfolioStub.astro` with the real `Portfolio.astro` gallery (justified grid + lightbox per GALLERY-01..04).
    - Phase 4 must ADD `{ href: '#portfolio', label: 'Portfolio' }` to the `links` array in `src/components/ui/Nav.astro` (single source of truth — desktop nav AND mobile menu both map this array). Recommended insertion: AFTER Home, BEFORE About.
    - Phase 4 must repoint Hero / About portrait / Pricing accent image imports from `@/assets/placeholder/*` to whichever final paths Phase 4 establishes (likely `@/assets/portfolio/`).
    - testimonial-1.jpg + testimonial-2.jpg are currently unused (RESEARCH §1 — no testimonial author photos); Phase 4 decides: delete, repurpose for portfolio thumbs, or retain as unused.
  - **READY FOR PHASE 5 (Contact Form Backend & GDPR):**
    - Contact form swap protocol (4 steps) documented in 03-02 SUMMARY; no changes from this plan affect it.
    - `/privacy` stub body replacement (GDPR-01) — also documented in 03-02 SUMMARY.
    - When privacy policy ships for real, Phase 5 removes `Disallow: /privacy` from `public/robots.txt` and removes the `<meta name="robots" content="noindex, nofollow">` from `src/pages/privacy.astro`.
  - **READY FOR PHASE 6 (SEO + Sitemap + Launch):**
    - All section IDs + nav anchors + footer privacy link are stable — Phase 6 sitemap generation will index only the routes that are NOT in `robots.txt` Disallow.
    - The BaseLayout `<slot name="head" />` ordering invariant (after `<Font />` preloads — per Phase 2 SUMMARY) is preserved by THIS plan (no head-slot edits). Phase 6 06-01 SEO meta injection should continue to respect this order.
    - Phase 6 should re-run Lighthouse mobile and confirm Performance ≥90 / Accessibility ≥95 (Phase 3 aspirationally targets 100/100 a11y).

Use the `human_checkpoint_pending` frontmatter block per Phase 2 SUMMARY 02-02 convention to enumerate the 10 manual smoke-test items.
</output>

<done>
- [ ] `src/components/ui/Section.astro` includes `scroll-mt-20 lg:scroll-mt-24` in the `class:list`.
- [ ] `src/layouts/BaseLayout.astro` has `<div data-nav-sentinel>` as the first child of `<body>`.
- [ ] `src/components/ui/Nav.astro` is the extended version: `position: sticky top-0 z-50`, `data-scrolled="false"` initial, `bg-cream/40 backdrop-blur-sm` initial state, `data-[scrolled=true]:bg-cream/90 data-[scrolled=true]:border-rule` scrolled state, mobile hamburger `<button id="nav-toggle">` with full aria-* attributes + `h-11 w-11`, mobile menu `<ul id="nav-mobile-menu">` with `hidden` initial, single `<script>` block with IntersectionObserver + 5-path hamburger close handling.
- [ ] Nav.astro `links` array contains exactly 5 entries (Home / About / Pricing / Testimonials / Contact); contains NO `#portfolio` per D-03.
- [ ] Nav.astro contains NO `<details>`, NO `<summary>`, NO `<input type="checkbox">`.
- [ ] `src/styles/global.css` is unmodified (Phase 2 `scroll-behavior: smooth` + reduced-motion override already in place).
- [ ] `npm run check` and `npm run build` both exit 0.
- [ ] `dist/index.html` contains the sentinel, the hamburger toggle id, the mobile menu id, the bundled `<script type="module">` tag.
- [ ] `dist/privacy/index.html` also contains the sentinel.
- [ ] `dist/_astro/*.css` contains `position: sticky`, `data-scrolled` selectors, `scroll-margin-top: 5rem`, and the 6rem variant under the lg media query.
- [ ] `dist/_astro/*.js` contains `nav-toggle` and `IntersectionObserver` references.
- [ ] Manual 10-item smoke checklist enumerated as `human_checkpoint_pending` in `03-03-SUMMARY.md`.
- [ ] One commit per task on `main` (3 commits total), conventional-commit-styled.
- [ ] `03-03-SUMMARY.md` written.
</done>
